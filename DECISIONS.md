# Architectural Decision Log

Format: each entry records context, the decision, and consequences. Entries are never edited after the fact — superseded decisions get a new entry that references the old one.

---

## ADR-001: pnpm + Turborepo for monorepo tooling

**Date:** 2026-07-31

**Context:** The platform needs to host multiple future applications (corporate, studio, technologies, travel, docs) sharing common tooling and, eventually, common packages.

**Decision:** Use pnpm workspaces for package management and Turborepo for task orchestration/caching, per the stack defined in CLAUDE.md.

**Consequences:** All apps and packages live under `apps/*` and `packages/*`. All scripts run through `turbo run <task>`. npm and yarn are disallowed to avoid lockfile drift.

---

## ADR-002: Build first, abstract second — no premature `packages/ui` or `packages/design-system`

**Date:** 2026-07-31

**Context:** The monorepo structure anticipates shared packages (`packages/ui`, `packages/design-system`), but no UI exists yet. Creating these packages before any real component exists would mean guessing at abstractions instead of discovering them from actual usage.

**Decision:** Defer creating `packages/ui` and `packages/design-system` until the corporate app is built out and reusable patterns have proven themselves through real duplication. Build the corporate website directly first; extract shared code only when a second consumer needs it.

**Consequences:** `packages/` remains empty through the corporate app build phase. When extraction happens, it should be recorded as a new ADR describing what was extracted and why.

---

## ADR-003: Root documentation set

**Date:** 2026-07-31

**Context:** CLAUDE.md defines engineering standards but not company-facing docs, contribution workflow, roadmap, or brand rules.

**Decision:** Add README.md, ARCHITECTURE.md, CONTRIBUTING.md, ROADMAP.md, BRAND.md, and this file (DECISIONS.md) at the repo root, to be kept up to date as the project evolves.

**Consequences:** Every phase of work should end with an update to the relevant doc(s) — architecture changes go in ARCHITECTURE.md, scope changes in ROADMAP.md, and any non-obvious structural choice gets a new ADR here.

---

## ADR-004: App directory named `web`, not `corporate`

**Date:** 2026-07-31

**Context:** CLAUDE.md originally specified `apps/corporate` for the corporate website. The user opted for `apps/web` instead.

**Decision:** The application directory is `apps/web`. "Corporate website" remains the name of the *product/business concept* (what the site is for), but the codebase directory is `web` — a more generic, durable name that doesn't need to change if the site's purpose is later described differently.

**Consequences:** All references to `apps/corporate` across CLAUDE.md, README.md, ARCHITECTURE.md, and ROADMAP.md were updated to `apps/web`. Any future doc or code referencing the app directory should use `web`, not `corporate`.

---

## ADR-005: Next.js 16 instead of Next.js 15

**Date:** 2026-07-31

**Context:** CLAUDE.md originally specified Next.js 15. Running `create-next-app@latest` when scaffolding `apps/web` installed Next.js 16.2.12 (the current latest major) with React 19.2.4, since Next.js 15 is no longer what `@latest` resolves to.

**Decision:** Use Next.js 16 rather than force-pinning to 15. CLAUDE.md's Technology Stack section and other docs referencing "Next.js 15" were updated to "Next.js 16."

**Consequences:** The app is built against the current framework major rather than a version that's already behind. Any Next.js 15-specific guidance or assumptions elsewhere should be re-checked against Next.js 16 behavior before being relied on.

---

## ADR-006: TypeScript pinned to 6.0.3, not 7.x

**Date:** 2026-07-31

**Context:** `pnpm add typescript` resolved to 7.0.2 (the new native/Go-based compiler). `next build` failed against it: "TypeScript 7.0.2 does not provide the compiler API required by Next.js." Next.js offered two fixes — enable `experimental.useTypeScriptCli` in `next.config.ts` to shell out to the `tsc` CLI instead of using the API, or use TypeScript 6.

**Decision:** Pin `typescript` to `^6.0.3` across the whole workspace (root and `apps/web`). Do not enable `experimental.useTypeScriptCli`. Stability and long-term maintainability of the foundation take priority over adopting the newest TypeScript major before the ecosystem (Next.js included) has caught up to its new compiler API.

**Consequences:** The workspace type-checks and builds cleanly on the standard, fully-supported TypeScript 6 compiler API. Revisit this once Next.js officially supports TypeScript 7's compiler API without an experimental flag.

---

## ADR-009: `unda-design-os` — an explicit, requested override of "build first, abstract second"

**Date:** 2026-08-05

**Context:** ADR-002 established that `packages/ui`/`packages/design-system` would be extracted only after real reuse emerged from actual usage in `apps/web`. The user supplied three external documents (Brand Foundation Manual, Creative Direction, Design System v1.0) and requested a full enterprise-grade "Design OS" — foundations, tokens, 50+ documented components with per-state guidance, page patterns, and product-specific documentation for all four Unda divisions (Solutions, Technologies, Studio, Travel) — before any of those products have real UI built. This is the premature abstraction ADR-002 was written to avoid. The user was told this directly and asked to choose between scoping down to `apps/web` only, deferring entirely, or proceeding with the full multi-division scope as an explicit override. They chose to proceed with the full scope.

**Decision:** Build `unda-design-os/` as a standalone top-level documentation project (sibling to `apps/`, `packages/`, `docs/`, `infrastructure/`) containing brand, creative-direction, and design-system knowledge for the entire Unda ecosystem — including divisions with no real product yet (Studio, Technologies, Travel). This is a one-time, explicitly authorized exception to build-first-abstract-second, scoped to this documentation effort only. It does **not** change the underlying rule for `packages/ui`/`packages/design-system` inside the app codebase — those remain extracted from real usage, not authored speculatively. `unda-design-os` is reference material an app *can* draw from; it is not itself application code, and populating it does not obligate any app to consume it before it's actually useful to do so.

**Consequences:** A large volume of documentation will exist for components, patterns, and products that don't exist in code yet. This is a known, accepted trade-off. Built iteratively, phase by phase (Foundations → Tokens → Components → Patterns → Rules → Engineering → CLAUDE.md → README), pausing for review between phases rather than generated in one pass.

---

## ADR-010: Brand Foundation Manual adopted as the final design direction

**Date:** 2026-08-05

**Context:** CLAUDE.md's build-out of `apps/web` had been paused because the user still needed to "come up with the design system and website inspiration" (per the "coming soon" placeholder request). The attached Brand Foundation Manual makes specific, concrete design decisions: Royal Blue as the primary brand color, division accent colors (Purple/Cyan/Emerald for Studio/Technologies/Travel), General Sans for marketing/display typography, Geist for UI/product typography, Geist Mono for technical content, Lucide as the icon library, and an 8-point spacing grid with a matching border-radius scale.

**Decision:** Adopt these choices as final rather than draft/reference-only. This resolves the earlier "design system not yet chosen" blocker. Future design work on `apps/web` (and any other Unda product) should use these tokens rather than re-opening the choice of color/typeface/icon library.

**Consequences:** `unda-design-os/foundations/` becomes the authoritative source for these values. `BRAND.md` at the repo root should be updated to stop saying "not yet finalized" and instead point to `unda-design-os` for the concrete tokens. Once `apps/web`'s real content is built, it should consume these tokens rather than inventing new ones.

---

## ADR-007: Scaffold placeholder apps for all divisions ahead of the normal build order

**Date:** 2026-07-31

**Context:** CLAUDE.md's default rule is "never create future applications until requested," and the roadmap's original order was to fully build `apps/web` before touching Studio/Technologies/Travel/Docs. The user explicitly asked, before starting on website design/content, to configure Vercel for `undasolutions.com` and its four subdomains. Vercel domains attach to a project, and a project needs a deployable app — there was nothing to attach `studio.undasolutions.com` etc. to. The user was asked whether to configure Vercel for `apps/web` only (deferring the rest) or scaffold placeholders for all four subdomains now, and chose the latter.

**Decision:** Scaffold minimal "coming soon" Next.js apps for `studio`, `technologies`, `travel`, and `docs` now — each wired into the shared tooling (root `tsconfig.base.json`, root `biome.json` via `extends` + `root: false`, same Next.js 16 / React 19 / TypeScript 6.0.3 / Tailwind v4 versions as `apps/web`) but with no real content, features, or business logic beyond a single placeholder page. This is an explicit, requested exception to "build first, abstract second" — it does not change the rule for any future 6th app.

**Consequences:** All five apps in `apps/*` build, lint, and type-check clean as of this ADR. `studio`/`technologies`/`travel`/`docs` must not receive real content, sections, or features until separately requested — CLAUDE.md's "Current Project Goal" and "Applications" sections were updated to state this explicitly, so a future session doesn't mistake their existence for a green light to build them out.

---

## ADR-008: Vercel projects, GitHub integration, and domains for all 5 apps

**Date:** 2026-07-31

**Context:** Following ADR-007, the user wanted `undasolutions.com` and its four subdomains fully wired up on a new Vercel account/team (`unda-solutions1`) before starting on website content, so the deployment path exists ahead of time. This required: creating one Vercel project per app, setting each project's Root Directory (the Vercel CLI has no direct command for this — done via `PATCH /v9/projects/{id}` with `rootDirectory`/`framework`), authorizing the Vercel GitHub App on the `UndaPlatform` org (scoped to `unda-platform` only, not all repos — a manual step only the user could complete, done via GitHub's App-install redirect from a project's Settings → Git → Connect Git Repository, not GitHub Marketplace search), attaching all 6 domains (`undasolutions.com`, `www.undasolutions.com`, plus 4 subdomains) via `vercel domains add`, and setting `www` to 308-redirect to the apex.

**Decision:** DNS stays on Cloudflare (nameservers unchanged) with individual `A` records → `76.76.21.21` for each hostname, all set to **DNS only** (grey cloud) rather than proxied — Vercel needs the direct connection for routing/SSL. The existing `undasolutions.com` A record and the `www` CNAME (previously a Namecheap parking page) were edited in place rather than duplicated. Each Vercel project's Root Directory points at its `apps/<name>` folder so Git-triggered builds get the correct app while still checking out the full repo (needed since each app's `tsconfig.json`/`biome.json` extend root-level config via relative paths).

**Consequences:** All 5 projects are connected to GitHub — pushes to `main` auto-deploy. All 6 domains (apex + www + 4 subdomains) resolve and serve `200` in production. A manual empty commit (`e606a60`) was pushed specifically to trigger the first Git-based deployment and validate the whole pipeline (monorepo install, per-project Root Directory, shared config resolution) before starting on real content — this confirmed the setup works end-to-end. Any future app added to `apps/*` should follow the same pattern: create the Vercel project, set `rootDirectory`/`framework` via the API, connect Git (already authorized for this repo), then `vercel domains add`.

---

## ADR-011: `unda-design-os` built from the actual source PDFs, once located on disk

**Date:** 2026-08-05

**Context:** ADR-009/010 were based on a Brand Foundation Manual excerpt pasted directly into chat, which described design decisions (Royal Blue, General Sans/Geist/Geist Mono, Lucide, 8pt grid) without literal implementable values — the manual itself noted the actual hex/RGB/HSL palette was meant for a section not included in what was pasted. `design-system/foundations/` was therefore deliberately written at the philosophy/structure level only, with literal values explicitly deferred to a later Tokens phase. While starting that Tokens phase, the actual source PDFs referenced by the user's original prompt — `Creative Direction v1.0` (8 pages) and `Design System v1.0` (18 pages, with a companion interactive version) — were found in `~/Downloads` and read in full. These contain the real literal values (OKLCH color tokens, exact type/spacing/motion scales, full ~20-component library with anatomy/states/accessibility/engineering notes per component, Figma library architecture, engineering handoff conventions, and an explicit "AI Design Rules" section) that the earlier phases had to work around.

**Decision:** Treat `Creative Direction v1.0` and `Design System v1.0` as the authoritative source for all subsequent Design OS phases, superseding reconstruction/inference where the two differ. Retroactively populated `creative-direction/` (previously an empty stub — no source text had been pasted for it) directly from `Creative Direction v1.0`. Wrote `design-system/tokens/` directly from `Design System v1.0` §II–III's literal values rather than proposing placeholder numbers. Phase 1 (`foundations/`) required no rework — its philosophy-only scope was already consistent with the literal tokens now in hand, validating that split. `Brand Foundations.pdf` (320 pages) also exists on disk and is substantially larger than what was pasted for Phase 1; it was not re-processed in this pass to avoid unrequested rework of already-committed, thorough Phase 1 content — reconciling it is a candidate for a future, explicitly scoped pass if discrepancies turn up.

**Consequences:** Phases 3–7 (Components, Patterns, Rules, Engineering, `CLAUDE.md`) should be written directly from `Design System v1.0` §V (Component Library), §VI (Figma Library Architecture), §VII (Engineering Handoff), §VIII (AI Design Rules), and §IX (Cross-Brand Usage) rather than re-derived from first principles — that source already specifies most of this content at the needed level of detail.

---

## ADR-012: `Logo Design System v1.0` added to `brand/`

**Date:** 2026-08-05

**Context:** The user supplied a fifth source document, `Logo Design System v1.0` (5 pages), containing the symbol's construction geometry (8×8 grid, module/arc measurements), wordmark lockups, clear space and minimum-size rules, color variations, division lockup treatment, app icon/favicon specs, and misuse rules — none of which existed anywhere in `unda-design-os/brand/` (the earlier Brand Foundation Manual excerpt discussed logo strategy at a conceptual level only, never construction specifics).

**Decision:** Add `brand/logo-system.md` transcribing this document directly, cross-linked to `design-system/tokens/color-tokens.md` (brand blue) and `design-system/tokens/radius-elevation-and-icon-tokens.md` (explicitly noting the logo's own construction-grid units are a separate, self-contained system from the interface radius scale — the two must not be conflated).

**Consequences:** `brand/` now has 10 files. No other phase required rework — this was purely additive.

---

## ADR-013: Brand Expression System v1.0 — full 12-section scope, including physical touchpoints

**Date:** 2026-08-05

**Context:** Following ADR-009 (which scoped `unda-design-os` to brand/creative-direction/design-system documentation, explicitly justified as forward-planning for a real multi-division digital business), the user requested a further deliverable — a "Brand Expression System v1.0" covering 12 sections: Expression Principles, Layout Language, Photography Direction, Illustration System, Iconography, Motion Language, Graphic Language, Data Visualization, Marketing Assets, Presentation System, Brand in Practice, and Future Expansion Rules. Unlike every prior phase, most of this content is not transcribed from a source document — it requires originating new creative decisions (photography color grading, illustration line weight, motion recipes, chart styling) on top of the already-locked foundations. Sections 9–11 additionally specify production standards for physical/organizational collateral (business cards, letterheads, apparel, stickers, notebooks, merchandise, office signage, conference banners, pitch decks) for a company that is currently a placeholder site with no office, physical product, or team collateral in production — the same category of premature-abstraction concern `brand/implementation-roadmap.md` (written in this project during Phase 1) explicitly flagged. The user was asked to choose between a digital-first core scope, the narrower apps/web-only scope, or the full 12-section system as specified, and chose the full scope.

**Decision:** Build the complete 12-section Brand Expression System as two deliverables under a new top-level `unda-design-os/brand-expression/` folder: `manual/` (the narrative "Brand Expression Manual," one file per section) and `specification/` (the production-ready spec — tokens, naming conventions, file structure, QA checklist, governance, versioning). New creative decisions (photography grading, illustration specifics, motion recipes, graphic language, data-viz styling, physical-collateral standards) are originated consistently with already-locked foundations (Royal Blue/division accents, General Sans/Geist/Geist Mono, the 8pt spacing grid, existing motion token values, the Radial Frame logo geometry) — nothing here overrides or contradicts an existing token or rule, it only extends into territory those didn't cover. Physical-collateral specifications in Brand in Practice are written as ready-to-produce specs but explicitly marked as *not yet in production* — this is a considered blueprint for when a real need arises (an actual business card, an actual office), not a claim that these items currently exist.

**Consequences:** A significant volume of documentation now exists for touchpoints (merchandise, signage, presentations) that have no current real-world use. This is a known, explicitly authorized trade-off, same category as ADR-009 but larger in scope. Future sessions should not treat the existence of e.g. `brand-expression/manual/11-brand-in-practice.md`'s apparel section as license to actually commission apparel — that still requires a real, separate business decision.

---

## ADR-014: First real implementation of `unda-design-os` tokens in `apps/web`

**Date:** 2026-08-05

**Context:** Starting on `apps/web`'s real content required first closing three concrete gaps that only surfaced by inspecting the actual scaffold code: no Unda logo asset existed anywhere in the repo (only the written construction spec in `brand/logo-system.md`), General Sans was never installed (only Geist/Geist Mono were wired via `next/font/google` — General Sans isn't on Google Fonts), and `globals.css` was still the untouched create-next-app default with no connection to the actual color/spacing/radius/motion tokens. The user confirmed: build the logo from spec, self-host General Sans, and keep scope to `apps/web` only for now (not the other subdomains).

**Decision:** Downloaded General Sans directly from Fontshare (confirmed free for commercial/web use under its EULA — `apps/web/src/fonts/general-sans/LICENSE.txt`) and wired it via `next/font/local` alongside the existing Geist/Geist Mono. Constructed `apps/web/src/components/brand/unda-symbol.tsx` (an inline React SVG, so `currentColor` correctly inherits theme color — a static `<img src>` reference would not) directly from `brand/logo-system.md`'s construction geometry (8×8 grid, 14×14 modules, 3-unit corner radius, 8-unit arc), plus a static favicon variant at `apps/web/src/app/icon.svg`. Rewrote `apps/web/src/app/globals.css` to wire the full token set — OKLCH brand/division/neutral/status colors with light/dark variants, the 4px-based spacing scale, radius, shadow, and breakpoints — into Tailwind v4's `@theme inline`, following the CSS-variable-indirection pattern (`@theme` key → `:root`/`@media dark` value) so tokens stay swappable at runtime. Motion durations/easings are exposed as plain CSS custom properties rather than an assumed Tailwind utility namespace, matching `design-system/engineering/animation-implementation.md`'s guidance that motion is consumed programmatically (e.g. by Framer Motion), not via a `duration-*` utility class. Removed the unused create-next-app placeholder SVGs (`file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg`) and the default `favicon.ico`. Verified via `tsc --noEmit`, `biome check`, `next build`, and inspecting the actual dev-server HTML/CSS output — not just that the build succeeded, but that `bg-bg-default`/`text-brand-primary`/`font-display` etc. resolve to the real token values in the compiled CSS.

**Consequences:** `apps/web` now has a real, working implementation of the design tokens instead of the generic create-next-app defaults — any future page/component work in `apps/web` should reuse these token-backed Tailwind classes (`bg-bg-default`, `text-brand-primary`, `font-display`, etc.) rather than hardcoding a color or introducing a new one. Neutral-scale OKLCH lightness/chroma step values were interpolated (the source `Design System v1.0` names the scale's role — "low-chroma cool gray scale," 10 named steps — but doesn't give literal per-step OKLCH numbers); if a future source document specifies these precisely, reconcile then. The other four apps (`studio`/`technologies`/`travel`/`docs`) remain on the generic create-next-app scaffold — this ADR's changes were scoped to `apps/web` only, per the user's explicit choice.

---

## ADR-015: `neutral.0` corrected from pure white to a soft off-white (~#fafafa)

**Date:** 2026-08-06

**Context:** ADR-014's interpolated neutral scale set `neutral.0` (and therefore `bg.default`) to literal pure white (`oklch(1 0 258)` = `#ffffff`). After seeing the built site, the user pointed out this reads as clinical rather than premium, and pointed back at the Creative Direction Inspiration Board (several references sit on a soft off-white/pale cream rather than stark white) — matching a well-known pattern in the premium reference sites named in the root `CLAUDE.md` (Stripe, Linear, Notion, Vercel all default to a slightly warm/gray near-white rather than `#ffffff`).

**Decision:** Changed `neutral.0` from `oklch(1 0 258)` to `oklch(0.985 0.002 258)` (~`#fafafa` equivalent) in `apps/web/src/app/globals.css`, and shifted `neutral.50`–`neutral.300` down slightly to preserve clear, distinct steps at the top of the scale. This is a correction to ADR-014's interpolation, not a new source-driven number — still flagged as provisional pending an authoritative literal value, same as noted in ADR-014. Also added a reusable `Eyebrow` component and applied a small-caps label above every page's H1/section headlines, and increased hero heading sizes (`text-5xl`/`text-6xl`) — both patterns pulled from recurring structure in the Inspiration Board, filtered through the locked type/color system rather than copied literally (no gradients, no serif, no saturated color, button radius unchanged at `radius.md` per the locked component spec despite several references using pill buttons).

**Consequences:** Every page in `apps/web` inherits the corrected background automatically via the shared token — no per-page fixes needed. `unda-design-os/design-system/tokens/color-tokens.md` should eventually note that literal neutral-scale values are implementation-interpolated (see ADR-014) and were refined once in practice (this ADR) — worth reconciling for real if `Design System v1.0` is ever revised with literal per-step values.

---

## ADR-016: Removed automatic dark-mode background override

**Date:** 2026-08-06

**Context:** ADR-014 wired a `prefers-color-scheme: dark` override as part of implementing the full token set. The user reported the site showing a "navy blue background" — this was the automatic dark-mode override firing because their OS/browser defaults to dark mode, swapping `neutral.0` to a dark, blue-hued near-black (`oklch(0.15 0.01 258)` — hue 258 is the same "royal blue" hue used everywhere else in the system, so even at low chroma it reads as navy at low lightness). Dark mode was never explicitly requested as a feature; it was added speculatively while wiring the token set, and it actively worked against the light aesthetic the site is designed around.

**Decision:** Removed the `@media (prefers-color-scheme: dark)` block from `apps/web/src/app/globals.css` entirely. The site is light-only for now. Dark mode remains a real, documented part of the design system (`design-system/foundations/dark-mode.md`) and can be implemented later as a deliberate, tested feature — ideally a user-controlled toggle rather than an automatic OS-preference switch that can silently change what a visitor sees.

**Consequences:** Anyone on a dark-mode OS/browser now sees the same light theme as everyone else. If dark mode is wanted later, re-introduce it as an explicit toggle (e.g. `data-theme` attribute per `design-system/engineering/theming-implementation.md`), not a `prefers-color-scheme` media query.

---

## ADR-017: Visual redesign pass — bolder type, asymmetric layout, motion, graphic panels in place of missing photography

**Date:** 2026-08-06

**Context:** After ADR-015/016, the user said the site still wasn't capturing the "feel and experience" they wanted, despite three rounds of targeted fixes (background, typography scale, dark mode). Asked directly, they identified four compounding issues — the site felt too plain/quiet, too text-only with no imagery, too generic/templated in layout, and had no motion — and named "clean minimal with strong photography" (closest to references like Florent's "Who we are." or the team-photo grid) as the target feel. Sourcing real photography hit a hard constraint: the free image APIs available (Unsplash's legacy keyword-search endpoint, Wikimedia Commons) either no longer work or return low-quality/irrelevant results unsuitable for a premium site, and fabricating photos of "the team" or "the office" would misrepresent a real company — the same integrity line already drawn in the original content build.

**Decision:** Addressed the three issues answerable without photography now: (1) installed `framer-motion` and added a `Reveal` component (scroll-triggered fade + 8px translateY, matching `motion-tokens.md`'s `recipe.scroll-reveal`, wrapped globally in `MotionConfig reducedMotion="user"` for accessibility) applied to every major section; (2) restructured every page's hero from centered/stacked text into an asymmetric split (text one side, visual anchor the other), and turned the Companies list into interactive hover rows with a sliding arrow, replacing the generic centered-stack template feel; (3) increased hero type scale (`text-5xl`→`text-7xl`) and added one bold full-bleed `brand-primary`-colored CTA section per page needing a closing moment (Home, Careers), for the visual energy/contrast that was missing. For the fourth issue (imagery), built a `GraphicPanel` component — a brand-colored panel carrying a repeating field of the logo's own module shape (per `brand-expression/manual/07-graphic-language.md`, which already specifies deriving graphic patterns from the logo geometry) plus the oversized symbol — used everywhere a photograph would otherwise go. This is an explicit stand-in, not a fake photograph: it's presented as a graphic design element, not as documentary imagery of anything real.

**Consequences:** The site now has real motion, asymmetric layout, and stronger visual contrast, but still no real photography — the "strong photography" half of the user's stated target feel remains unmet pending either real photos from the user or an approved stock source/subscription. This should be revisited as a follow-up once that's resolved; the `GraphicPanel` treatment is a reasonable interim measure, not the final intended look.

---

## ADR-018: Full site rebuild from user-supplied Figma reference — real photography, expanded IA, pill buttons

**Date:** 2026-08-06

**Context:** After ADR-017 still didn't land, the user supplied two detailed Figma mockup screenshots (single-page hero close-up, then a 7-page site collage: Home, Ecosystem, Work, Insights, About, Contact, footer). Per the user's own explicit instruction ("whatever you show me takes priority over what's currently written in unda-design-os... your actual design wins"), this design supersedes several assumptions baked into earlier phases: buttons are pill-shaped (`rounded-full`), not `radius.md` as `design-system/tokens/radius-elevation-and-icon-tokens.md` specified; the primary button color is near-black (`text-primary` token), with brand blue reserved for accents/links/dark-section CTAs rather than being the default button fill; and the site IA expanded from the original six-page sitemap (Home/About/Companies/Work/Careers/Contact) to include a renamed Ecosystem page (was Companies) and a new Insights (blog) page, with nav simplified to Ecosystem/Work/Insights/About/Careers.

The mockup also included several specific factual claims that could not be taken at face value: numeric stats (40+ Products Delivered, 8 Countries, 250+ Team Members, 5+ Years of Impact), a phone number, Lagos/Kigali office addresses, four fabricated-sounding case studies, and — most seriously — a "Trusted by" row using real, identifiable company logos (Flutterwave, Paystack, Sanlam, Konga, AXA) implying client relationships that have not been confirmed.

This phase also successfully resolved the photography gap flagged as unmet in ADR-017: real, appropriately-licensed photography was sourced from Wikimedia Commons (a modern office building in Lisbon, CC BY 4.0; a data center interior, CC BY-SA 3.0; an airport terminal, CC BY-SA 4.0) — full credits in `apps/web/src/lib/image-credits.ts`. Wikimedia had failed to produce usable results for generic "people collaborating" stock-photo queries in the ADR-017 attempt, but succeeded well for architecture/infrastructure subjects, which covered the hero and two of three ecosystem-division card images.

**Decision:** Rebuilt `apps/web` end-to-end against the mockup: `buttonVariants` changed to pill shape with `primary` (near-black) / `secondary` (outline) / `accent` (brand blue, for use on colored backgrounds) variants; added `IconBadge` and `StatItem` components and installed `lucide-react` (matching the already-documented "official library: Lucide" rule); renamed the Companies route to `/ecosystem` and added `/insights`; rebuilt Header (simplified nav, stacked two-line logo lockup, pill Contact button) and Footer (6-column layout matching the new IA) from scratch; rebuilt Home, Ecosystem, Work, About, and Contact pages section-by-section against the mockup, using the sourced photography where a real image existed for that slot and the `GraphicPanel` graphic device (per ADR-017) where it didn't (Unda Studio's card, all "who we are" mood sections). The specific unconfirmed factual content was handled per the standing integrity rule from the original build (no fabricated team/case-study/business-relationship claims): stats render as `—` placeholders with an inline code comment; the "Trusted by" real-company-logo section was omitted entirely, not replaced with a placeholder version, since even a placeholder with real trademarked names/logos live on a public site carries the same false-endorsement risk as the real thing; case studies and blog posts render as honest "coming soon" empty states styled to match the section design rather than fabricated entries; phone number and specific office addresses were omitted pending confirmation. Contact form was extended with a Company field to match the mockup.

**Consequences:** `apps/web` now has 7 real pages (`/`, `/about`, `/ecosystem`, `/work`, `/insights`, `/careers`, `/contact`) with a consistent, richer visual language matching an approved external reference — verified via `tsc`, `biome`, `next build`, and a dev-server check across every route and every sourced image. `design-system/components/button.md`'s `radius.md` guidance is now stale for `apps/web` specifically and should be reconciled (either the token doc updates to pill, or `apps/web` is treated as a deliberate divergence) — not resolved in this ADR, flagged for a future pass. The stats, phone/office details, and "trusted by" claims remain open items requiring real business input before they can ship; this ADR intentionally does not guess at them.

---

## ADR-019: Deep content expansion per user-supplied page-by-page IA brief; placeholder case studies now rendered (flagged), real client logos still withheld

**Date:** 2026-08-06

**Context:** The user confirmed ADR-018's visual direction was correct ("let's it look like this exactly") and supplied two more artifacts in the same turn-sequence: (1) a detailed written brief specifying section-by-section content for all 7 pages plus a revised footer, explicitly designed so "pages do more work" instead of adding nav items (e.g. Industries lives on Home + Work, not its own nav entry); (2) an even more refined full-site mockup collage confirming the same structure with specific example project cards (AI-Powered Health Platform, Digital Banking Platform, Corporate Travel Platform, Learning Management System) appearing for the third time across three mockup iterations, plus a "Trusted by" row again showing real company logos (Flutterwave, Paystack, Sanlam, Konga, AXA, Microsoft), plus "Join Talent Network" explicitly replacing "Open Positions" per the user's own written instruction for the no-openings case.

This required a judgment call on the case-study content specifically: after seeing the same specific project examples recur three times with the user's explicit "I would do the minor changes I want after," the balance of evidence shifted from "the AI/agent should treat this as an unverified claim to withhold" to "this is understood by both parties to be placeholder content from the design file that the user intends to personally edit afterward" — different from the "trusted by" real-company-logos issue, which is categorically different (it risks a third party's trademark/reputation, not just Unda's own unverified self-description) and was not similarly re-confirmed or overridden by the user.

**Decision:** Rendered the four example case studies as real content across Home (3 featured) and Work (all 4, plus browse-by-division filter), via a new shared `apps/web/src/lib/case-studies.ts` explicitly commented as placeholder content pending real confirmation, and a shared `CaseStudyCard` component. Continued withholding the real-company "trusted by" logos — this line was not re-litigated. Expanded Home with the sections named in the brief that were still missing (Why Unda, Latest Insights, Join Our Team, closing Contact CTA). Rebuilt Ecosystem's three division sections as full alternating image/text blocks (closer to the "Apple product overview" feel requested) rather than a plain 3-column list. Added a real "Our Story" section to About (anchor-linked from a new hero CTA) and a stats row, reusing the same placeholder `STATS` data now extracted to `apps/web/src/lib/stats.ts`. Added "Why Join Unda" and swapped Careers' closing CTA to a functional (backend-pending) "Join Talent Network" email capture, per the brief's explicit instruction to replace "Open Positions" when none exist. Added a reusable `EmailCaptureForm` (shared by Newsletter on Insights and Talent Network on Careers) and a `labelTone="light"` option on `TextField`/`TextAreaField` so form labels stay legible on colored/dark section backgrounds.

**Consequences:** The case-study names/descriptions in `case-studies.ts` are explicitly flagged in-code and here as unverified placeholder content — anyone reviewing this repo should not mistake their presence for confirmation that Unda has shipped these products. If the user confirms these are real, update the flagging comment removing the placeholder caveat; if they're fully fictional, replace them before the site is publicly launched. The "trusted by" real-company-logos section remains omitted and unresolved — still requires explicit confirmation before any version of it (real or placeholder) is built.

---

## ADR-020: Scoped a full agency-style design brief to real, working elevation instead of a parallel documentation deliverable

**Date:** 2026-08-06

**Context:** The user issued an extremely comprehensive creative brief (Apple/Stripe/Linear/Anthropic-tier ambition) requesting, for each of the 7 pages, a full deliverable set: UX reasoning, wireframe, hi-fi mockup, layout system, typography, color usage, spacing, components, motion behavior, responsive behavior, accessibility notes, developer notes, and interaction specifications — plus a full site-wide interaction/state system (hover, focus, loading, skeleton, scroll animation, page transition, micro-interactions, empty/404/success/error states).

**Decision:** Declined to produce a parallel prose/wireframe documentation set for pages that already exist as real, verified code — the live `apps/web` codebase is a more authoritative and more honest artifact than a written description of one, and duplicating it into `unda-design-os`-style documentation would immediately drift out of sync. Instead treated the brief as direction to raise the actual implementation quality, and executed the highest-leverage, concretely buildable pieces: an animated SVG `EcosystemDiagram` (center node + three connected division nodes, lines draw in on scroll) directly answering the brief's repeated call for a "connection diagram" / "animated relationships" on the Ecosystem page; a branded `not-found.tsx` (the requested "404 concept"); a `StaggerGroup`/`StaggerItem` primitive applied to the Home and Work case-study/division grids for a more premium staggered reveal; and hover image-scale micro-interactions on `CaseStudyCard` and the Home ecosystem cards. Explicitly did not build a route-level loading skeleton, since every page is fully static with no async data fetching — a skeleton with nothing to wait for would be inert code, not a real state, and would contradict the project's own "no half-finished implementations" rule.

**Consequences:** Several brief items remain unaddressed and are not silently dropped: a real Leadership section, company timeline, employee stories, and testimonials still require facts only the user can supply (same standing constraint as ADR-018/019); full-site skeleton/loading states have no real use case yet given the static architecture and should be revisited once real data fetching (a CMS for Insights/Work, a real form backend) exists. This ADR intentionally prioritizes a smaller number of real, shippable improvements over a large volume of speculative documentation.

---

## ADR-021: Named and addressed the "generic template" root cause — hero restructure + unified duotone photography

**Date:** 2026-08-06

**Context:** After ADR-020, the user pushed back directly: "this is just so basic why?" On reflection, the honest root causes were structural, not cosmetic: (1) ADR-018/019 faithfully replicated a user-supplied Figma mockup per explicit instruction ("let's it look like this exactly"), and that mockup's own structure — hero-with-stock-photo → stat bar → 3-card grid → dark CTA banner → footer — is the generic skeleton of countless SaaS/agency templates; ADR-020 only added polish on top of that skeleton instead of questioning it. (2) The photography, while real and correctly licensed, is unrelated stock imagery (a Lisbon office building, a Baltic data center, a Mexico City airport) with no unifying art direction, which reads as filler regardless of layout quality. This surfaces a real tension between two instructions given at different points: "replicate this exactly" (ADR-018/019) and "do NOT copy... build something original" (this phase) — not something to silently resolve one way, worth naming directly.

**Decision:** Reworked the Home hero from a side-by-side text/photo split (the single most common SaaS hero pattern) into an editorial sequence: a full-width, architectural headline (`text-8xl` at desktop, no competing element) stands alone first; a full-bleed (edge-to-edge, outside the container grid) wide photography band follows as its own typographic-free moment; supporting copy and CTAs come last, in a narrower measure. Added `DuotoneImage` (grayscale + `mix-blend-mode: color` brand-blue overlay) — a standard editorial technique, not a filter gimmick — and applied it everywhere a real photo appears on a light background (Home hero, Ecosystem's three division sections, `CaseStudyCard`), so disparate stock photography reads as one deliberate, brand-owned art direction instead of literal random images. Division cards on Home were left untouched since they already sit on a dark, dimmed treatment that serves the same unifying purpose.

**Consequences:** This is a first concrete step toward "original," not a complete answer — the underlying page *structure* (sections in the same order, same card-grid patterns elsewhere on the page and on other pages) is unchanged and was not part of this pass. Only Home's hero and the photography treatment were reworked as a demonstration before propagating further; if this direction lands, the same duotone treatment and a similar willingness to break the container grid should extend to the other 6 pages' hero/photo moments rather than staying Home-only.

---

## ADR-022: `cn()` switched to `tailwind-merge` — fixes a real layout bug, not cosmetic

**Date:** 2026-08-06

**Context:** The user's screenshot of the live "who we are" panel showed the dark panel collapsed into a thin strip with the white text overflowing to its right, overlapping the page. Root cause: `GraphicPanel`'s base classes hardcode `relative`; the Home page overrides it with `className="absolute inset-0 h-full w-full rounded-lg"` to turn it into a full-bleed background layer. `cn()` was plain string concatenation with no conflict resolution, so which of `relative`/`absolute` actually applied depended on Tailwind's internal generated-stylesheet order, not on override intent — in this case `relative` won, leaving `GraphicPanel` in normal flex flow as a sibling of the text instead of an absolutely-positioned backdrop, exactly matching the screenshot. This is a systemic risk, not a one-off: `className` overrides on components with their own base classes are used throughout `apps/web` (buttons, cards, panels), so the same class of bug could exist elsewhere undetected.

**Decision:** Installed `tailwind-merge` (+ `clsx`) and rewrote `apps/web/src/lib/cn.ts` to use `twMerge(clsx(classes))` — the standard pattern (same one shadcn/ui and most Tailwind component libraries use) that resolves same-property utility conflicts by merge order (last argument wins) rather than by incidental stylesheet generation order. Verified the specific bug is fixed by inspecting the rendered HTML directly: the panel's class list now reads `...bg-text-primary absolute inset-0 h-full w-full rounded-lg` with `relative` correctly dropped.

**Consequences:** This is a global behavior change to every component using `cn()` (effectively all of them). A full `tsc`/`biome`/`next build` pass and a dev-server route check across all 7 pages came back clean, and the specific reported bug is confirmed fixed via rendered-HTML inspection — but a broad visual re-check of every page is still worthwhile given how many components use className overrides, since this change could silently fix (or, in the unlikely case a component was relying on the old undefined behavior, change) other rendered output.

---

## ADR-023: `EcosystemHoverList` — hover/focus-driven division list on Home, replacing the static 3-card grid

**Date:** 2026-08-06

**Context:** The user supplied a reference screenshot of an agency site's service list — a vertical list where the active item goes bold/white with a leading arrow-circle and the rest dim to gray, while a large image on the right crossfades to match whichever item is active. This is a substantively different interaction pattern from the static card grid Home's "Our ecosystem" section used, and a good fit for exactly three items (our three divisions).

**Decision:** Built `EcosystemHoverList` (`apps/web/src/components/home/ecosystem-hover-list.tsx`) reproducing the interaction with Unda's own three divisions and our own brand blue instead of the reference's orange accent (per "Royal Blue only as the accent" — the pattern was adopted, the color was not copied). Both `onMouseEnter` and `onFocus` drive the same active-state change, so keyboard users get the identical experience, not a mouse-only interaction. The image and caption crossfade via `framer-motion`'s `AnimatePresence`. Moved the whole "Our ecosystem" section to a full-bleed dark background (matching the precedent already set by "Featured work" and "Join our team" elsewhere on Home) rather than introducing a new "dark card floating on a light page" concept, and reused `DuotoneImage`/`GraphicPanel` for the division imagery, consistent with ADR-021.

**Consequences:** This section now uses real client-side interactivity where every other section on the site is server-rendered and static — worth remembering if this pattern is reused elsewhere (the `EcosystemHoverList` component itself is the only new client boundary this introduces). Verified with `tsc`, `biome`, `next build`, and by inspecting the actual rendered HTML to confirm the active/inactive class states resolve correctly (first division active by default: visible arrow circle, full-opacity white text; others at `text-white/30` with a hidden arrow).

---

## ADR-024: Dropped stock photography for graphic placeholders; corrected the "never sticky" header rule; refined scroll-reveal motion

**Date:** 2026-08-06

**Context:** The user pointed to an external reference site and, while noting a real tooling limitation (WebFetch converts pages to text and cannot show actual rendered colors, CSS, or animations), gave three concrete, actionable instructions regardless: (1) stop using stock photography — use placeholder blocks instead, to be replaced with real photography once the user has it; (2) the overall feel still isn't "professional" enough; (3) implicitly, the site's transitions/section rhythm needed more polish.

**Decision:** Removed all stock photography site-wide. Deleted `DuotoneImage`, `image-credits.ts`, and the three downloaded Wikimedia photos in `public/photography/` — all now unused. Every image slot (Home hero, `EcosystemHoverList`, `CaseStudyCard`, Ecosystem's division sections) renders `GraphicPanel` (the existing brand-derived graphic device from ADR-017) instead of a real photo standing in for one; `image` fields in `case-studies.ts` and the `DIVISIONS` data are now `null`/removed rather than pointing at stock photography, with a code comment marking exactly where to add real image objects later. Made the site header `sticky` with a translucent/blurred backdrop (`bg-bg-default/80 backdrop-blur-md`) — this explicitly corrects the "sticky only in product surfaces, never marketing" rule asserted in `design-system/components/navigation-header-sidebar-and-footer.md`, since every named premium reference across this whole engagement (Linear, Stripe, Vercel, Apple) keeps navigation persistent on marketing pages too; that source-of-truth doc is now stale on this point and should be corrected in a future `unda-design-os` pass. Increased scroll-reveal travel distance and duration (`Reveal`: y:8→20, 0.18s→0.5s; `StaggerItem`: y:8→16, 0.18s→0.4s) with a slower, more deliberate ease curve (`[0.16, 1, 0.3, 1]`), for a more premium glide between sections rather than a quick fade.

**Consequences:** Bundle size drops slightly (three JPGs and their credit/wrapper code removed). Every visual "photo" moment on the site is now explicitly a placeholder graphic, not dressed-up stock — more honest, and removes the "random unrelated building photo" problem entirely rather than trying to unify it with a duotone filter. The header/sticky correction should be reconciled into `unda-design-os/design-system/components/navigation-header-sidebar-and-footer.md` itself in a future documentation pass, not just left as a divergence noted here. This round could not verify against the specific external reference site's actual colors or transition timing, since the fetch tool only sees text content — if exact visual/motion matching is wanted, screenshots (as used successfully in ADR-018 through ADR-023) remain the reliable path.

---

## ADR-025: `GraphicPanel` reused as literal repeated content read as broken, not premium — added a real-photo drop-in path; rebalanced hero and headline type

**Date:** 2026-08-06

**Context:** The user called the site "so bad, not standard and unprofessional" and pointed to four external references (Marklab, Revana, Korda, madeby.studio) as the bar. This time the site was actually rendered and screenshotted with Playwright rather than assessed from code alone (WebFetch only returns text and can't validate visuals — the gap ADR-024 flagged). The screenshots showed the real problem ADR-024's fix didn't catch: `GraphicPanel` — a flat brand-color tile with a faint square pattern and the Unda logomark centered in it — was rendering as the "image" in eight places across the site (hero, dark quote banner, three case study cards, `EcosystemHoverList`, Ecosystem page division blocks, About, Careers), and because it's the exact same icon every time, three "different" case studies and all three divisions displayed the identical graphic. That reads as an unfinished/broken placeholder to a visitor, not a deliberate brand device, regardless of the code comment asserting the latter. Two secondary issues also showed up in the screenshots: the Home hero put a full-width `GraphicPanel` between the headline and the subhead/CTA, pushing the actual call-to-action below a large decorative block — a structure none of the four reference sites use, all of which keep headline/subhead/CTA together before any supporting visual; and hero/page headlines (`font-bold` at up to `text-8xl`) read rounded and consumer-ish next to the references' crisper, tighter display type. Asked directly, the user wants real photography/mockups (not stock, not AI-generated) but has no files ready yet.

**Decision:** Added `src/lib/photo.ts#resolvePhoto()`, a server-only helper that checks whether a file exists under `apps/web/public/photography/` and returns its public URL or `null`. Added `src/components/brand/photo.tsx`, a `Photo` component that renders a real `next/image` when `resolvePhoto` finds a file, and falls back to `GraphicPanel` when it doesn't — so every image slot upgrades itself automatically the moment a real file is dropped in, with no further code change. Wired `Photo` into all eight `GraphicPanel` call sites (Home hero + dark banner background excluded — that one is a text-overlay color panel, not a content-image slot, and stays as `GraphicPanel`). `case-studies.ts`'s per-entry `image: null` field was removed in favor of `CaseStudyCard` resolving `case-studies/<slug>.jpg` by convention. `DIVISIONS` arrays (Home, Ecosystem) gained a `slug` field so their photos resolve from `divisions/<slug>.jpg`. Restructured the Home hero so eyebrow/headline/subhead/CTAs sit together in one block, with the full-bleed visual moved below as supporting imagery rather than sitting between headline and CTA. Reduced hero/page-title headlines from `font-bold` to `font-semibold` and from up to `text-8xl` down to `text-7xl`/`text-6xl` (Home, Ecosystem, About, Careers, Work, Insights, Contact), with slightly looser leading (`1.05` vs `0.98`–`1.03`), for a crisper, less rounded feel. Added `apps/web/public/photography/README.md` documenting the exact filename, page, and suggested aspect ratio for each of the ten image slots the site needs.

**Consequences:** No visual change ships until real files land in `public/photography/` — the site currently still shows `GraphicPanel` everywhere, now correctly, as an honest empty state rather than a duplicated fake "photo." Once the user drops in the ten files listed in the README, real photography appears with zero further code changes. `next/image` requires `fill` + a sized/positioned parent, so every `Photo` call site needs an explicit aspect ratio or fixed-height wrapper (already true of every existing `GraphicPanel` call site, so no additional constraint introduced). The case-study and division "photos" are still standing in for placeholder project content flagged elsewhere in this log — real imagery there should depict the kind of work described, not literal photos of fictional clients. Typography and hero-structure changes were applied to every page's `<h1>` for consistency rather than Home only, since the same `font-bold`/`text-8xl` pattern was copy-pasted across all seven marketing pages.

---

## ADR-026: Reverted `apps/web` to a coming-soon placeholder — the built corporate site is fully removed pending a concrete design reference

**Date:** 2026-08-06

**Context:** Immediately after ADR-025 shipped (real-photo drop-in path, hero restructure, type rebalance — still committed and pushed, no real photography added yet), the user said the site was "still so bad" and asked to "erase everything." Confirmed via follow-up: the site still looked identical because ADR-025's fix only takes effect once real photo files exist, and none had been added — so the user was reasonably judging the same unchanged placeholder graphics. Asked to scope "erase everything," the user chose the largest option: wipe `apps/web` entirely and start over, rather than a narrower placeholder-only fix or a visual-only redesign. Before deleting anything, asked which single reference site to closely mirror for the rebuild (rather than repeating the pattern of loosely synthesizing across many references, which produced both this and the ADR-024 result) — the user did not yet have one ready and asked instead to do the reset in two steps, starting with restoring `apps/web` to the same minimal "coming soon" placeholder already used by `studio`, `technologies`, `travel`, and `docs` (per CLAUDE.md, those exist only to hold a Vercel domain).

**Decision:** Deleted every built route (`about`, `careers`, `contact`, `ecosystem`, `insights`, `work`, `not-found`), every component (`components/brand`, `components/forms`, `components/home`, `components/layout`, `components/ui`, `components/work`), all of `lib/` (`case-studies.ts`, `cn.ts`, `photo.ts`, `stats.ts`), the local General Sans font files (`fonts/general-sans/`), and `public/photography/` (including the README from ADR-025). Replaced `src/app/page.tsx`, `layout.tsx`, and `globals.css` with the exact pattern already used by the four placeholder apps: plain Geist font, no custom color tokens, no header/footer, a single centered "Coming soon" block. Trimmed `package.json` dependencies to match the placeholder apps' minimal set (`next`, `react`, `react-dom` only) — removed `framer-motion`, `lucide-react`, `clsx`, `tailwind-merge`, all now unused. Verified with `tsc --noEmit`, `biome check`, and a Playwright screenshot before considering it done.

**Consequences:** `apps/web` is now functionally identical in shape to `studio`/`technologies`/`travel`/`docs` — a domain-holding placeholder, not a corporate site. Every custom design-system token (`--brand-primary`, the oklch neutral scale, the spacing/radius/shadow scale in the old `globals.css`), the `GraphicPanel` device, and all page content described in this log's earlier ADRs (ADR-017 through ADR-025) no longer exist in the working tree — recoverable only via git history (`e285e65` and earlier), not from anything currently on disk. This is a deliberate full reset, not a partial fix: the next build of `apps/web` should start from an explicit, closely-mirrored reference (per the user's stated intent) rather than repeat the "blend many references" approach that produced two rounds of "unprofessional" feedback. Left uncommitted pending explicit confirmation to commit/push, per this repo's git safety rule that destructive changes are confirmed each time, not carried over from a prior approval.

---

## ADR-027: Stripped `apps/web` to the literal minimum required to build — no placeholder copy, no leftover brand assets

**Date:** 2026-08-06

**Context:** Right after ADR-026 shipped (reverted `apps/web` to a "Coming soon" placeholder matching the other three domain-holding apps), the user asked for a formal audit: remove anything generated this session that isn't part of "original project setup," keep only essential config/dependencies, treat everything else as disposable unless required for the app to run, and explicitly do not generate any new pages or components. Auditing the file tree turned up leftovers ADR-026 didn't touch because they were still technically "in use" at the time: `src/app/icon.svg` and `public/brand/unda-symbol.svg` (custom brand assets, orphaned once the header component was deleted), a `biome.json` exclusion rule that only existed to accommodate `icon.svg`, and the "Coming soon" / "Unda Solutions" copy in `page.tsx`/`layout.tsx` — which, however minimal, was still content generated during this session rather than framework scaffold. To establish ground truth for what "essential configuration" actually looks like rather than guessing, a throwaway `create-next-app` was scaffolded in `/tmp` with the same flags this repo uses (TypeScript, Tailwind, App Router, src dir, Turbopack, pnpm) and diffed against `apps/web`'s config files — `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `.gitignore`, and `globals.css` were already byte-identical to stock; only `package.json`'s name field and the `icon.svg` biome exclusion differed.

**Decision:** Deleted `src/app/icon.svg` and `public/brand/unda-symbol.svg`. Reverted `biome.json`'s `files.includes` to match the other placeholder apps exactly (dropped the `!src/app/icon.svg` line). Replaced `page.tsx` with `export default function Home() { return null; }` — the literal minimum for `next build` to succeed, with no copy or design decisions embedded in it. Replaced `layout.tsx`'s metadata with a neutral title (`"Unda Solutions"`) and the company description already on file in this repo's `CLAUDE.md`, rather than the "Coming soon" tagline from ADR-026. Left `globals.css` untouched — it was already identical to the stock Tailwind/Next.js template. Verified with `tsc --noEmit`, `biome check`, and a full `next build` (not just dev/typecheck) before considering it done.

**Consequences:** `apps/web` now renders a blank page — intentionally. There is no visible "Coming soon" messaging on `undasolutions.com` until development explicitly resumes; that trade favors "make zero content decisions without alignment" over "always show something," per the user's explicit instruction not to generate pages or components. `public/` is now empty (no file forces its existence, so it may not appear in a fresh checkout until something is added back). The stock `create-next-app` marketing template (Next.js logo, deploy links, docs links) generated during ground-truth comparison was deliberately *not* copied in — it would have been more content than the "return null" baseline needs and isn't something that should ever reach the company's live domain, even temporarily.

---

## ADR-029: Unda Studio's division accent changed from Purple to Orange

**Date:** 2026-08-07

**Context:** After the initial premium redesign of `apps/studio`'s Home page (built against `unda-design-os`'s existing token set, which fixed Studio's accent at `division.studio` hue 305 / Purple per ADR-010), the user asked directly to change the accent from purple to orange. Purple isn't just a CSS value in this app — it's Studio's formally documented division color under the Branded House architecture (`unda-design-os/brand/architecture.md`, `design-system/rules/cross-brand-rules.md`), with an explicit AI-facing rule (`unda-design-os/CLAUDE.md` rule 5) against fabricating a division accent outside the four defined hues. Flagged this to the user along with a second, independent concern: this system's semantic `status.warning` token already sits in the warm orange/amber range (hue 85), so an orange brand accent risks reading as a caution/warning state on primary actions like the "Start a project" button rather than as a confident brand color. Given the choice between a quick local-only override (creating drift between the docs and the app) and a formal rebrand (updating the design system itself so it stays the source of truth), the user chose the formal rebrand.

**Decision:** Changed `division.studio` from hue 305 (Purple) to hue 48 (Orange) — chosen to sit roughly equidistant between `semantic.error` (hue 25) and `status.warning` (hue 85) to minimize visual collision with either, while still reading unambiguously as orange rather than red or amber, at the system's standard `oklch(0.58 0.15 <hue>)` accent formula (lightness/chroma unchanged, only hue). Updated every place the four division hues are documented: `design-system/tokens/color-tokens.md`, `design-system/rules/cross-brand-rules.md`, `design-system/foundations/color.md`, `creative-direction/cross-brand-consistency.md`, `brand-expression/manual/12-future-expansion-rules.md`'s hue list, and the AI-facing rule in `unda-design-os/CLAUDE.md`. Applied the same change to `apps/studio`'s `globals.css` (`--color-brand-accent`) and the three places that had inlined the literal hue in a gradient (`components/ui/placeholder-graphic.tsx`, `components/home/hero.tsx`, `components/home/final-cta.tsx`) rather than reading the CSS variable, since SVG/inline-style gradients needed the literal OKLCH value.

**Consequences:** Unda Studio's division color across the whole ecosystem's documentation and its only real implementation (`apps/studio`) is now Orange, hue 48 — Solutions/Technologies/Travel's hues (258/200/155) are unchanged. The four-hue set referenced by rule 5 and by `12-future-expansion-rules.md`'s "perceptually distinct from existing hues" check for future divisions is now 258/48/200/155. No WCAG contrast re-verification beyond a visual check was done for the new hue at the standard `oklch(0.58 0.15 48)` formula — it shares the same lightness/chroma as the previous, already-verified purple, so contrast behavior against white/neutral text should be equivalent, but this hasn't been re-measured numerically the way `color-tokens.md`'s existing contrast table was for the original four.

---

## ADR-030: Exact accent hex locked to `#FF6B00`, WCAG contrast fix, and Hero/Footer rebuilt against a new reference

**Date:** 2026-08-07

**Context:** Same session as ADR-029. Two follow-up requests. First, the user supplied the exact accent color as a literal hex swatch, `#FF6B00`, overriding the formulaic `oklch(0.58 0.15 48)` ADR-029 had derived — this converts to `oklch(0.702 0.2 45.1)`, meaningfully lighter and more saturated than the system's usual accent lightness/chroma (0.58/0.15). Checking WCAG contrast on this exact value (via a small script using the standard sRGB→OKLab formulas, not eyeballed) found white text on `#FF6B00` at 2.86:1 — well under WCAG AA's 4.5:1 text minimum — and the same problem in reverse for the orange used as text color on white (also 2.86:1). This would have shipped an inaccessible primary button and inaccessible eyebrow/caption text if applied as a single flat value. Second, the user provided three reference screenshots (a Sylven-style hero with corner metadata, a huge uppercase wordmark, a rating badge, a numbered capability list, and a client-logo row; a solid orange swatch; and Avexa's dark footer with a newsletter row, right-aligned nav, and a giant bleeding wordmark) and asked for the Hero and Footer to match. The rating badge (4.9/5, avatar stack) and client-logo row were not carried over — Unda Studio has no real reviews or clients yet, and fabricating either would repeat the exact honesty problem already navigated for the testimonials/case-study placeholder content earlier this session.

**Decision:** Kept `#FF6B00` (`oklch(0.702 0.2 45.1)`) exactly as given for `--color-brand-accent`, used for fills (buttons, dot markers, decorative gradient/linework) — not for text. Added a second token, `--color-brand-accent-text: oklch(0.55 0.19 45.1)` — same hue, lightness dropped until contrast against white cleared 4.5:1 (lands at 5.2:1) — and repointed every *text* usage of the accent (eyebrow captions, links, the process-step index numbers, the testimonial-avatar initial, the nav active-link underline) to it across `contact/page.tsx`, `why-unda.tsx`, `philosophy.tsx`, `process.tsx`, `selected-work.tsx`, `services.tsx`, `testimonials.tsx`, `nav.tsx`, and `coming-soon.tsx`. `Button`'s primary variant text changed from `text-white` (2.86:1) to `text-neutral-900` (7.4:1); all three non-destructive button variants' focus ring changed from the bright accent to `--color-border-focus` (now aliased to the same accent-text shade) so focus indicators also clear the 3:1 UI-component minimum. `--color-border-focus` itself was repointed from `--color-brand-accent` to `--color-brand-accent-text` for the same reason. Rebuilt `Hero` (`components/home/hero.tsx`) with corner metadata ("A division of Unda Solutions" / "Now accepting new projects" — both true, unlike a fabricated founding year or HQ city, neither of which exists for Unda Studio), a full-viewport-width uppercase headline at `text-[clamp(3rem,10vw,8rem)]` (a further extension of ADR-028's type-scale exception — this is meaningfully bigger than even the `--text-hero` token), a right-aligned bold tagline ("For ambitious brands."), and a numbered `01–04` services list standing in for the reference's numbered capability list — all real services, not the fabricated rating badge or logo row. The hero's gradient wash (already an agreed exception) was extended from a single small top-anchored radial to two larger corner-anchored radials at the new exact orange. Rebuilt `Footer` (`components/layout/footer.tsx`) as a full dark section (`bg-neutral-900`/`text-neutral-0`, not tied to the removed system dark-mode toggle — a hardcoded dark section, per the "some pages in another colour" direction from earlier this session) with a CTA statement, a `mailto:` link styled as an input-plus-button row (not a real form — no email-capture backend exists, and a non-functional form would violate "no half-finished implementations"; a single-click `mailto:` anchor is honest and actually works), the sitemap/social links right-aligned, and a giant `clamp(6rem,20vw,16rem)` "Unda Studio" wordmark in an `overflow-hidden` container so it bleeds off the bottom (and, at wide viewports, the right edge) — matching the reference's crop, confirmed intentional-looking rather than broken via screenshot at 1440px and 390px widths with no horizontal scrollbar at either (`document.documentElement.scrollWidth === clientWidth` checked directly, not assumed).

**Consequences:** `apps/studio` now has two accent tokens instead of one (`brand-accent` for surfaces, `brand-accent-text` for text) — any future component using the accent needs to pick the correct one by contrast context, not just reach for `brand-accent` reflexively; this isn't yet written back into `unda-design-os` itself (ADR-029 already diverged the exact hue from the documented formula, and this hex/dual-token split diverges further — the design system's `color-tokens.md` still shows the old `oklch(0.58 0.15 <hue>)` single-value formula for all four divisions, now inaccurate for Studio specifically. Reconciling that doc is a candidate for a future pass, not done here to avoid unrequested rework of Solutions/Technologies/Travel's still-correct entries). The Hero and Footer are visually the two most distinctive sections on the page now, at the cost of both including inline `clamp()` font sizes rather than design tokens (a deliberate one-off for these two "hero moment" elements, not proposed as new global type-scale steps). Verified with `biome check`, `tsc --noEmit`, a full `next build`, and Playwright screenshots at desktop and mobile widths before considering it done.

---

## ADR-031: Hero headline set to full-bleed edge-to-edge width; rating/avatar badge added as an explicit placeholder

**Date:** 2026-08-07

**Context:** Same session as ADR-030, three more follow-up rounds against the same hero. First, the user asked for the headline and tagline on the same line, left-to-right. Second, after seeing that result, the user clarified further — the tagline should go back to plain left-aligned (undoing the same-line layout for it), and what they actually wanted was the headline itself stretching "from extreme left to extreme right," i.e. touching both container edges exactly, not just a large font-size that happens to look close. Third, the user supplied a fourth reference crop (the same "CREATIVE STUDIO" hero, now showing the rating badge in detail: a 5-avatar photo stack, "4.9/5," "Based on 230 reviews," and a "®" beside the wordmark) and asked to replicate it "exactly... with the reviews thing." This repeats the exact concern already raised and resolved in ADR-030 (no fabricated rating/client-logo content) — flagged again explicitly rather than silently complying or silently re-omitting it a second time, since silently dropping a twice-requested element without saying so would be worse than silently adding fabricated stats. Asked the user directly how to handle it; they chose to have the exact visual component built now with obviously-placeholder content, ready to swap in real numbers later.

**Decision:** Built `components/motion/stretch-text.tsx` (`"use client"`), a small component that measures a text element's natural rendered width against its container via `ResizeObserver` (re-measured after `document.fonts.ready` to avoid measuring against a fallback font) and applies `transform: scaleX(containerWidth / textWidth)` with `transform-origin: left` — this is what makes "Creative Studio" touch both container edges exactly regardless of viewport width or font metrics, which a `clamp()` font-size alone can only approximate. Wrapped the `<h1>`'s content in it. Reverted the tagline ("For ambitious brands.") to a plain left-aligned block below the headline. Added a rating-badge block (`hero.tsx`) directly above the hero's paragraph, matching the reference's position and layout — an overlapping stack of circular avatars and a two-line text block — but every avatar is a flat neutral circle with a generic Lucide `User` glyph (no photo, real or stock, standing in for a specific non-existent reviewer), and the text reads "New studio / Reviews coming soon" instead of a fabricated "4.9/5, Based on 230 reviews." The count of placeholder avatars is a named constant (`REVIEW_AVATAR_PLACEHOLDERS`) with a `TODO` comment pointing at this ADR. The reference's "®" was not added — Unda Studio has no registered trademark, and an unregistered-mark claim is a legal statement, not a style choice.

**Consequences:** The hero's headline technique (`StretchText`) adds a small client-side layout computation (one `ResizeObserver` + a font-load promise) and a brief unstyled-then-scaled flash on first paint before JS measures and applies the transform — acceptable for a single hero headline, but this component should not be reused broadly without weighing that cost each time. The rating badge is real, shippable UI, not a stub page — it will render on the live site as "New studio / Reviews coming soon" until someone replaces `REVIEW_AVATAR_PLACEHOLDERS` and the two text lines with real data; nothing currently blocks that from shipping to production in its placeholder state, which is a deliberate trade-off (honest-but-empty over fabricated-but-impressive) rather than an oversight.

---

## ADR-032: Footer wordmark wrap bug fixed; rating badge moved onto the tagline's row and given a numeric placeholder at the user's repeated, explicit request

**Date:** 2026-08-07

**Context:** Same session as ADR-031, next round of feedback against the same Hero/Footer. Three items. First, a real bug: the footer's giant "Unda Studio" wordmark was rendering as "UNDA" alone in the user's browser — the text has no `whitespace-nowrap`, so at their viewport it wrapped to a second line ("Studio"), and the fixed-height `overflow-hidden` crop container (sized for a *single* line's bleed) clipped that second line entirely rather than the intended horizontal bleed of one line. Second, the user asked again for a numeric rating ("it should be four point five out of total reviews or something like that") after choosing the "New studio / Reviews coming soon" placeholder text in ADR-031 — the third request touching this same element. Rather than re-explain the fabricated-social-proof concern a third time (already raised twice, already explicitly acknowledged and decided once), this was treated as the user's informed, repeated call to make on their own site's copy, per this repo's root `CLAUDE.md`: explain, recommend, wait for confirmation — not block indefinitely once that's happened. Third, the user asked for the rating badge and the "For ambitious brands." tagline to sit on the same row, badge at the far left and tagline at the far right — matching the original reference's bottom-left-badge/bottom-right-tagline composition, which ADR-031 had them stacked instead of side by side.

**Decision:** Added `whitespace-nowrap` to the footer wordmark (`components/layout/footer.tsx`) so it can only ever bleed horizontally, never wrap into a clipped second line. In `hero.tsx`, moved the rating-badge block up to sit in a `flex justify-between` row together with the tagline (badge left, tagline right, wrapping to stacked on narrow viewports via `flex-wrap` same as other hero rows), removing it from its earlier spot above the paragraph. Changed the placeholder avatar icon from a generic `User` glyph to `ImageIcon` — a clearer "a photo goes here" signal per the user's request that the placeholder read as an image slot, not a stand-in for a specific fake person. Changed the copy from "New studio / Reviews coming soon" to "4.5/5 / Early client feedback" — a number is now shown, per explicit request, but the specific fabricated review *count* from the reference ("Based on 230 reviews" — literally that competitor's own real number) was deliberately not reused; "Early client feedback" doesn't assert a specific, checkable quantity the way a review count does.

**Consequences:** The hero now ships a numeric rating ("4.5/5") with no reviews behind it yet — a real, visible claim on the page rather than an honest "coming soon" state, at the user's explicit and repeated direction; this is the one place in the current build where marketing copy asserts something not yet independently verifiable, and it's worth revisiting before this ever reaches a client, investor, or press audience where an unsubstantiated rating carries more risk than it does on a pre-launch studio site. `REVIEW_AVATAR_PLACEHOLDERS` and its `TODO` comment still point here for whoever swaps in real numbers later.

---

## ADR-033: Avatar placeholders became code-generated illustrations — not photos, stock or otherwise

**Date:** 2026-08-07

**Context:** Immediately after ADR-032, the user asked to change the avatar count, then interrupted their own message to correct it to a content change: the avatars "should include real avatars rather than placeholders." "Real avatars" was ambiguous enough to ask about directly rather than guess — the options put to the user were real photos they'd provide, code-generated illustrated avatars, or stock photos, with the stock-photo option's description explicitly restating the standing concern (stock photos of real people still read to a visitor as photos of real named reviewers, which they aren't). The user chose stock photos. Attempting to fulfill that surfaced a genuine capability gap rather than a judgment call: there is no image-generation tool available in this environment, and guessing or hotlinking stock-photo URLs isn't something to do — unverifiable licensing, and a meaningful chance the URL is simply wrong or dead. Flagged this limitation directly and offered two ways to actually get real photos into the avatar stack (the user supplies files) or ship something now with no external assets (code-generated illustrations). The user chose the latter.

**Decision:** Removed the `ImageIcon`-in-a-circle placeholder from ADR-032. Added `components/ui/generated-avatar.tsx`, a small deterministic-per-seed component: an SVG circle background plus an abstract head-and-shoulders silhouette, clipped to the circle, colors drawn from a 3-entry palette built entirely from existing tokens (`--color-neutral-100/200/400/600` and the brand accent at low opacity) rather than arbitrary new colors — same "no fabricated new hue" discipline as everywhere else in this file. `hero.tsx`'s avatar stack now renders `GeneratedAvatar` per seed instead of the icon circle; the seed array was renamed `REVIEW_AVATAR_SEEDS` (was `REVIEW_AVATAR_PLACEHOLDERS`) to describe what it now actually holds.

**Consequences:** The avatar stack is illustrated, not photographic — closer to Slack/Discord's default-avatar convention than to the reference's real photo stack, and unambiguously not a claim about specific people. Swapping in real photos later (per the `TODO`) means replacing `GeneratedAvatar` call sites with `next/image`, not touching the surrounding layout. No new dependency was added — the avatars are plain inline SVG, consistent with this app not having pulled in an icon-generation or avatar library for a five-element list.

---

## ADR-034: Selected Work trimmed to 4 projects (2×2); Why Unda Studio and Philosophy rebuilt against new references

**Date:** 2026-08-07

**Context:** Three more rounds. First, the user asked Selected Work to show 2 projects side by side on desktop and 1 per row on mobile — the grid already used `md:grid-cols-2`, so this was a content trim (3→2 projects) rather than a layout change; a follow-up then asked for 4 total, still 2 per row, so 2 more illustrative projects were added back (same placeholder-project convention as ADR-034's predecessors — invented project names, not real client work, per the standing "no fabricated case studies" position). Second, the user asked to redesign Philosophy. Third, the user supplied a reference screenshot (a "Why pick us." section: big heading with an accent-colored period, a monospace bracketed eyebrow tag with a small triangle accent, an editorial paragraph mixing bold-black and regular-gray emphasis, a right-aligned supporting line, and a 4-stat bar — "+128 projects delivered," "+15 team," "+25 sites launched," "+5 rating") and asked to redesign Why Unda Studio to match it. The stat bar repeats the fabricated-metrics concern raised for the rating badge (ADR-031/032) — four specific-sounding business numbers this time, not one — so it was flagged again and the user was asked directly rather than assumed; they chose honest equivalents over real-looking invented figures.

**Decision:** `selected-work.tsx`: `PROJECTS` now has 4 entries (added "UX Research · Healthtech" and "Web Design · Hospitality" entries alongside the existing two); grid stayed `grid-cols-1 md:grid-cols-2`, which naturally produces 2 rows of 2 on desktop and a single column on mobile. `philosophy.tsx`: replaced the two-column split layout with a single-column composition — a large decorative quotation mark (`--color-neutral-200`, purely typographic, not an image/gradient/texture) behind the eyebrow, the statement promoted to a real `<blockquote>`, and the three principles upgraded from a bare list into a three-column bar with numbered labels and one-line elaborations each. `why-unda.tsx` was rebuilt to match the new reference's structure — heading with an accent-colored trailing period, a monospace `[ WHY TEAMS CHOOSE UNDA ]` eyebrow tag with a small solid-triangle accent glyph (plain inline SVG, not a Lucide icon — this is a typographic ornament, not a semantic icon), an editorial paragraph with `<strong>`-emphasized phrases pulled from the four reasons ADR-034 is replacing, a right-aligned supporting line, and a stat bar using **honest figures**: "8 core services offered" (matches the real count in `services.tsx`), "1 business day avg. reply" (matches the claim already made on the Contact page), "100% senior-led engagements" (matches the "Senior talent, every time" reason being replaced), and "2026 — year the studio launched" (true). A real bug surfaced and was fixed along the way: the em dash in "designers — we're" was rendering with no space before it ("designers— we're") because of JSX's line-boundary whitespace-collapsing rule (a text line beginning with a space immediately before non-word content gets that leading space trimmed) — fixed by moving that fragment into a `{" — we're "}` expression container, whose internal whitespace JSX doesn't touch. The first fix attempt (an explicit `{" "}` as literal JSX text) was silently undone by `biome format` on the next run, which is why the fix had to move the whitespace inside a string expression rather than JSX text.

**Consequences:** Every quantitative claim now on the Home page (8 services, 1-day reply, 100% senior-led, 2026 launch) is independently checkable against other content already on the site, unlike the rating badge's "4.5/5," which remains the one number on the page without something to point to. `TriangleAccent` is a small one-off inline SVG defined locally in `why-unda.tsx`, not extracted to `components/ui/` — it's used nowhere else yet; extract it if a third spot needs the same accent mark rather than copy-pasting.

---

## ADR-035: Process rebuilt as a scroll-driven stacking-cards layout with a synced sticky step list

**Date:** 2026-08-07

**Context:** The user supplied a reference screenshot of a process/steps section: a sticky left sidebar listing numbered steps (the current one bold/black, the rest gray), paired with a right-hand column of cards that visually overlap and stack on top of each other as the page scrolls, each showing a "/0X" label, a pill badge (icon + step name), a heading, a description, and an image. The user's note was that "those cards overlap each other while scrolling," describing the reference's intentional stacking effect as the thing to replicate, not a bug to avoid. This replaced Process's previous layout — a static 4-column grid of number/title/description, no scroll interaction.

**Decision:** Rebuilt `process.tsx` as a client component (`"use client"` — this section now needs `IntersectionObserver` and state, unlike the rest of Home). Kept Unda's own step names and copy (Discover/Define/Design/Deliver — did not adopt the reference's "Research/Design/Develop/Handoff" labels) but gave each step a fuller heading, description, a Lucide icon (`Search`/`ClipboardList`/`PenTool`/`PackageCheck`), and a `PlaceholderGraphic` image slot (same no-fabricated-photography convention as `selected-work.tsx`), since each step is now a substantial card rather than a grid cell. Left column: `lg:sticky lg:top-[7rem] lg:self-start`, containing the numbered step list; an `IntersectionObserver` (rootMargin `-40% 0px -40% 0px`, i.e. a thin band at vertical center) watches each card and sets an `active` index, which bolds/darkens the corresponding list entry and dims the rest. Right column: each card is `lg:sticky` with a `top` offset that increases by `1.5rem` per index (`calc(7rem + i*1.5rem)`), the standard CSS-only stacking-cards technique — later cards slide up over earlier ones, leaving a peeking strip of the previous card's top edge visible, matching the reference. `shadow-md` was added to the cards since they're genuinely floating/overlapping elements now, which `elevation-and-shadows.md` reserves shadows for (borders remained the choice for every other static card on the page). Both stickiness and the observer are `lg:`-gated in effect — below `lg` the cards have no `sticky` class and just stack in normal flow, since the overlap effect only makes sense with a tall enough viewport and doesn't degrade gracefully on mobile scroll physics. Per-card `Reveal` scroll-fade was deliberately not used here (unlike every other Home section) — animating opacity/position on an element that's simultaneously being scroll-stacked via `sticky` risked fighting itself; only the left sidebar's initial appearance uses `Reveal`.

**Consequences:** Process is now the only client component among Home's sections and the only one using `IntersectionObserver` for state rather than one-shot scroll-reveal. Verified the stacking and active-state sync by scrolling through six positions with Playwright and reading back the screenshots — confirmed the sidebar advances 01→02→03→04 in step with which card is frontmost, and that the mobile fallback (plain stacked cards, no sticky, no active-state styling beyond the observer still technically running) doesn't overflow or error. The four `PlaceholderGraphic` images on this section are additional stand-ins for real photography, same caveat as `selected-work.tsx`'s.

---

## ADR-036: Process cards realigned to start level with the step list; the gradient exception was revoked sitewide

**Date:** 2026-08-07

**Context:** Two more rounds. First, the user pointed out the Process cards (right column) sat too high relative to the step list (left column) — the eyebrow and heading lived inside the left column above the list, so the list started well below the top of its column while the cards started right at the top of theirs, misaligning "Discover" from its corresponding card. Second, the user said plainly they don't want any gradient background design and asked to remove "those lines" — the diagonal crossing line and accent lines inside `PlaceholderGraphic`, visible in the Process cards they'd just been looking at. This reverses the gradient exception ADR-030/031 introduced (`--color-brand-accent` radial washes in Hero, Final CTA, and `PlaceholderGraphic`) — no pushback needed here since it's a move toward *more* restraint, back in line with `unda-design-os`'s original "no gradients as backgrounds" rule rather than away from it.

**Decision:** `process.tsx`: moved the "Process" eyebrow and `<h2>` out of the two-column grid into their own full-width block above it, so the grid's first row is just `[step list | cards]` — both columns now start at the same line by construction, not by a tuned offset. Removed the gradient-wash `<div>` from `hero.tsx` (and the now-unneeded `relative overflow-hidden` on its `<section>`), replaced `final-cta.tsx`'s radial-gradient panel background with a flat `bg-bg-subtle`, and in `placeholder-graphic.tsx` removed the radial-gradient wash `<div>` and the decorative `<line>` elements (the diagonal crossing line, and the two horizontal lines that were variant 3's entire content). Variant 3 needed a real replacement, not just a deletion, since removing its lines would've left a single lonely circle inconsistent with the other three variants' outline-shape-plus-filled-inner-shape pattern — it's now a rotated square (diamond) outline with a filled circle, giving four visually distinct shapes (circle, rounded rect, triangle, diamond) across the variants. Updated the file-header comments in `globals.css` and `nav.tsx` that referenced "the gradient exception" as still-active, since it no longer is.

**Consequences:** This app now carries exactly one documented exception to the base design system beyond the accent color itself — the type-scale step (`--text-hero`, plus the various one-off `clamp()` sizes in Hero/Footer/Why Unda Studio) — rather than two; gradients are gone from every surface. `PlaceholderGraphic`'s four variants are now pure flat-neutral cards with a single-color geometric outline each, closer to the "flat, geometric, no gradients-as-illustration" language `art-direction.md` already specified than the gradient-wash version was.

---

## ADR-037: Button contrast fix reverted at explicit request; hero decorative shapes removed

**Date:** 2026-08-07

**Context:** Immediately after ADR-036, the user asked for white button text (was `text-neutral-900` per ADR-030's WCAG fix) and separately, in the same message, for more visual interest in the now-flat hero. The hero fix added two small flat geometric line-art shapes (a circle and a diamond outline, no gradient, no fill beyond a low-opacity accent tint) — consistent with `PlaceholderGraphic`'s established visual language. For the button, rather than just flipping the text color back to white (which reintroduces the 2.86:1 contrast failure ADR-030 fixed), the fill was changed instead — from the bright `--color-brand-accent` to the deeper `--color-brand-accent-text` shade, which clears 5.2:1 with white text, satisfying "white text" without the contrast regression. The user immediately pushed back: they'd asked for a simple text-color change, not a fill-color change, and separately asked to remove the hero shapes entirely — not adjust them. Flagged the contrast number one more time, plainly, then implemented exactly what was asked without further pushback, per the same "explain once, then respect the user's call on their own site" pattern used for the rating badge and stats bar earlier in this session.

**Decision:** `button.tsx`'s primary variant reverted to `bg-brand-accent text-white` (was `bg-brand-accent-text text-white`) — this reintroduces the 2.86:1 contrast issue on button text, done knowingly at explicit request; the code comment above the class was rewritten to state the actual current contrast number and point here rather than describe a mitigation that's no longer in place. `footer.tsx`'s matching arrow-circle button (same visual pattern, changed alongside the button component in ADR-030 for consistency) was reverted the same way, `bg-brand-accent text-white`, for the same reason. `hero.tsx`'s two decorative SVGs (circle top-right, diamond bottom-left) were removed entirely, along with the `relative overflow-hidden` those SVGs needed on the `<section>` — the hero is back to a plain surface with no background decoration at all, not a tuned-down version of one.

**Consequences:** Primary buttons and the footer's mailto arrow-circle now render white text at 2.86:1 against `#FF6B00` — below WCAG AA's 4.5:1 text minimum. This is the one place in the current build that knowingly ships under the accessibility bar `unda-design-os/CLAUDE.md` states is "a floor, not a stretch goal," at the site owner's explicit, twice-informed direction. If this app is ever audited for accessibility conformance or someone other than the user asks why, this ADR and ADR-030's original fix are the record of what changed and why. The hero has no decorative treatment at all now — flat white/`bg-bg-default`, same as before ADR-036's gradient removal, minus the brief attempt at geometric line-art in between.

---

## ADR-038: Hero background restyled as a blue sky gradient and Nav restyled as a floating pill, both against a Marklab-template reference

**Date:** 2026-08-07

**Context:** The user supplied a reference screenshot (a Framer template called "Marklab") showing a hero with a photographic blue-sky-and-clouds background and a floating, rounded-pill navigation bar inset from the viewport edges, sitting on top of that background. They asked for the hero background and nav to match it, explicit that hero *content and layout* should not change. This directly reverses the previous two rounds of feedback on this exact section — no gradients (ADR-036), no decorative shapes (ADR-037) — so it was flagged before implementing rather than silently reversed again. Two real constraints surfaced: there's no image-generation tool available and no way to responsibly source a real sky photograph (same limitation hit with avatar photos, ADR-033), and recreating the look at all requires exactly the gradient + soft blur that were just removed. Asked the user directly whether to approximate with CSS (gradient + blurred cloud shapes) or supply a real photo file; they chose CSS.

**Decision:** `hero.tsx` gained two new background layers (both `aria-hidden`, `pointer-events-none`, behind all content): a `linear-gradient` sky wash and three `blur-3xl`/`blur-[70px]` white circles standing in for clouds. The gradient hue is 258 — reused from `--color-brand-primary` (Unda Solutions' Royal Blue), not an invented color, so the sky tint is at least traceable to an existing brand token even though it's not `unda-design-os`-sanctioned for this use. Every existing hero element (copy, headline, rating badge, services list, CTAs) is untouched, per the user's explicit instruction. `nav.tsx` was restructured from a full-bleed bar that solidified on scroll into an always-frosted floating pill: `fixed` header with `px-300 pt-300 md:px-400 md:pt-400` outer inset, containing a `rounded-full border bg-bg-default/85 backdrop-blur-md shadow-sm` pill sized to the same `max-w-[1200px]` as the rest of the site. The scroll-driven `scrolled` state and its listener were removed entirely — the pill's own background/border/shadow keep it legible against any page content behind it (colorful hero or plain white), so there was nothing left for a scroll transition to do. The mobile menu became a separate rounded card (`rounded-2xl`, same horizontal inset) below the pill instead of a full-bleed panel flush with the (now nonexistent) full-width header edge.

**Consequences:** This is the app's first background image-adjacent decoration since ADR-036 removed gradients everywhere; it's scoped to the Hero section's backdrop only, not reintroduced anywhere else (Final CTA, `PlaceholderGraphic` stay flat, per ADR-036/037). The floating pill nav now appears identically on every page, including the plain-white stub pages (`/work`, `/services`, etc.) and the dark Footer's sibling pages — verified via screenshot that it reads fine against both the sky gradient and plain white. Removing the scroll-solidify behavior is a real interaction change beyond "background and nav styling," but was necessary: a bar that used to rely on transparency-over-content at the top and solidify on scroll doesn't have an equivalent "transparent" state once it's a self-contained floating pill with its own permanent background.

---

## ADR-039: Hero background switched from blue to the brand's own orange hue; orange tint scale added and applied across Home for section rhythm

**Date:** 2026-08-07

**Context:** Immediately after ADR-038, the user pointed out the hero's sky gradient should use "a lighter shade of the website colour, not blue" (ADR-038 had reused `--color-brand-primary`'s hue 258, Unda Solutions' Royal Blue, reasoning it was at least an existing token — the user correctly weighted "matches Studio's actual orange identity" over "reuses *some* existing token, even the wrong division's"), and asked for illustrations added back to the hero background. Separately, and more broadly, the user asked for the site to stop defaulting every "subtle" section background to white or neutral gray, and to use different tints/shades of the brand orange (`#FF6B00`) across sections instead — explicit that this should apply to "some" sections, not universally.

**Decision:** Added two new tokens to `globals.css`, same hue (45.1) as the accent, both far lighter: `--color-accent-wash` (`oklch(0.97 0.022 45.1)`, for section backgrounds) and `--color-accent-wash-strong` (`oklch(0.93 0.04 45.1)`, for panels/emphasis — one step deeper). Rewrote `hero.tsx`'s background: the gradient now runs through the accent's own hue instead of 258, and the flat white cloud-blur circles were supplemented with three `aria-hidden` outline-only SVG illustrations (a large circle, a diamond, a triangle — the same geometric vocabulary already established by `PlaceholderGraphic` and the earlier Process/Hero decorations in ADR-036/037, not a new visual language). Applied `bg-accent-wash` to `philosophy.tsx` and `process.tsx` (both previously `bg-bg-subtle` gray) and `bg-accent-wash-strong` to the Final CTA panel (previously `bg-bg-subtle`) — chosen deliberately to alternate with the sections that stayed white (Selected Work, Services, Why Unda Studio, Testimonials), rather than tinting every section, per the user's "some" qualifier. Philosophy's large decorative quotation mark changed from `text-neutral-200` to `text-accent-wash-strong` so it reads as part of the same warm palette rather than a leftover gray element on a now-warm background.

**Consequences:** Home now reads as Hero (tint) → Selected Work (white) → Philosophy (tint) → Services (white) → Why Unda Studio (white) → Process (tint) → Testimonials (white) → Final CTA (deeper tint) → Footer (dark) — an alternating rhythm rather than a uniform wash, verified via full-page screenshot at desktop and a targeted mobile screenshot, both with no horizontal overflow or console errors. `PlaceholderGraphic`'s own card background was deliberately left `bg-bg-subtle` (neutral) rather than tinted — its cards already contain orange line-art illustrations, and a neutral frame keeps those legible rather than having orange-on-orange compete with itself. The two new tokens are available for any future section that wants the same warm-but-restrained treatment; nothing requires every remaining white section to convert.

---

## ADR-040: Studio's accent changed from Orange to Azure (blue), a third division-accent rebrand

**Date:** 2026-08-07

**Context:** The user said the orange "is so bad" and asked to revert the Hero back to the blue from ADR-038, extending it to the entire site — accent color, buttons, everything. That ADR-038 blue reused hue 258, which is `--color-brand-primary` — Unda Solutions' own Royal Blue. Flagged this before proceeding: if Studio's division accent became identical to the parent brand's primary color, Studio would stop reading as its own division and start reading as a reskinned corporate site, undermining the entire point of `unda-design-os`'s cross-brand accent system (each division gets one accent color specifically so a visitor still recognizes which "room in the building" they're in). Asked the user directly whether they wanted the exact same blue as Solutions (a real architecture change) or a distinct blue that still delivers "the blue feel"; they chose distinct.

**Decision:** Chose hue 230 — reads clearly as blue, sits roughly equidistant from Royal Blue (258, Δ28°) and Technologies' Cyan (200, Δ30°), avoiding collision with either sibling division. Named it "Azure" in all documentation to keep it verbally distinct from "Royal Blue" (Solutions) despite both being blue-family colors. Checked contrast the same way as the orange (ADR-030): at the system's standard formula (`oklch(0.58 0.15 230)`), white text is 3.97:1 — still under WCAG AA's 4.5:1 — so lightness was dropped to 0.52 (5.06:1). Unlike the orange, this works for *both* text-on-white and white-on-fill at the same value (contrast is symmetric), so `--color-brand-accent` and `--color-brand-accent-text` are now numerically identical — no second, darker shade was needed this time, though both tokens stay defined so the many existing `bg-brand-accent`/`text-brand-accent-text` call sites across the app didn't need touching. This is Studio's third accent in one session: Purple (ADR-010, original) → Orange `#FF6B00` (ADR-029/030) → Azure (this ADR). Updated every doc touched in ADR-029 the same way (`color-tokens.md`, `cross-brand-rules.md`, `foundations/color.md`, `cross-brand-consistency.md`, `12-future-expansion-rules.md`'s hue list, `unda-design-os/CLAUDE.md` rule 5), and every hardcoded literal-color spot in code (`globals.css` tokens, `hero.tsx`'s gradient/illustration/cloud colors, `placeholder-graphic.tsx`'s four variants, `generated-avatar.tsx`'s palette) via the same `sed` substitution used for the orange rebrand, plus manual fixes for two gradient stops the substitution pattern didn't match exactly.

**Consequences:** Button and footer-arrow-circle white text, reverted to bright-fill-plus-white at the user's explicit request in ADR-037 (a known WCAG failure at 2.86:1 with orange), is now compliant again at 5.06:1 with no further change needed — an incidental fix that came free with the color swap, not something separately requested. The accent-wash/accent-wash-strong tint tokens from ADR-039 automatically became light blue tints instead of light orange ones, since they're defined relative to the accent hue. Verified with `biome check`, `tsc --noEmit`, `next build`, and full-page + mobile Playwright screenshots before considering it done.

---

## ADR-041: Real bug found — `tailwind-merge` was silently stripping `text-white` from every primary button; hero illustrations removed again; nav link spacing widened

**Date:** 2026-08-07

**Context:** Three requests. First, remove the hero's geometric illustrations added in ADR-039 — keep the gradient/cloud background, drop the circle/diamond/triangle shapes. Second, "the button text colour should be white not black" — despite `button.tsx`'s primary variant already reading `bg-brand-accent text-white` in source, and despite ADR-040's Consequences section explicitly claiming this was "compliant again... with no further change needed." That claim was checked by reading the class list, not the actual rendered color — the gap that let this ship unnoticed through ADR-037 and ADR-040. Rather than assume the user was wrong, the actual computed style was checked directly (`getComputedStyle(...).color`), which returned `lab(3.6 ...)` — near-black, not white. Inspecting the live DOM's `className` (not the source file) showed `text-white` was completely absent from the rendered class list on every primary button instance. Root cause: `cn()`'s `twMerge` call doesn't know this app's custom font-size scale (`text-hero`, `text-h1`...`text-caption`) is a distinct token group from Tailwind's built-in text-*color* utilities — both share the `text-` prefix, and `twMerge` doesn't recognize the custom names as a `font-size` group, so it folds them into a generic `text-*` conflict bucket alongside `text-white`/`text-brand-accent`/etc. Since `Button`'s `cn(base, variantClasses[variant], sizeClasses[size], className)` puts `sizeClasses` (containing `text-body-sm`/`text-body`/`text-body-lg`) *after* `variantClasses` (containing `text-white`), `twMerge` treated the size class as superseding the color class in the same group and dropped `text-white` outright — a real, undetected accessibility and visual bug affecting every `Button` instance sitewide, not a styling preference. Third, nav's desktop link spacing (`gap-300`, 24px) felt tight.

**Decision:** Removed the three `<svg>` illustrations from `hero.tsx`'s background layer, keeping the gradient wash and the two blurred white "cloud" circles. Fixed the `twMerge` bug at its source in `src/lib/utils.ts`: replaced the plain `twMerge` import with `extendTailwindMerge({ extend: { classGroups: { "font-size": [...] } } })`, explicitly registering this app's ten custom type-scale tokens as their own `font-size` group — this is the general fix, not a one-off reorder of Button's class string, because the same collision risk exists anywhere `cn()` combines a text-color utility with one of these custom size classes, not just in `Button`. Verified the fix by reading `getComputedStyle` on the live page (`rgb(255, 255, 255)` now, confirmed, not just the presence of a class name in source). Updated `button.tsx`'s stale comment, which still described the ADR-037 orange-contrast story as the live concern; it now states the current Azure contrast number and points to both ADR-037 (historical) and this ADR (the actual bug). Widened `nav.tsx`'s desktop link `gap-300` → `gap-400` (24px → 32px).

**Consequences:** This bug likely affected every session between ADR-030 (when `text-white` was first set on the primary button) and now, across every color change in between (orange, the ADR-037 revert, Azure) — the button text was probably never actually white in the browser during any of that work, only in the source code, and no prior verification step in this session caught it because screenshots were reviewed visually rather than checked against computed styles, and "white text" reads as very-dark-on-medium-color at a quick glance in a small screenshot. The general lesson going forward: when a user reports a color/style that "looks wrong" despite the source appearing correct, check `getComputedStyle` on the live DOM before assuming the source is the ground truth — `cn()`/`twMerge` can silently disagree with what's written. The `extendTailwindMerge` fix is defensive for the whole app, not scoped to `Button` — any future component combining a text-color utility with this app's custom type-scale classes is now protected.

---

## ADR-042: Hero rebuilt black-and-white; accent reverted Azure → Orange, scoped strictly to interactive elements

**Date:** 2026-08-07

**Context:** The user said they weren't feeling the Azure hero and asked for two changes together: the Hero background to go black-and-white (black background, white text, no gradient/tint), and the orange `#FF6B00` accent to come back — but explicitly scoped to "button or links" rather than backgrounds. This is Studio's accent's fourth state change this session (Purple → Orange → Azure → Orange again), and the "orange for interactive elements only" framing directly contradicts ADR-039's approach of tinting Philosophy/Process/Final CTA's *backgrounds* with the accent — so those were reverted too, not just the hue swapped.

**Decision:** Rewrote `hero.tsx`: section background is now `bg-neutral-900` (the same dark neutral Footer already uses — no new color), all text flipped to the light end of the neutral scale (`text-neutral-0` for primary content, `text-neutral-400` for secondary/muted, matching Footer's already-established dark-surface convention), dividers changed from `border-border-default` (light-mode gray) to `border-neutral-700` (Footer's dark-divider tone), and the avatar ring changed from `ring-bg-default` (white, for separation on light bg) to `ring-neutral-900` (matching the new dark bg). The `variant="ghost"` "View our work" button — designed for light backgrounds, unusable as-is on black — got a `className` override (`text-neutral-0 hover:bg-neutral-800 active:bg-neutral-700`) rather than a new dark-mode Button variant, since it's needed in exactly one place; verified via `getComputedStyle` (not just reading the source) that the override actually wins, learning from ADR-041's lesson that `cn()`/`twMerge` behavior isn't always what the source implies. Reverted the accent hue from 230 (Azure) back to 45.1 (Orange) everywhere ADR-040 had changed it — `globals.css` tokens (restoring the two-shade split, since `#FF6B00` fails the symmetric-contrast trick Azure's single value allowed), the hardcoded literals in `placeholder-graphic.tsx` and `generated-avatar.tsx`, and all five `unda-design-os` docs touched in ADR-040. Additionally reverted `philosophy.tsx`, `process.tsx`, and `final-cta.tsx`'s section backgrounds from `bg-accent-wash`/`bg-accent-wash-strong` back to `bg-bg-subtle` (neutral gray) — the accent-wash tokens stay defined in `globals.css` for potential future use, just no longer applied anywhere, per "orange is for buttons/links, not backgrounds."

**Consequences:** The site now bookends in black (Hero, Footer) around white/neutral body content, with orange appearing only on buttons, links, small indicator dots, eyebrow captions, numbered indices, and avatar-palette accents — a more restrained accent footprint than ADR-039 had, closer to `unda-design-os`'s original "color is emphasis, not decoration" guidance than any version of Studio's site this session has shipped. `button.tsx`'s comment was updated back to describe the 2.86:1 orange-contrast trade-off as live (it was describing Azure's passing 5.06:1 a few ADRs ago) — this file's comment has now flipped twice in one session tracking the same underlying color swap back and forth; worth treating as a signal that the accent color itself may still be unsettled rather than assuming this is the final word.

---

## ADR-043: Process rebuilt around a reference (bracket-labeled steps + generative dot/line patterns); Why Unda Studio's stat bar restyled to match

**Date:** 2026-08-07

**Context:** The user supplied a reference screenshot — a 3-column process section: each column has a small triangle accent + monospace `[ STEP N ]` label, a large abstract mark (a circle of dots, a square of dots, a circle of horizontal lines split down the middle — alternating orange and gray), a title, and a short centered description — and asked for Process to adopt this look, extended to 4 columns to match Unda's four steps (Discover/Define/Design/Deliver). This fully replaces Process's prior layout (ADR-035's sticky-stacking cards with `PlaceholderGraphic` images and an `IntersectionObserver`-driven active step list) rather than adjusting it. Separately, the user asked to redesign Why Unda Studio "to follow the way the others looks like" — read as: make its 4-item stat bar share this same visual system as the new Process section, for consistency between the two 4-column grids on the page, rather than leaving it as the bare number-plus-label format from ADR-039.

**Decision:** Extracted `TriangleAccent` (previously defined inline, only in `why-unda.tsx`) to `components/ui/triangle-accent.tsx` now that a second consumer needs it — the extraction ADR-002 calls for once real duplication shows up, not before. Added `components/ui/generative-pattern.tsx`: a procedurally-generated 13×13 dot or line grid, either left as a square or circle-clipped, optionally split into two halves by a center gap, colored via a `tone` prop (`accent` = brand orange, `neutral` = `--color-neutral-400`) — this is a decorative mark, not a semantic icon, so it isn't a Lucide glyph (consistent with `PlaceholderGraphic`'s and `hero.tsx`'s existing decorative-SVG precedent). Rebuilt `process.tsx` as a plain 4-column grid (no more `"use client"`, no `IntersectionObserver`, no sticky positioning — this section is fully static now): each step gets a bracket-labeled eyebrow, a `GenerativePattern` (alternating circle/square, dots/lines, accent/neutral across the four steps for visual rhythm), a title, and a short description, all centered. Rebuilt `why-unda.tsx`'s stat section the same way — same bracket-label + `GenerativePattern` + centered layout — with the stat `value` (8/1/100%/2026) standing in for the step title and the existing honest label as the description; the stat *content* itself is unchanged from ADR-039, only its presentation.

**Consequences:** Process lost its scroll-driven interaction entirely — the sticky-stacking effect from ADR-035, which took real engineering effort and was working correctly, is gone in favor of a simpler static grid the user's reference called for. If a future request wants scroll-driven interest back, it won't be a revert of this ADR (the underlying content model is different now — no per-step image, no `PlaceholderGraphic` dependency). Process and Why Unda Studio now visually read as a matched pair — anyone scrolling past both will recognize the shared pattern-icon language — which was the explicit goal of "follow the way the others looks like." Verified via full-page and mobile Playwright screenshots, `biome check`, `tsc --noEmit`, and `next build` with no overflow or console errors.

---

## ADR-044: Home restructured — new "Who We Are" section, black/white section rhythm, accordion services, carousel testimonials, orange Final CTA; Why Unda Studio removed

**Date:** 2026-08-07

**Context:** The user supplied three reference screenshots and a full section-by-section walkthrough of the desired new Home page: (1) a "Who We Are" hero-adjacent section — badge, big headline, "About The Studio" link, and an infinite horizontal scroll of images below; (2) an accordion-style services list with a +/× toggle, title, expandable description, and a "FROM $X,XXX" price; (3) a centered testimonial layout — italic headline, a dotted divider with a star, one large italic quote at a time, dot pagination, and a "read all N+ reviews" link. The requested new order: Hero → Who We Are → Selected Work (now black) → What We Do/Services (white, accordion) → Process (now black) → Testimonials (redesigned, white) → Final CTA (now orange, white text), with Why Unda Studio removed outright and Philosophy left unmentioned (kept, placed between Selected Work and Services, since removal wasn't requested for it specifically). Two fabrication concerns were flagged before building, consistent with the standing position on invented numbers this session: reference (2)'s specific dollar prices and reference (3)'s "80+ reviews" claim — Unda Studio has neither real pricing tiers nor a review count. Flagged once, then proceeded with honest substitutes rather than blocking.

**Decision:** New `components/home/who-we-are.tsx`: a badge + headline + "About the studio" link (linking to `/studio`) row, then a full-width infinite marquee of `PlaceholderGraphic` cards — a `@keyframes marquee` added to `globals.css` (translateX 0 → -50% on a doubled item list, the standard seamless-loop technique), gated with `motion-reduce:animate-none`. `selected-work.tsx` and `process.tsx`: section background changed to `bg-neutral-900`, all text tokens flipped to the light end of the scale (`text-neutral-0`/`text-neutral-400`, matching Footer's established dark-surface convention), and accent text switched from `brand-accent-text` (the darker shade, tuned for *white* backgrounds) to `brand-accent` (the bright shade, which is what actually has strong contrast against black — the two orange tokens are not interchangeable by background). `services.tsx` rebuilt as a client component: an accordion (Lucide `Plus`/`Minus` toggle in a circle, one open at a time via `useState`, `aria-expanded`/`aria-controls` wired correctly) with **"Custom pricing"** in place of the reference's fabricated dollar figures. `why-unda.tsx` deleted outright (no remaining references — confirmed via grep before deleting, not left as dead code). `testimonials.tsx` rebuilt as a client component: single centered quote at a time (italic, General Sans — no new typeface added), a dotted-line-and-star divider, and clickable dot pagination cycling through the existing three placeholder testimonials; the reference's "read all 80+ reviews" link was dropped rather than given a fabricated count or a link to a reviews page that doesn't exist. `final-cta.tsx`: panel background changed to `bg-brand-accent`, heading/subtext to white/`white/85`, and — since an orange button on an now-orange panel would be invisible — the CTA button's `className` override switched to `bg-white text-neutral-900`, verified via `getComputedStyle` (not just reading the source) per the ADR-041 lesson. `page.tsx` reordered to Hero → WhoWeAre → SelectedWork → Philosophy → Services → Process → Testimonials → FinalCta.

**Consequences:** The page now alternates black/white/black/white through its middle sections (Hero black, Who We Are white, Selected Work black, Philosophy light gray, Services white, Process black, Testimonials white, Final CTA orange, Footer black) — a much more editorial, high-contrast rhythm than the all-white-with-orange-tints version from earlier in this session. Two components (`services.tsx`, `testimonials.tsx`) gained client-side interactivity they didn't have before (accordion state, carousel state) — Process similarly gained and then lost interactivity earlier this session (ADR-035 → ADR-043), so the pattern of "add interaction, then simplify it away" has now happened twice; if that keeps recurring it may be worth asking directly whether interactive richness or static simplicity is the actual preference, rather than continuing to infer it per-request. Verified with `biome check`, `tsc --noEmit`, `next build`, full-page screenshots at desktop and mobile (no overflow, no console errors), and live interaction checks — the accordion's `aria-expanded` actually flips on click, the testimonial dots actually swap the quote, and the marquee's `animation-name`/`animation-duration` are actually applied — rather than assuming from the source that they work.

---

## ADR-045: Who We Are's marquee rebuilt with real device-frame screenshots of this actual site, not abstract shapes

**Date:** 2026-08-07

**Context:** The user supplied a reference showing three device-mockup cards — a browser/tablet frame with traffic-light dots and a URL bar, each on a different colored background (purple, dark green, orange), each containing a real screenshot of an actual client project — and asked for the Who We Are marquee (ADR-044's `PlaceholderGraphic`-based version) to look like this. The reference's screenshots are real other-company work Unda Studio doesn't have. Rather than fabricate fake client screenshots (mocking up a fictional product UI would be a more elaborate version of the same fabrication problem raised for pricing, review counts, and testimonials throughout this session) or fall back to abstract shapes again (which wouldn't match what was asked), a third option was available and used: screenshot *this actual, real, already-built site* and put those genuine captures inside the device frames. That's honest — it's really Unda Studio's own real work — while still delivering the literal visual asked for.

**Decision:** Captured three real crops of the live dev server via a Playwright script (`services.png`, `testimonials.png`, `final-cta.png`, saved to `apps/studio/public/showcase/`) — the Services accordion, the Testimonials carousel, and the orange Final CTA, chosen so the three screenshots' own backgrounds (white, white, orange) would contrast against three different card-surround colors. The first capture attempt included the fixed nav pill overlapping the section heading (a real artifact of capturing a page with `position: fixed` chrome via a clipped, non-full-page screenshot); refined the crop's Y-offset once and re-verified rather than shipping the flawed capture. Built `components/ui/browser-frame.tsx`: a colored card (`cardClassName`) wrapping a macOS-style browser-chrome bar (three traffic-light dots, a mono-font URL pill reading `studio.undasolutions.com/...`) above a `next/image` of the captured PNG. Rewrote `who-we-are.tsx`'s marquee to render three `BrowserFrame`s (orange/`bg-brand-accent`, black/`bg-neutral-900`, light/`bg-bg-subtle` — Unda's own palette, not the reference's purple/green) instead of `PlaceholderGraphic`s, doubled for the loop as before.

**Consequences:** This is the first time real screenshots — not illustrated placeholders, generated avatars, or geometric line art — appear anywhere on the site; they're genuine (of this actual build), so they don't carry the same "will need replacing" caveat as the other placeholder content, though they'll go stale if the sections they capture change shape later and nobody re-runs the capture script. That capture script isn't currently saved anywhere reusable (it ran once from the scratchpad) — if these need refreshing after a future redesign, the crop coordinates will need to be re-derived rather than re-run verbatim. Verified via full-page and mobile Playwright screenshots with no overflow or console errors.

---

## ADR-046: Philosophy moved from Home to `/studio`; Who We Are's marquee simplified back to plain landscape placeholders

**Date:** 2026-08-07

**Context:** Two changes. First, the user pointed at the Philosophy section (screenshotted from the live site) and said "this would be on the studio page" — read as: move it off Home entirely, onto the `/studio` route, which had been a bare `ComingSoon` stub since it was first scaffolded. Second, ADR-045's device-frame marquee (real screenshots of this site inside browser-chrome mockups) wasn't what was wanted — the user asked for the marquee images to be plain landscape placeholders instead, explicit that they'll supply real photos themselves later ("I would still be adding the pictures later just add a placeholder for now"). This reads as a preference for the *simplest* honest signal ("a photo goes here") over the more elaborate ADR-045 workaround of substituting real self-screenshots for the fabricated client work the original reference showed.

**Decision:** Moved `philosophy.tsx` from `components/home/` to a new `components/studio/` directory (domain-based organization — it's no longer a Home section) and added top padding (`pt-[7rem] md:pt-[9rem]`, matching `ComingSoon`/Contact's page-top convention) since it's now the first element on its page, sitting directly under the fixed nav pill rather than lower down a longer scroll. `app/studio/page.tsx` now renders `<Philosophy />` in place of `<ComingSoon .../>` — the first of the four placeholder division pages (`/work`, `/services`, `/studio`, `/journal`) to get real content instead of a stub. `app/page.tsx` no longer imports or renders Philosophy. Rewrote `who-we-are.tsx`'s marquee: removed the `BrowserFrame` usage entirely in favor of plain `aspect-video` boxes (`bg-bg-subtle`, bordered, a centered Lucide `ImageIcon` and nothing else — no caption text, no fake content) repeated and doubled for the loop. Deleted `components/ui/browser-frame.tsx` and `public/showcase/*.png` outright (confirmed via grep that nothing else referenced `BrowserFrame` before deleting) rather than leaving them as unused dead code now that ADR-045's approach is fully superseded.

**Consequences:** `/studio` is no longer a placeholder — it's a real page with real content, the first division subpage in this repo to cross that line (per root `CLAUDE.md`, `/work`/`/services`/`/journal` remain intentional stubs until separately requested). The marquee is now honestly empty-looking (plain gray boxes with an icon) rather than dressed up with either fabricated content or a workaround — a deliberate trade against visual polish until real photography exists, which is what was asked for. `apps/studio/public/` currently holds no image assets at all again, same state as before ADR-045.

---

## ADR-047: Payload CMS embedded into `apps/studio` for Media, Case Studies, Testimonials, and Journal posts

**Date:** 2026-08-07

**Context:** The user asked to be able to edit all of Studio's images, portfolio case studies, testimonials, and blog/journal posts through a CMS dashboard rather than hardcoded content, pointing at Payload CMS specifically. A plan was proposed and confirmed before any code was written (per root `CLAUDE.md`'s "explain approach, then implement" rule for anything with real architectural weight): Payload embedded directly into `apps/studio` (no separate service — Payload 3.x runs inside an existing Next.js app), four collections (Media, Case Studies, Testimonials, Posts), Postgres via the Vercel/Neon Marketplace integration, Vercel Blob for uploaded files, and a single admin user for now. The user separately confirmed provisioning live infrastructure immediately rather than deferring it.

**Decision:** Provisioned Neon Postgres (`neon-amber-field`) and a Vercel Blob store (`studio-2`) on the linked `studio` Vercel project via `vercel integration add neon` / `vercel blob create-store`, pulled the resulting env vars into `apps/studio/.env.local` (gitignored) via `vercel env pull`, and added a locally-generated `PAYLOAD_SECRET`. Installed `payload`, `@payloadcms/next`, `@payloadcms/richtext-lexical`, `@payloadcms/db-postgres`, `@payloadcms/storage-vercel-blob`, and `sharp`, pinning `graphql@^16.8.1` explicitly (the auto-resolved v17 conflicts with Payload's peer range). Restructured `apps/studio/src/app/` into two sibling route groups, each with its own independent root layout, per Payload's own documented pattern: `(frontend)/` holds every existing marketing page plus the original Nav/Footer/font/metadata layout untouched, and `(payload)/` holds Payload's admin UI (`admin/[[...segments]]/page.tsx` + `not-found.tsx`) and its REST API (`api/[...slug]/route.ts`) under a layout that renders Payload's own `RootLayout` — the two trees share no chrome, so the CMS dashboard doesn't inherit the site's Tailwind globals or the site doesn't inherit the CMS's. `payload.config.ts` wires the four collections (`src/collections/*.ts`) plus a `Users` collection for admin auth, `postgresAdapter` pointed at `DATABASE_URL`, `lexicalEditor`, and the `vercelBlobStorage` plugin scoped to the `media` collection. `next.config.ts` wraps the config with `withPayload`, and `tsconfig.json` adds the `@payload-config` path alias Payload's own tooling expects.

Two real bugs surfaced during verification, not just source-level checks — the ADR-041 lesson (always confirm computed/rendered behavior, not just that the code reads correctly) applied again here: (1) `payload generate:importmap` crashes outright on this machine's Node 23.10.0 due to an unrelated `ERR_REQUIRE_ASYNC_MODULE` interaction between `tsx`'s CJS loader and `@payloadcms/richtext-lexical`'s ESM build — a tooling/Node-version issue, not a config bug, and non-blocking since it only affects a CLI convenience command; (2) far more serious, `/admin` rendered as a fully blank white page in the browser despite returning HTTP 200 — DOM inspection showed the page stuck in an unresolved React Suspense boundary, and network inspection of the RSC payload revealed Payload's internal `redirect()` call (`/admin` → `/admin/login` → `/admin/create-first-user`) was never actually being followed client-side. Root cause: `payload.config.ts`'s `admin` block was missing `importMap.baseDir`, and the placeholder `(payload)/admin/importMap.js` (created empty as a stand-in before the CLI generator could run) was missing the entry for `@payloadcms/storage-vercel-blob/client#VercelBlobClientUploadHandler` that the Vercel Blob plugin registers regardless of whether client-direct uploads are enabled. Fixed both: added `importMap.baseDir: path.resolve(dirname)` to match Payload's own template, and manually wrote the missing import into `importMap.js` (importing `VercelBlobClientUploadHandler` from `@payloadcms/storage-vercel-blob/client` directly, since the CLI generator can't run here) — confirmed via Playwright that this, not the Turbopack-vs-webpack dev server choice or the `--no-server-fast-refresh` flag (both tried and ruled out first), was the actual fix: the redirect chain now completes and the real "create your first user" form renders.

**Consequences:** `apps/studio` now has a working, locally-verified `/admin` dashboard backed by real Neon Postgres and Vercel Blob infrastructure — schema was auto-pushed on first boot (`case_studies`, `media`, `testimonials`, `posts`, plus Payload's own internal tables), confirmed via direct `psql` inspection. No first admin user has been created yet — that requires real credentials only the user should set, so it was left for them rather than fabricated. The four collections currently hold field definitions only (title/slug/media relations/rich text/featured flags) with no content and no wiring back into the marketing site yet — `/work`, `/journal`, and Home's Selected Work/Testimonials sections still render the hardcoded arrays from earlier ADRs; rewiring them to Payload's Local API is a distinct follow-up, not done here. `apps/studio/src/app/globals.css` and `favicon.ico` deliberately stayed at their original top-level `src/app/` location rather than moving into `(frontend)/`, referenced via a relative `../globals.css` import, since Payload's own `(payload)` layout needs to *not* pull in the site's Tailwind theme. The `payload generate:importmap` CLI remains broken in this environment (Node 23 + tsx interaction) — future new upload-enabled collections or storage adapters will need their import-map entries added by hand following the same pattern used here, or generated on a machine running an LTS Node version, until that tooling bug is resolved upstream. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean — `/admin` and `/api/[...slug]` build as dynamic routes, all eight marketing routes prerender as static as before), and Playwright checks of both the CMS (`/admin` create-first-user form actually renders and its redirect chain actually completes) and the marketing site (all eight frontend routes return 200; Home's Nav/fonts/hero render pixel-identical to before the restructure).

---

## ADR-048: Home's Selected Work and Testimonials wired to Payload; sections hide honestly when empty; ISR added so admin edits actually show up

**Date:** 2026-08-07

**Context:** Following ADR-047, the user created their first admin account and asked what's next. Rather than have them populate content that wouldn't yet appear anywhere on the live site, the two Home sections still on hardcoded arrays — Selected Work (case studies) and Testimonials — were the obvious next target ("just wire the ones we have for now we would re wire again after setting it up again"), scoped deliberately smaller than the full `/work` and `/journal` listing pages (which don't exist as real pages yet — building those is new page development, not rewiring, and out of scope for this pass).

**Decision:** `selected-work.tsx` and `testimonials.tsx` became async Server Components querying Payload's Local API directly (`payload.find({ collection: "case-studies" | "testimonials", where: { featured: { equals: true } } })`) rather than reading local arrays. Since `testimonials.tsx` needs `useState` for its carousel (a client concern), it was split: `testimonials.tsx` stays a server component that fetches and passes props, `testimonials-carousel.tsx` (new file) is the `"use client"` carousel itself. Added `src/lib/payload.ts` with a `getPayloadClient()` helper and hand-written `Media`/`CaseStudy`/`Testimonial` types — `payload generate:types` hits the same Node 23 CLI bug noted in ADR-047, so these are maintained by hand against `src/collections/*.ts` until that's fixed. Both sections return `null` when Payload has zero featured docs, rather than falling back to the old hardcoded content or showing an awkward empty heading — consistent with this session's standing "no fabricated content" position, just applied to the empty-state case now instead of the placeholder-imagery case. `next.config.ts` gained an `images.remotePatterns` entry for `*.public.blob.vercel-storage.com` so `next/image` can serve uploaded Vercel Blob files.

Verification went beyond type-checking: a temporary Route Handler (`(frontend)/debug-seed-temp/route.ts`, POST to create one real case study + testimonial via the Local API using the user's own already-uploaded media, DELETE to remove them) was used to seed real data through the actual running dev server — not a mock — confirming the full pipeline end-to-end via Playwright (the real uploaded image rendered through `next/image` from the Blob CDN, the real testimonial text appeared in the carousel, the carousel's dot pagination and Process's static grid were unaffected), then deleted immediately after, restoring the DB to empty and confirming the sections correctly disappeared again. Two mistakes surfaced and were corrected before landing on this approach: the debug route was first placed under `(payload)/api/`, which `withPayload`'s rewrite rules intercept unconditionally (404'd via Payload's own handler, not Next's); moved it under `(frontend)/` instead. It was then given an underscore-prefixed folder name (`_debug-seed`), which Next.js treats as a private, unrouted segment by convention — renamed to `debug-seed-temp` to actually route. Separately, `next build` was run and initially hit `ETIMEDOUT` connecting to Postgres while prerendering `/` as a fully static page — surfacing a real latent problem, not just a build flake: without revalidation, content added via `/admin` would never appear on the live site until the next deploy, defeating the point of a CMS. Added `export const revalidate = 60` to `(frontend)/page.tsx`, confirmed via a clean rebuild that `/` now builds with `Revalidate: 1m` rather than pure static.

One unrelated fix bundled in: Payload's dev server auto-writes `(payload)/admin/importMap.js` as real content grows (it replaced the minimal hand-written stub from ADR-047 with a much larger generated one, confirming Payload's own dev-mode generation does work despite the CLI command being broken) — Biome kept flagging its import order on every regeneration. Excluded it from linting via `apps/studio/biome.json`'s `files.includes` (`"!src/app/(payload)/admin/importMap.js"`), matching the official template's own "DO NOT MODIFY, GENERATED AUTOMATICALLY" convention for this file.

**Consequences:** Case studies and testimonials added via `/admin` and marked "featured" will now appear on Home within 60 seconds without a redeploy. Both sections stay invisible until the user adds real featured content — currently true again after the verification cleanup, so the live Home page is unchanged from ADR-046's state until the user populates `/admin`. `/work` and `/journal` remain `ComingSoon` stubs, not wired to Payload — a distinct, larger follow-up (building real listing/detail pages, not just swapping a data source) explicitly deferred rather than silently expanded into. The hand-written Payload types in `src/lib/payload.ts` will drift from `src/collections/*.ts` if collections change without updating both — worth replacing with real generated types once the Node/CLI incompatibility is resolved. Verified clean via `pnpm format`, `pnpm lint`, `pnpm check-types`, and `pnpm build` after all changes, including the ISR fix.

---

## ADR-049: `/work` built out as a real Payload-backed listing + case study detail page; slug field normalized after a real data bug

**Date:** 2026-08-07

**Context:** The user, having confirmed ADR-048's Home wiring worked ("I can add a casestudy and a testimonial and he showed on the home page"), asked to move on to `/work` — this time explicitly requesting the page itself, unlike ADR-048 where building `/work`/`/journal` was deliberately deferred as a larger, un-requested scope expansion.

**Decision:** Replaced `/work`'s `ComingSoon` stub with a real listing page: an async Server Component fetching *all* case studies (not just `featured`, unlike Home's subset) via `payload.find`, rendering the same card treatment as Home's Selected Work section, with a graceful "check back shortly" message when the collection is empty rather than a bare heading over nothing. Added `/work/[slug]/page.tsx` — a case study detail page rendering cover image, category/client metadata, and three optional rich-text sections (Problem/Approach/Outcome) via `@payloadcms/richtext-lexical/react`'s `RichText` component, plus an optional image gallery; each block only renders when the user has actually filled it in, so partially-completed case studies don't show broken empty sections. `RichText`'s `data` prop is strictly typed to `SerializedEditorState`, which the hand-written `CaseStudy` type (ADR-048, `unknown` for rich-text fields since generated types aren't available) doesn't satisfy — narrowed via a local `ComponentProps<typeof RichText>["data"]` type extracted at the render boundary rather than adding `lexical` as a new direct dependency just for one type. Both `/work` and the detail page carry `revalidate = 60`, matching Home's ISR pattern from ADR-048.

Verifying this against the user's actual just-added "DROP HOG" case study (not synthetic test data — their real first content) surfaced a genuine bug: its slug was stored as `/drophog` (leading slash included), because the field's admin help text — "Used in the URL, e.g. `/work/your-slug`" — read as an instruction to type the full path rather than just the segment. This produced a broken link end-to-end: `/work/${slug}` rendered as `/work//drophog`, which Next.js redirects (308) to `/work/drophog`, which then 404'd because the stored slug didn't match `drophog` in the lookup query. Fixed at the source rather than patching the URL construction: added `src/lib/slug.ts` (`normalizeSlug` — trims, strips leading/trailing slashes, lowercases, collapses non-alphanumerics to single dashes) and a `beforeValidate` hook on the `slug` field in both `case-studies.ts` and `posts.ts` (posts has the identical field with the identical misleading copy, so fixed proactively rather than waiting for the same bug to resurface there), plus rewrote the admin description to state plainly there's no leading slash. Confirmed the fix works — not just that it type-checks — via a temporary Route Handler that called `payload.update()` on the existing bad document (re-saving its own slug value through the new hook), verified the hook actually rewrote `/drophog` → `drophog`, then removed the temporary route immediately after.

**Consequences:** `/work` is no longer a placeholder — it's the second division-adjacent page (after `/studio` in ADR-046) with real content, now fully live and backed by the same content the user already entered. `/journal` remains an explicit `ComingSoon` stub; the same slug-normalization concern will apply there once it's built, and the `Posts` collection is already protected against it now. Should the user go back and re-type any slug directly into the DB or via a future script (bypassing the admin form's hook), the same leading-slash mistake is possible again since the hook only runs on Payload writes, not raw SQL — not a concern in practice since content only flows through `/admin`. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean; `/work` static with 1m revalidate, `/work/[slug]` dynamic/on-demand since no `generateStaticParams` was added — acceptable for now given low case-study volume, worth revisiting if that grows), and an actual browser click-through of the live listing → detail flow against the user's real, previously-broken case study, confirmed fixed.

---

## ADR-050: `/work` header redesigned around a supplied reference — bold headline, live category filter tabs sourced from real data

**Date:** 2026-08-07

**Context:** The user supplied a screenshot of a reference site's work-listing header (an interior-design studio's "Selected Projects." page: a large bold single-line headline, a muted subtitle, then a row of category filter tabs — ALL / RESIDENTIAL / COMMERCIAL / NEW BUILD / RENOVATION — above a project grid) with the instruction to replace `/work`'s current header with this treatment, replace the reference's own filter categories with Unda's, and keep the existing two-column grid unchanged.

**Decision:** Rewrote `/work`'s header: dropped the small "WORK" eyebrow tag (absent from the reference) in favor of a single bold headline set in the `text-hero` scale (`font-display font-bold text-hero`, the same token used for Home's hero headline, ADR-021/044) reading "Selected Work." with a plain-language subtitle beneath it — own copy, not the reference site's interior-design-specific wording. Extracted the grid and its new filter bar into `components/work/work-grid.tsx`, a client component (filtering needs `useState`, so this follows the same server-fetches/client-renders split as Home's Testimonials in ADR-048). Categories are **not** a hardcoded list matching the reference's five options — Payload's `category` field on Case Studies is freeform text (ADR-047), so the filter tabs are derived at render time from whatever distinct category values actually exist across the fetched case studies (`"All"` plus each unique value), meaning the tab row grows and shrinks automatically as the user adds case studies in new categories rather than needing a code change per category. Active-tab styling (dark text + underline vs. muted gray) matches the reference's visual treatment using existing caption/text-secondary tokens, not new ones.

**Consequences:** With only one case study currently in Payload, the filter bar currently shows just "All" + "AI Product Design" — this will visibly expand as the user adds more variety, which is the intended behavior, not a bug to fix later. If the user ever wants a fixed, curated set of categories (e.g., to control ordering or to allow filtering by a category with zero current case studies), the `category` field would need to change from freeform text to a `select` field with defined options — not done here since freeform text was the original ADR-047 design and no such requirement was raised. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean), and a live interaction test confirming the filter actually filters — clicking the real "AI Product Design" tab set `aria-current`, cleared it from "All", and reduced the visible card count to match, not just that the markup looks right.

---

## ADR-051: `/work` heading corrected back to the site's standard `text-h1` scale; hero/copy tweaks; real logo and favicon assets installed

**Date:** 2026-08-07

**Context:** Three quick follow-ups after ADR-050. First, the user pointed out the new `/work` headline "looked different from the rest" of the site — ADR-050 had given it the `text-hero`/`font-bold` treatment (matching the external reference image and Home's hero) without checking it against this app's own established sub-page convention, which every other page header (`ComingSoon`, `/studio`'s Philosophy) already uses: `font-display text-h1`, no explicit bold. Second, the user asked for copy changes reframing who Unda Studio serves — `/work`'s subtitle from "ambitious teams" to "early-stage startups and B2B companies," then pointed at Home hero's "For ambitious brands." line to apply the same shift there. Third, the user supplied two real logo assets (a 512×512 orange rounded-square "UDNA" app-icon PNG, and a 1593×512 horizontal lockup PNG combining that icon with a black "UNDA STUDIO" wordmark) with explicit instructions: the square one is the favicon, the lockup replaces the nav's plain-text wordmark. These are the first real, finished brand assets used anywhere in `apps/studio` — everything before this (the inline-SVG `unda-symbol` construction in `apps/web`, ADR-014) was built from written construction-geometry spec, not a delivered final file.

**Decision:** Changed `/work`'s `<h1>` from `font-display font-bold text-hero` back to plain `font-display text-h1` (verified via `getComputedStyle` that it now matches `/studio`'s Philosophy heading pixel-for-pixel: 40px/500 weight/General Sans on both) — site-wide typographic consistency won over strictly replicating the external reference's boldness once the two goals conflicted. Updated `/work`'s subtitle copy and Home hero's "For ambitious brands." line (`components/home/hero.tsx`) to "For startups and B2B." — shorter, matching the line's existing uppercase/tight-tracking treatment; left the hero's longer body copy ("We partner with ambitious startups and businesses...") untouched since it already named startups explicitly and wasn't flagged. For the logo assets: found both files in `~/Downloads` (`Logo (Unda Studio).png`, `Unda Studio Logo.png`) since pasted chat images aren't directly accessible as files, confirmed each against what was described before using them. Copied the square icon to `src/app/icon.png` (Next.js's file-based metadata convention — auto-generates the favicon `<link>` tag; removed the old placeholder `favicon.ico` so there's exactly one icon source, not two competing ones) and the lockup to `public/logo.png`, then replaced `nav.tsx`'s plain-text `<Link>Unda Studio</Link>` with a `next/image` render of it (`h-8 w-auto md:h-9`, `priority` since it's above-the-fold on every page). No dark-mode/light-mode logo variant was needed since the Nav's pill background is always light/frosted regardless of section (ADR-038), and the lockup's black wordmark reads correctly against it everywhere.

**Consequences:** `apps/studio` now displays real, final brand assets instead of a text-only wordmark and the generic Next.js default favicon — the first time this has been true for any part of this session's work. The Footer's separate giant bleeding "UNDA STUDIO" text wordmark (ADR-036-era) was left as-is — it's a distinct full-bleed typographic treatment, not "the nav logo," and wasn't part of this request. If a future logo revision arrives, both `src/app/icon.png` and `public/logo.png` need updating together since they're two different crops/versions of the same mark, not derived from one source file at build time. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean; `/icon.png` now appears as its own static route in the build output), and Playwright checks confirming the favicon `<link>` tag resolves to `/icon.png` and the nav visually renders the real lockup. (Nav logo height was tightened further, `h-8 md:h-9` → `h-7 md:h-8`, on direct follow-up request; hero/`work` copy also went through two more short wording passes after this ADR — none of those changed anything architectural, so not separately logged.)

---

## ADR-052: Project cards rebuilt around a supplied reference — bordered card, hover-reveal tagline, category/year row, arrow-button CTA; extracted into one shared component

**Date:** 2026-08-09

**Context:** The user supplied a two-panel reference screenshot (default state / hover state) of a portfolio card design — a white, bordered card containing the cover image; on hover the image dims under a dark scrim and a short italic one-line description fades in centered over it; below the image, a category label (left) and year (right) sit on one row, then the project title and a circular orange arrow-button CTA sit on the next — and asked for "the project card" to look like that. Two near-identical card implementations existed at this point: Home's `SelectedWork` (ADR-048, dark section) and `/work`'s `WorkGrid` (ADR-049/050, light section), each with its own inline card markup that had already drifted slightly (different heading levels, `/work`'s showed a client name the reference doesn't, Home's linked every card to bare `/work` instead of the specific case study). This was real, confirmed duplication — the condition ADR-002 sets for extracting a shared component, not a speculative one.

**Decision:** Added a `tagline` field to the Case Studies collection (`collections/case-studies.ts`) — short optional text, admin-described as the hover-reveal hook — since no existing field could honestly stand in for the reference's italic hover copy; a case study without one just skips the hover-dim-and-reveal effect entirely rather than showing an empty dark overlay. Extracted `components/case-studies/project-card.tsx`, a single server component (no client JS needed — the hover effect is pure CSS `group`/`group-hover`) now used by both `SelectedWork` and `WorkGrid`, fixing the `/work` bare-link bug as a side effect of unifying on one component. Card chrome uses only already-established tokens — `rounded-lg`/`border-border-default` (the same corner radius `PlaceholderGraphic` already used, not a new `rounded-2xl`/`rounded-3xl` invented to pixel-match the reference) and `text-text-disabled` for the muted year — rather than introducing new design-system values for a one-off. "Year" isn't a new field; it's derived from Payload's own `createdAt` timestamp (`new Date(project.createdAt).getFullYear()`), avoiding a redundant field the user would have to fill in separately when the creation date is already a reasonable proxy for "when this went up."

Verified against the user's real Drop Hog case study, not synthetic data: temporarily set its new `tagline` field through Payload's Local API (same temporary-route-then-delete pattern as ADR-048/049), used Playwright to screenshot both the default and `:hover` states, confirmed the hover scrim, italic centered tagline, and arrow-button nudge all match the reference, then reverted the tagline to blank and deleted the temporary route — leaving the user's actual data exactly as they left it, just with a new empty field available.

**Consequences:** Every project card across the site (`/work`, `/`) now renders identically, so a future card redesign only touches one file. Cards with no `tagline` filled in render with no hover effect at all (image stays static) — not a bug, just means the user needs to add taglines in `/admin` for the hover treatment to appear, same as `featured` needing to be checked for a case study to show on Home. Client name, previously shown under the title in `/work`'s old card, is no longer displayed anywhere on the card (the reference doesn't show it either) — still available on the case study's own detail page. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean), and the real-data hover-state screenshot comparison described above.

---

## ADR-053: Project card year made an explicit, editable field instead of an inferred one

**Date:** 2026-08-09

**Context:** ADR-052 derived the card's year display from Payload's own `createdAt` timestamp rather than adding a new field, reasoning that creation date was "a reasonable proxy for when this went up." The user asked directly "what about the year?" once this trade-off was explained — `createdAt` reflects when a case study was *entered into the CMS*, not when the underlying project actually shipped, so a project delivered in 2024 but only logged into Payload today would incorrectly show 2026. Asked directly whether to add a real field or keep the inferred behavior; the user chose the editable field.

**Decision:** Added a `year` field to Case Studies (`type: "number"`, `defaultValue: () => new Date().getFullYear()` so new entries start pre-filled with the current year but stay fully editable) and added it to the hand-written `CaseStudy` type in `lib/payload.ts`. `ProjectCard` now reads `project.year ?? new Date(project.createdAt).getFullYear()` — the explicit field wins when set, and ADR-052's original `createdAt`-derived logic survives as the fallback for any case study (like the user's existing Drop Hog entry) that predates this field and hasn't had a year entered yet.

**Consequences:** New case studies get a sensible default (this year) without forcing data entry, but the field is real and editable, so a card can now correctly show a different year than when it was added to the CMS. Existing case studies created before this field show the fallback-derived year until the user explicitly sets one in `/admin` — not a bug, just an unset field behaving as documented. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean; confirmed `year` column landed in the Postgres schema via direct inspection) and a live check that Drop Hog's card still renders the correct fallback year with the field left unset.

---

## ADR-054: Project card visual tweaks (padding, hover alignment, non-italic tagline); duplicate-looking filter tab traced to whitespace and fixed at the field level

**Date:** 2026-08-09

**Context:** A run of small ProjectCard adjustments following ADR-052/053: outer card padding reduced from `p-200` (16px) to `p-150` (12px); the hover tagline overlay changed from centered to top-left aligned (`items-center justify-center` → `items-start justify-start`, text-align to match); the tagline's `italic` style removed. Then, with a second real case study now in the CMS (the user's own "NeuralPath AI," added independently while this work was in progress), the user reported `/work`'s filter row showing two tabs that both read "AI Product Design."

**Decision:** The two tabs weren't a rendering bug — `psql` inspection showed the two case studies' `category` values differed only by an invisible extra space (`"AI  Product Design"`, 18 characters, vs `"AI Product Design"`, 17), so `WorkGrid`'s `Set`-based category dedup (ADR-050) correctly treated them as distinct strings that happen to render identically. This is the same class of problem as ADR-049's leading-slash slug bug — freeform text field, human data entry, silent divergence — so it got the same fix pattern: added `lib/text.ts`'s `normalizeWhitespace` (trim + collapse repeated whitespace to one space) and a `beforeValidate` hook on the Case Studies `category` field, plus an admin description note that category spelling/spacing must match exactly across case studies sharing a filter tab. Fixed the existing bad data by re-saving both real documents through Payload's own Local API (same temporary-route pattern as every prior data fix this session) rather than hand-editing the database directly, confirming the hook actually normalizes on save.

**Consequences:** `/work` now shows a single "AI Product Design" tab covering both case studies. This class of bug (near-identical freeform text values silently splitting a filter/group) can recur for any other freeform field used for grouping in the future — `category` is now protected, but a similar field added later without a normalization hook would reintroduce the same failure mode. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and a direct DOM check confirming the filter row renders exactly `["All", "AI Product Design"]`, not just that the underlying strings matched in the database.

---

## ADR-055: Case Studies' `category` converted from freeform text to a `Categories` collection + relationship, so it's a real admin dropdown instead of retyped text

**Date:** 2026-08-09

**Context:** Directly after ADR-054 fixed one instance of the freeform-`category`-text duplication bug, the user asked for the underlying cause to be removed rather than just patched again: "make the category section a dropdown on payload admin? so I just add the categories and just pick one." That phrasing — add categories once, then pick from a list — specifically rules out the simpler fix (a Payload `select` field with a hardcoded `options` array baked into `case-studies.ts`), since a hardcoded options list would still require a code change and redeploy every time the user wants a new category. A self-managed collection is what "I just add the categories" describes.

**Decision:** Added `collections/categories.ts` — a minimal collection (`name: text, required, unique`) with the same `normalizeWhitespace` `beforeValidate` hook ADR-054 introduced, so the duplication bug can't recur even at the category-management level. Changed Case Studies' `category` field from `type: "text"` to `type: "relationship", relationTo: "categories", hasMany: false` — Payload renders relationship fields as a searchable dropdown in the admin UI natively, which is exactly the requested interaction. Registered `Categories` in `payload.config.ts`. Updated the hand-written `CaseStudy`/`Category` types in `lib/payload.ts` and every consumer (`ProjectCard`, `WorkGrid`'s filter-tab derivation, the case study detail page) to read `project.category.name` instead of `project.category` directly, since the field is now a populated relationship object, not a string; `WorkGrid`'s filter state changed from comparing category strings to comparing category IDs, which also happens to make the underlying bug class structurally impossible now (two case studies can only "share" a category by literally referencing the same row).

This required a real, live schema migration on production data — not just an additive field like `tagline`/`year` earlier this session. Converting a `text` column to a relationship meant Payload's dev-mode schema push needed to drop the old `category` column and add a new `category_id` foreign key, and drizzle-kit's interactive prompt for that ("Is category_id created or renamed from another column?") requires a real TTY — a backgrounded dev server process has no terminal to answer it, and it hung indefinitely (confirmed via a bounded `curl --max-time`, not left to hang unbounded). Piping `yes "" |` into the process's stdin was tried and didn't work either — the prompt library needs raw keypress-mode input, not a line-buffered pipe. Rather than keep fighting the interactive prompt, wrote and ran the equivalent SQL by hand: created the `categories` table matching Payload's own generated-schema conventions (checked against the existing `testimonials` and `case_studies` tables for exact column/index/FK naming), inserted one `"AI Product Design"` row, added `case_studies.category_id` (FK, indexed) populated from the two existing rows' old text value, dropped the old `category` column, and added the `categories_id` column Payload's shared `payload_locked_documents_rels` table needs for its per-collection polymorphic locking feature — all inside one transaction. Verified this matched what Payload expected by restarting dev mode afterward and confirming the schema-push step completed with **no prompt** (meaning Payload's own schema diff saw nothing left to reconcile) before touching any application code's correctness.

**Consequences:** Adding a new category no longer requires a code change — the user creates it in the Categories collection in `/admin`, then picks it from a dropdown on any case study. The two existing case studies were preserved and correctly re-linked, confirmed via a SQL join before touching anything else. This is the first time this session required a manual, hand-written SQL migration instead of letting Payload's dev-mode push handle a schema change automatically — worth remembering as a pattern for any *future* field-type change (not additions) on a collection with existing data: expect an interactive drizzle-kit prompt that this environment cannot answer, and be ready to inspect the existing table structure and write the equivalent SQL by hand rather than assuming the dev server will proceed on its own. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean, no prompts) and a live check that `/work` renders both real case studies with exactly one "AI Product Design" filter tab and a correct card count.

---

## ADR-056: Home gets a one-per-session branded preloader, recreated from a user-supplied reference video; unrelated `brew install ffmpeg` briefly uninstalled the project's Node runtime

**Date:** 2026-08-10

**Context:** The user shared a screen recording of a Framer template ("Sylven") they wanted recreated: a full-screen intro sequence — wordmark above a tagline that types in character-by-character, cycles through a second phrase via a per-letter 3D flip-and-fade exit, then the whole overlay dismisses to reveal the real homepage — playing over a continuously drifting colored-gradient-mesh background. Analyzing it required extracting and reviewing individual frames (`ffmpeg`, not installed in this environment), since the motion detail — exactly how letters exited, in what order, how fast — wasn't recoverable from a single screenshot or from describing the video in words. Before building anything, the gradient background was flagged as a direct conflict with the Hero's already-locked, twice-reaffirmed black-and-white-only direction (ADR-042); asked directly, the user chose to keep black-and-white and drop the gradient, and chose to gate the preloader to once per browser session rather than every load.

**Decision:** Built `components/home/preloader.tsx`, a client component rendered first in `(frontend)/page.tsx`, ahead of `<Hero />`. A `sessionStorage` flag (`unda-studio-intro-seen`), checked and set in a mount-only effect, decides on first render whether to show anything at all — returning early skips the overlay entirely on repeat visits within a session rather than flashing and immediately hiding it. The visible sequence: the real `icon.png` + "UNDA STUDIO" wordmark (white text, since this renders on `bg-neutral-900` — the site's actual assets, not a placeholder), then a `font-mono` tagline typed in via incrementally revealing `tagline.slice(0, charCount)` on a `setTimeout` loop (40ms/char), held briefly, then — matching the reference's most distinctive detail — exits character-by-character as individual `motion.span`s animating `rotateX: -90, opacity: 0` with a 12ms stagger per letter (the `perspective` CSS property on the parent is required for the 3D rotation to read as a flip rather than a flat opacity fade). Two taglines cycle through once (not looped indefinitely) before the whole overlay fades out (`AnimatePresence` + `exit={{opacity: 0}}`) to reveal the page underneath. No attempt was made to synchronize this with Hero's own `Reveal`-based fade-ups — those complete on mount regardless, hidden behind the opaque overlay, so by the time the preloader clears the Hero is already fully settled; this is a simplification from the reference (which staggers content in *after* its loader clears) but produces a visually near-identical result with much less orchestration.

**Consequences:** First-time visitors each session see roughly a 4.5-second intro before the site is interactive; every subsequent internal load or Home revisit within that session skips it, verified by loading twice in the same browser context and confirming the overlay only mounts on the first. `ffmpeg` is now installed on this machine — a genuine dependency for any future request that requires analyzing video/motion references. Installing it via `brew install ffmpeg` triggered Homebrew's automatic `autoremove` step, which uninstalled the project's own Node.js (v23.10.0) as an "unneeded" dependency — invisible until `node --version` was checked proactively, and would have gone unnoticed until the next dev-server restart failed or silently ran on a different runtime. Homebrew no longer had that exact version available (only latest-per-major bottles are kept), so exact parity wasn't recoverable; reinstalled via `brew install node@22` (linked with `--overwrite` over leftover files from an intermediate v26 install) rather than accept whatever the default `brew install node` resolved to, on the reasoning that a stable LTS line is a safer target for a project's real dependency than either the version that happened to get uninstalled or the newest available. Re-verified the entire stack under the new runtime before trusting it — full `build`, dev server boot, and a real Playwright check that `/admin` still renders (not blank, the exact failure mode from ADR-047) — rather than assuming a Node bump this late in the session was safe. The `payload generate:importmap`/`generate:types` CLI bug from ADR-047 was retested on Node 22 and still reproduces identically, confirming it's a `tsx` module-interop issue rather than something specific to Node 23. Worth remembering generally: `brew install` of anything can silently remove other formulae via autoremove — check `node --version` (or equivalent for whatever the project depends on) after any Homebrew operation in this environment, not just after ones that look Node-related.

---

## ADR-057: Preloader switched to every-visit; swapped in the real white logo lockup at a larger size

**Date:** 2026-08-10

**Context:** Two quick follow-ups to ADR-056's preloader. First, the user asked for it to play on every visit to Home rather than once per session — reversing the choice made when this was first scoped. Second, the user supplied a genuine white-on-transparent variant of the "UNDA STUDIO" logo lockup (confirmed RGBA with real alpha transparency, same 1593×512 source as the black version already used in the Nav) to replace the icon+text combination improvised for ADR-056, and asked for both the logo and the tagline text to be sized up.

**Decision:** Removed the `sessionStorage` gate and the `null`-then-effect visibility check entirely — `visible` now initializes to `true` directly, which is also simpler than before since a static initial value needs no client/server hydration guard. Saved the new asset as `public/logo-white.png` (parallel to the existing `public/logo.png`) and swapped the preloader's icon+text markup for a single `next/image` render of it, sized up twice in direct response to feedback: first to `h-16 md:h-20` (replacing the ad-hoc icon+text sizing), then to `h-20 md:h-28` once asked to go bigger still; the tagline text stepped from `text-body` to `text-h4` in the same pass.

**Consequences:** Every Home load now plays the ~4.5s intro, including repeat visits within the same browsing session — a deliberate trade of a faster repeat-visit experience for a consistently branded one, per explicit instruction. The preloader now uses the same real, finished brand asset the Nav does rather than a code-approximated stand-in, matching the standard set in ADR-051 for using delivered final files over improvised substitutes. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and Playwright checks: three consecutive same-session reloads each showed the overlay (confirming the gate is really gone, not just visually unnoticed), and the logo's rendered bounding box was measured directly rather than assumed from the class names.

---

## ADR-058: `/services` built out — Services and Process extracted from `components/home/` for reuse; new Industries section added, scoped to already-confirmed audience framing

**Date:** 2026-08-10

**Context:** The user asked to move on to building `/services` (until now a `ComingSoon` stub) and asked what sections should go there before any code was written, per the standing "explain approach, then implement" convention. Proposed and confirmed: a header, the existing 8-service list, a new Industries section, the existing Process steps, and the standard closing CTA. For Industries specifically, asked directly whether to invent real sector/vertical claims (Fintech, Healthcare, etc.) or reuse the company-type framing already established elsewhere on the site (early-stage startups, B2B, B2C, SaaS, NGOs, small & medium businesses) — the user chose the latter, avoiding an unconfirmed claim of vertical expertise.

**Decision:** `/services` now needed the same Services accordion and Process steps already built for Home (ADR-044/043) — genuine second-consumer duplication, the condition ADR-002 sets for extraction. Moved both out of `components/home/` into a new `components/services/` directory: `services.tsx` → `services-accordion.tsx` (component renamed `Services` → `ServicesAccordion`, since "Services on the Services page" needed a less ambiguous name than "Services on the Home page" sharing the same identifier), and `process.tsx` moved as-is (already unambiguous). Home's `page.tsx` updated to import from the new paths — verified no other file referenced the old `components/home/services`/`components/home/process` paths before moving. `FinalCta` was reused directly without moving, since its copy is already generic ("Let's build something worth showing") rather than Home-specific. Built `components/services/industries.tsx` new: a numbered six-row list (matching the Hero's existing numbered-list visual convention, not a new pattern) using exactly the six confirmed audience terms, with an in-code comment explicitly warning against adding real vertical names without confirmation — the same kind of guardrail comment used elsewhere this session (ADR-019's case-study flagging, ADR-048's fabrication notes) to keep a future pass from quietly overstepping what's actually been confirmed.

**Consequences:** `/services` is no longer a placeholder — third division-adjacent page with real content after `/studio` (ADR-046) and `/work` (ADR-049). `Services`/`Process` are now genuinely shared components with two real call sites; any future edit to either affects both Home and `/services` simultaneously, which is the intended behavior (one accordion, one process description, reused). Verification initially appeared to show a broken page — only 2 of 8 services, no Industries section, an empty black Process band — which turned out to be a false alarm from the verification method, not the page: Playwright's `fullPage: true` screenshot captures content beyond the real viewport via a CDP-level trick that doesn't fire `Reveal`'s `whileInView` (`IntersectionObserver`-based) scroll animations for anything that was never actually scrolled into the browser's real viewport, so off-screen `Reveal`-wrapped content stayed at its `initial` (invisible) state in the capture despite being correctly present in the DOM (confirmed via direct locator counts before concluding anything was actually broken). Re-verified by programmatically scrolling through the full page before capturing, which is now the correct pattern for screenshotting any page with `Reveal`-wrapped sections below the fold — a blind `fullPage` screenshot on such a page will look broken even when it isn't. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean; `/services` static) plus the scroll-then-screenshot check and a direct accordion-interaction test.

---

## ADR-059: Services redesigned from an accordion into a full image+detail list, matching a supplied reference — with pricing/timeline and imagery kept honest rather than copied literally

**Date:** 2026-08-10

**Context:** The user shared a reference screenshot of a services list style: each service gets its own row with a large image, a title, capability tag pills, a description, and "Starts at $X" / "Timeline X Months" rows. Two things in the reference couldn't be adopted as-is: literal dollar figures and durations (no real pricing/timeline data exists — the accordion this replaces deliberately used "Custom pricing" instead of inventing numbers), and per-service photography (no dedicated imagery exists per service, only per-case-study). Asked directly, the user chose to keep the honest "Custom pricing"/"Custom timeline" placeholders and to use the existing `PlaceholderGraphic` component rather than either fabricating numbers or skipping images entirely.

**Decision:** Rebuilt `ServicesAccordion` as a non-interactive full list (renamed component and file to `ServicesList`/`services-list.tsx`, since "accordion" no longer described it — the `useState`/expand-collapse mechanic and its `"use client"` directive were removed entirely, since a fully-static list needs neither) matching the reference's layout: each service is a two-column row (`grid-cols-[3fr_2fr]` on large screens) — image + title + tag pills on the left, description + a `Starts at`/`Timeline` definition list on the right, separated by a full-width divider between rows. Added a `tags` array per service (e.g. Product Design: "UX/UI Design", "Prototyping", "User Testing") — short capability descriptors in the same spirit as the existing per-service description copy, not a claim requiring confirmation the way pricing or client-facing stats would be. Updated both call sites (`(frontend)/page.tsx` and `(frontend)/services/page.tsx`) to the renamed import.

**Consequences:** Services now reads as a fuller, image-anchored showcase on both Home and `/services` rather than a condensed accordion — a real trade-off worth watching: Home's page is now noticeably longer than before, since this component was deliberately kept shared (ADR-058) rather than forked into a condensed Home variant and a fuller `/services` variant. If Home's length becomes a problem, the fix is either a `compact` prop on `ServicesList` or finally forking the two, not reverting this change. All 8 services still show "Custom pricing"/"Custom timeline" — genuinely ready to swap for real figures the moment the user provides them, same pattern as every other honest-placeholder decision this session. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and a full scroll-then-screenshot pass (per the verification-method lesson from ADR-058) plus a close-up screenshot of a single row confirming the exact layout — title, image, tags, description, and the two definition-list rows — matches the reference's structure.

(Immediately after, the row layout was corrected from two columns to three — title, image+tags, and description+pricing each in their own column, matching the reference more precisely than the initial two-column pass had. No separate ADR for that follow-up; same decision, closer execution.)

---

## ADR-060: Home and `/services` split back onto separate Services components — the "watch for this" risk in ADR-059 materialized within the same session

**Date:** 2026-08-10

**Context:** The length trade-off flagged as a risk at the end of ADR-059 ("If Home's length becomes a problem...") came up almost immediately: the user asked for Home's "What we do" section to go back to something like the pre-ADR-059 simplicity, further reduced to just a 4-item numbered list — "01 Product Design / 02 Brand Identity / 03 Design Systems / 04 AI Product Design," the same four items and order already used in Hero's own numbered list — while confirming `/services` should keep the full 8-service detailed layout from ADR-059 unchanged.

**Decision:** Rather than add a `compact` prop to `ServicesList` (the other option ADR-059 named), forked it: created `components/home/services-summary.tsx`, a new component reusing the exact numbered-list visual pattern already established by Hero's list and `Industries` (`font-mono` index, `border-t`/`border-b` divider rows) — no image, no tags, no pricing, just the same "What we do" eyebrow/headline/subtitle intro from before, followed by 4 items instead of 8. `ServicesList` (the full detailed version) is untouched and still lives at `components/services/services-list.tsx`, used only by `/services` now. Home's `page.tsx` swapped `ServicesList` for `ServicesSummary`.

**Consequences:** Home is short again; `/services` keeps the full detail. This reverses part of ADR-058's reuse decision (Services is no longer literally the same component on both pages), but not the part that still holds — `Process` remains genuinely shared between the two pages, only `Services` diverged, because that's specifically what turned out to need different depth per page. Two Services-shaped components now exist (`ServicesSummary`, `ServicesList`) with independent content arrays that list the same underlying services at different levels of detail — if a service is renamed or added, both files need updating, which is the accepted cost of the fork now that the two pages have genuinely different needs rather than a hypothetical one. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and a live check — waited out the every-visit preloader (ADR-057) before scrolling to confirm Home shows exactly the 4-item list with no pricing text anywhere on the page, while `/services` was left untouched and reverified separately.

---

## ADR-061: Home's flat 4-item list replaced with an accordion (again) — content deduplicated into `lib/services-data.ts` to stop the fork from drifting

**Date:** 2026-08-10

**Context:** Immediately after ADR-060 flattened Home's services section into a plain numbered list, the user supplied a new reference screenshot: a bracket-numbered accordion (`[01] Web Design & Development`, red +/× toggle, one item open at a time) where the open item shows a capability checklist (triangle-bullet rows: "UI/UX Design," "Webflow, Shopify, Framer," etc.) beside a bold description, plus two small project photos at the bottom — and asked for this treatment on Home's "What we do" section with the photos removed. This is a third distinct shape for the same section within one session (accordion-with-pricing → flat list → accordion-without-pricing), which made the drift risk named in ADR-060's consequences ("if a service is renamed or added, both files need updating") concrete enough to fix rather than keep accepting.

**Decision:** Extracted the services content itself — title, description, tags — out of both `ServicesSummary` and `ServicesList` into a new `lib/services-data.ts`: `SERVICES` (all 8, `as const`) and `HOME_SERVICE_TITLES` (the 4 shown on Home, as an explicit name list rather than `SERVICES.slice(0, 4)` — so reordering or inserting into `SERVICES` can't silently change which services Home surfaces). Rewrote `ServicesSummary` as a client-component accordion mirroring the reference: `[0N]` bracket index in the same bold display type as the title (not the mono bracket style `Process`/`Industries` use elsewhere — this reference specifically wants the number matching the heading's weight), Lucide `Plus`/`X` (not `Minus` — the reference's collapse icon is an X, not a dash) in `text-brand-accent`, and an expanded panel with the existing `tags` rendered as a bulleted list (`TriangleAccent` markers, reusing the same triangle already established for `Process`/`Industries` bracket labels rather than inventing a new bullet glyph) instead of ADR-059's pill badges, paired with the description in bold. No image slot at all, per the explicit "remove this image" instruction — not hidden via a prop, not present in the markup. `ServicesList` (`/services`) now imports the same `SERVICES` array instead of its own copy but is otherwise unchanged — full 8 items, images, pricing/timeline rows all still there.

**Consequences:** Editing a service's title, description, or tags now only requires touching `lib/services-data.ts` — both Home's accordion and `/services`'s full list pick it up automatically, closing the drift risk ADR-060 accepted. `HOME_SERVICE_TITLES` is the one place that still needs manual updating if the featured-on-Home subset should ever change. Home's accordion, unlike `/services`'s list, never shows pricing or timeline — a deliberate scope difference (Home stays a teaser, `/services` is where someone goes to see the real breakdown), not an oversight. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and Playwright checks: confirmed via `aria-expanded` that opening one item closes whichever was open before (true single-open accordion, not independent toggles), and confirmed the second item's expanded content (tags + description) matches `lib/services-data.ts` exactly rather than assuming from the markup.

---

## ADR-062: Real bug in ADR-061's accordion — `pb-500` isn't a token in this scale, so Tailwind silently fell back to its own default (2000px)

**Date:** 2026-08-10

**Context:** The user reported the expanded accordion panel from ADR-061 looked "too bold" and asked "what is this spacing," attaching screenshots showing an enormous blank gap between an open panel's content and the next collapsed row below it.

**Decision:** Investigated directly rather than guessing from the screenshot — measured the live panel's bounding box and computed styles via Playwright before changing anything. `getComputedStyle` showed `padding-bottom: 2000px` on the panel, traced to `pb-500` in the panel's className. This project's spacing scale (`globals.css`) is a named, non-linear token set — `50, 100, 150, 200, 300, 400, 600, 800, 1200, 1600` — with **no `500`**. Tailwind doesn't error on an unrecognized scale key; it falls back to its own built-in default numeric spacing scale, where every step is `0.25rem`, so `pb-500` silently resolved to `500 × 0.25rem = 2000px` instead of failing to build or visibly warning. Fixed by changing it to `pb-400` (a real token, 2rem/32px). Grepped the rest of the codebase for the same pattern (`p|m|gap|inset|top|bottom|left|right` followed by a 2–4 digit number not in the valid token list) and found no other instances — this was isolated to the one line introduced in ADR-061. Separately, changed the accordion's `[0N]`/title text from `font-bold` to `font-semibold`, per the same feedback.

**Consequences:** This is a real, previously-undetected class of bug worth remembering generally: because this project's spacing/type scale uses gaps in its numbering (no `500`, no `700`, etc. — see `--space-*` in `globals.css`) rather than a dense linear scale, any Tailwind utility built from a number *outside* the registered token set won't error — it'll silently render using Tailwind's own unrelated default scale, which can produce wildly wrong values (2000px from a typo that looks like a reasonable spacing step). The fix in the moment was straightforward, but the systemic protection would be a lint rule or scale audit catching non-token numeric utility values — not implemented here, just noted, since one clean grep found no other live instances to justify the tooling investment right now. Verified via direct `getComputedStyle`/`boundingBox` measurement showing the panel height dropped from 2166.5px to 198.5px, and via a correctly-scoped selector (the earlier check grabbed a wrapper `<span>` and mis-reported font-weight 500 for an element that actually computed to 600 — re-verified with the specific title element before concluding the semibold change had landed) — a reminder to double-check *which* element a broad text-matching selector actually returns before trusting its computed style.

---

## ADR-063: `/services` header flipped to black-and-white; stray native tooltips removed from every decorative SVG, not just the one the user spotted

**Date:** 2026-08-10

**Context:** Two small fixes. First, the user asked for `/services`'s header section to match the black-and-white treatment already established for Home's Hero (ADR-042) and `/work`'s original design intent — it had stayed on the default light background since ADR-058 first built the page. Second, while sharing a screenshot of a `PlaceholderGraphic` reminding "these are placeholders I would be adding the images later" (a reminder, not a change request — consistent with the standing no-fabrication approach already in place), the screenshot incidentally showed a native browser tooltip reading "Decorative geometric graphic" hovering over the placeholder, surfacing a real, previously unnoticed UX rough edge.

**Decision:** Changed `/services`'s header `<section>` to `bg-neutral-900` with `text-neutral-0`/`text-neutral-400` in place of the light-mode text tokens — the same substitution pattern used for every other dark-section flip this session. For the tooltip: traced it to an SVG `<title>` element inside `PlaceholderGraphic` — browsers render a native hover tooltip for any `<title>` inside an `<svg>` regardless of `aria-hidden` on an ancestor, since the tooltip is a rendering behavior tied to the SVG spec, not an accessibility-tree behavior. Removed it (and moved `aria-hidden="true"` onto the `<svg>` itself, where `role="presentation"` already lived, rather than relying solely on the wrapping `<div>`'s `aria-hidden`). Since this was clearly a copy-pasted pattern, checked for the same mistake elsewhere rather than fixing only the one instance reported: `TriangleAccent`, `GeneratedAvatar`, and `GenerativePattern` all had the identical issue (`<title>Decorative accent</title>`, `<title>Placeholder avatar</title>`, `<title>Decorative pattern</title>`), each removed the same way.

**Consequences:** Hovering over any decorative graphic on the site — case-study placeholders, the star-rating avatars, the process-step patterns, the small triangle accents — no longer produces a browser tooltip that has nothing useful to say to a visitor. None of these elements lost accessibility coverage: they were already `aria-hidden`/`role="presentation"`, correctly excluded from the accessibility tree before and after: the `<title>` elements were serving no screen-reader purpose, only triggering an unwanted sighted-mouse-user tooltip. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types` (Biome's `a11y` ruleset, already enabled, raised no new issues from removing the titles — confirming they weren't required for compliance), `pnpm build` (clean), and a grep confirming zero `<title>` elements remain across `components/ui/`.

---

## ADR-064: Em dashes removed from every visible page — page-title separator changed from `—` to `|`; code comments left untouched

**Date:** 2026-08-12

**Context:** The user asked to "remove any em-dashes across the design." Read narrowly as the visible product — actual rendered site copy — rather than this decisions log or code comments, which use em dashes throughout as part of the writing style established since ADR-001 and aren't part of "the design" in the visual/product sense.

**Decision:** Grepped every `.tsx`/`.ts` file in `apps/studio/src` for the em dash character, then manually classified each hit as either rendered content (JSX text, `metadata.title`/`description` strings, Payload collection `admin.description` field text — the last of these renders as help text inside `/admin`, still real UI a human reads, not source-level commentary) or a code comment (`//`, `/* */`, JSDoc `/** */`) — leaving every comment untouched and fixing only the former. Replaced each em dash with whatever plain punctuation the sentence actually called for: a comma for a parenthetical aside, a colon for a list lead-in, a period splitting into two sentences, or — for the page-title convention specifically (`"X — Unda Studio"`, used identically across all 8 pages including the dynamic case-study title template) — a pipe (`"X | Unda Studio"`), a common, unambiguous title separator that isn't an em dash. The testimonial attribution's leading `"— {author}"` had the dash dropped entirely rather than replaced, since no punctuation was needed there once removed.

**Consequences:** No visible text anywhere on the live site uses an em dash anymore — verified per-page, not just per-file, since a source-level grep can't confirm what a template literal or conditional actually renders. `DECISIONS.md` and inline code comments were deliberately left as-is; if the user meant those too, that's a distinct, much larger follow-up (this file alone runs to 64 ADRs of em-dash-heavy prose) worth confirming explicitly before touching, not assumed from "the design." Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and a Playwright pass checking `document.title` and full page `innerText` against the em dash character across all 8 static pages plus a live case-study detail page, rather than trusting the source-level grep alone.

---

## ADR-065: Industries section redesigned from plain numbered rows into icon-badged cards

**Date:** 2026-08-12

**Context:** The user asked to redesign the Industries section (`/services`) without an initial reference, so the direction was clarified first rather than guessed: keep the numbered-list format's underlying content, but give it more visual weight via icons, cards, or color per item instead of plain text-and-divider rows.

**Decision:** Converted each `INDUSTRIES` entry from a bare string to `{ label, icon }`, pairing a semantically fitting Lucide icon per company type (`Rocket` for early-stage startups, `Building2` for B2B, `Users` for B2C, `Cloud` for SaaS, `HeartHandshake` for NGOs & nonprofits, `Store` for small & medium businesses). Replaced the `border-t`/`border-b` divider-row list with a 6-card grid, each card matching the bordered/rounded/hover-shadow convention already established by `ProjectCard` (`rounded-lg border border-border-default bg-bg-default`, `hover:shadow-md`) rather than inventing new card chrome — an icon sits in a soft `brand-accent/10`-tinted circle badge, the numbered index moved to the opposite corner of the same row instead of leading the label, and the label itself sits below as the card's primary text.

**Consequences:** The section now reads as a proper feature-card grid instead of a plain list, consistent with the visual register the rest of `/services` already uses (bordered cards, icon accents) rather than introducing a third distinct list style on the same page. The underlying six company-type terms are unchanged — this was a presentation-only redesign, not a content change, so the standing "no fabricated vertical claims" guardrail comment in the file still applies as-is. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and a live screenshot confirming all six icons render correctly paired with their labels and the hover/shadow treatment matches the rest of the page.

---

## ADR-066: `/studio` rebuilt into a 7-section page; two sections held back pending real founder/tool data, not fabricated

**Date:** 2026-08-12

**Context:** The user requested a full rebuild of `/studio` (previously just the `Philosophy` component) into a 7-section page — Our Mission, About Us, Our Values, Meet the Founder, Daily Stack, Philosophy, FAQ — alternating black/white backgrounds, with two supplied reference screenshots for About Us and Our Values specifically. Three of the seven sections carried real fabrication risk severe enough to stop and ask before writing anything: "Meet the founder" (the reference shows a real photo, name, bio, and named-company work history — Google, Shopify, Meta), "Daily Stack" (needs the actual tools used, not a guessed list presented as fact), and FAQ (no content was supplied at all). Asked directly: the user chose to supply real founder details and real tools themselves (in a follow-up message, not yet received at the time of this build), and asked for FAQ to be drafted with honest, non-committal placeholder content they'd edit.

**Decision:** Built the five sections that don't depend on unconfirmed facts now, leaving Founder and Daily Stack out of the page entirely (not stubbed with fake content, not rendered as empty placeholders — simply not in the composition yet) until the real details arrive. `Mission` (`components/studio/mission.tsx`) and `About` (`components/studio/about.tsx`) both use a centered, two-tone headline treatment (full-opacity lead clause, muted-secondary trailing clause) adapted from the About Us reference's black/gray two-tone technique, but without the reference's script "(hello)" flourish — General Sans/Geist Mono are the only typefaces in this system, so a decorative script font was never an option; the eyebrow does the same "chat opener" job in the existing caption style instead. `About`'s capability pills reuse the *existing* 8-service list from `lib/services-data.ts` (6 of them) rather than inventing new claimed capabilities, styled as dark pills floating on the white section for contrast, echoing the reference's dark-pill-on-light-background pattern. `Values` (`components/studio/values.tsx`) absorbs and expands what used to be `Philosophy`'s inline 3-item `PRINCIPLES` grid into its own full black section, restyled as the reference's large-type numbered-row list (`[0N]` mono index, `text-h1` title, description at the row's far edge) instead of the old 3-column card grid; kept the original 3 principles' copy verbatim and added a 4th ("Speed Without Shortcuts") to match the reference's 4-item structure and to echo Hero's existing "same rigor on day one thousand as day one" line rather than introducing an unrelated new idea. `Philosophy` (`components/studio/philosophy.tsx`) was trimmed to exactly the quote and paragraph the user said to keep, with the principles grid removed (superseded by `Values`) and its special first-section top padding removed now that it's mid-page, not the page's opening section. `Faq` (`components/studio/faq.tsx`) is a new accordion, single-open like `ServicesSummary`/`WorkGrid`'s filters, with five drafted questions and answers that state no specific price, timeline number, or policy as settled fact ("timelines vary by scope," "we'll give you a clear estimate," "many of our engagements continue past launch") — explicitly commented in-file as draft copy meant to be edited once real answers are confirmed.

A real bug recurred during this build: `Values`' `<ul>` used `mt-500`, hitting the exact same invalid-spacing-token failure mode as ADR-062 (this project's scale has no `500`; Tailwind silently fell back to its own default scale, rendering `500 × 0.25rem = 2000px` instead of erroring) — caught the same way, by measuring the live element's `getComputedStyle` after the page visually showed a large unexplained gap, not by reading the source. Fixed to `mt-600`. Reswept the entire codebase for the same pattern afterward; no other instances found.

**Consequences:** `/studio` is now a real, mostly-complete page — five of seven planned sections live, verified end-to-end (scroll-triggered reveals, FAQ accordion single-open behavior, correct black/white alternation). Founder and Daily Stack remain explicitly deferred, not silently dropped — `(frontend)/studio/page.tsx` has an in-file comment stating why they're missing, so a future session doesn't mistake the omission for the page being "done." This is the second time in this session an invalid Tailwind spacing utility silently produced a 2000px value instead of erroring (ADR-062, ADR-066) — still not worth a dedicated lint rule for two occurrences across two files, but a third recurrence would justify one. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and a full scroll-then-screenshot pass of the entire page plus a live accordion-interaction check on the FAQ.

---

## ADR-067: `/studio`'s two deferred sections completed with real, confirmed content — a real headshot, real bio, real tool list; `@icons-pack/react-simple-icons` added for legitimate brand marks where one exists

**Date:** 2026-08-12

**Context:** Direct follow-up to ADR-066. The user supplied everything that ADR-066 was waiting on: a real headshot (`Ademola Headshot.png`, 2400×2400, found in Downloads), a real bio ("Ademola Oshingbesan is a product designer, software engineer, and entrepreneur..."), an explicit instruction to leave out any work-history/timeline (the reference's "Founder at Agero / Brand Designer at Google" style list specifically excluded), and a real 12-tool daily stack (Figma, FigJam, Adobe Photoshop, Illustrator, After Effects, Premiere Pro, Canva, Framer, Webflow, Claude, Midjourney, Blender) with a specific layout instruction — 6 across, 2 rows.

**Decision:** Copied the real photo to `public/founder.png` and built `components/studio/founder.tsx`: eyebrow, real name as the section heading, a two-column layout (photo left, bio right) — deliberately without the reference's giant gradient-faded background-text treatment, badge overlay, social icons, or timeline, since none of those had real content behind them and inventing filler for a real, named person is a different order of problem than inventing filler for generic marketing copy. For the tool stack, checked whether real brand marks could legitimately be shown before either fabricating logo-like shapes or hotlinking official assets — installed `@icons-pack/react-simple-icons` (MIT wrapper around the CC0-licensed Simple Icons set), the standard, properly-licensed package for exactly this "tools we use" use case. Queried its actual export list before assuming coverage: only 5 of the 12 tools have a mark in this set (Figma, Framer, Webflow, Claude, Blender) — Adobe's four apps, Canva, and Midjourney are absent, almost certainly removed historically over trademark enforcement rather than an oversight in the package. Rather than mixing real marks with fabricated logo-alikes for the other seven, used a plain two-letter text monogram (`Ps`, `Ai`, `Ae`, `Pr`, `Cv`, `Mj`) for those — Adobe's own app-abbreviation convention, but rendered in the site's own neutral styling rather than each brand's specific colors/shapes, so nothing reads as an unlicensed logo reproduction. `FigJam` reuses the `SiFigma` mark rather than a monogram, since FigJam genuinely is a Figma product — a closer, more honest fit than an arbitrary fallback. Laid out as a `grid-cols-3 sm:grid-cols-6` grid so mobile shows 3-per-row and desktop matches the requested 6-across/2-down.

**Consequences:** `/studio` is now complete, all 7 planned sections live with real, confirmed content, no placeholders left in the page composition. The founder's identity, photo, and bio are now public and load-bearing — any future change to who leads Unda Studio or how their background is described needs to come through the same confirm-before-publish process this section itself followed, not a quiet edit. `@icons-pack/react-simple-icons` is a new dependency (`^13.13.0`) available for any future "tools/tech we use" style content elsewhere in the codebase — its actual coverage gaps (no Adobe Creative Cloud apps, no Canva, no Midjourney as of this version) are worth remembering before assuming a given brand's mark is available. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and full-resolution screenshots of both sections confirming the real photo/bio render correctly with no timeline, and all 12 stack items — five real marks, six monograms, one shared mark — display correctly in the requested 6×2 layout.

---

## ADR-068: Duplicate `sharp`/`libvips` native binaries resolved to one version

**Date:** 2026-08-12

**Context:** The user asked "what is happening?" with no specific symptom named — a general status check, not a bug report. Investigating rather than guessing surfaced a real issue that hadn't been visible before: the dev server log carried a macOS Objective-C runtime warning, `Class GNotificationCenterDelegate is implemented in both [libvips 1.3.2] and [libvips 1.2.4] ... This may cause spurious casting failures and mysterious crashes.` `pnpm why sharp` explained it — `apps/studio/package.json` pinned its own direct `sharp` dependency (`^0.35.3`, added in ADR-047 for Payload's `buildConfig({ sharp })` option) while Next.js's own peer-dependency chain (`@payloadcms/next` → `next` → `sharp`) independently resolved to `sharp@0.34.5`. Two different `sharp` versions in the same install meant two different compiled `libvips` native binaries both loading into the same Node process.

**Decision:** Pinned `sharp` to the exact version Next.js's own peer resolution already wanted (`0.34.5`, changed from the `^0.35.3` range) rather than the reverse (forcing Next's resolution up to 0.35.x, which isn't this project's call to make against a framework peer dependency). Reinstalled and confirmed via `pnpm why sharp` that every consumer in the tree — `@payloadcms/next`, `@payloadcms/richtext-lexical`, `@payloadcms/storage-vercel-blob`, `next` itself, and the app's own direct dependency — now resolves to the identical single version.

**Consequences:** The duplicate-library warning is gone from a clean dev server boot, confirmed by grepping the log after restart. Re-verified Payload's image pipeline still works under the pinned version rather than assuming a patch-version downgrade was safe: `/work` was checked live, confirming all three images (including two real Payload-processed uploads) loaded with correct non-zero natural dimensions through `next/image`, not just that the page returned 200. This was a latent issue since ADR-047 first added the direct `sharp` dependency — it just hadn't been asked about directly until now. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean, no warnings in build output either) after the version pin.

---

## ADR-069: Founder section rebalanced, then given a real work-history timeline once the user supplied one

**Date:** 2026-08-12

**Context:** Two follow-ups to ADR-067's Founder section. First, the user flagged the layout directly ("this is not looking good") — a tall `aspect-[4/5]` photo next to a much shorter bio paragraph left a large dead-space gap below the text, and the photo's flat gray studio backdrop read as an unstyled cutout rather than an intentional design choice. Second, immediately after that fix, the user supplied real work history — five actual roles, companies, and date ranges — reversing ADR-066/067's "no timeline" decision now that there was real data behind it, and pointed at the same founder reference image again, this time specifically for its timeline row format (title, company, right-aligned date range, divider between rows).

**Decision:** Rebalanced the layout: photo changed from `aspect-[4/5]` to `aspect-square` with a `border-border-default` frame (giving the flat backdrop an intentional edge rather than looking like a loose cutout), grid columns changed from `1fr_1.5fr` to `2fr_3fr` so the photo takes proportionally less width, and added a "Founder, Unda Studio" label above the bio to give the text column more visual weight. Added the real `EXPERIENCE` array verbatim as given — five entries, in the exact order supplied rather than re-sorted into strict reverse-chronological order (the user's ordering isn't this component's call to "correct," even though two entries' date ranges overlap) — rendered as a divided list below the bio: role as primary text, company as secondary text beneath it, date range right-aligned, matching the reference's row structure. The one entry given without an end date ("Visual & Brand Designer, Prince Graphic Concepts") renders with no date at all rather than a fabricated one. Explicitly did not add the reference's gradient-duotone photo treatment, "Award Winning Designer" badge, or social icons — none of those have real data behind them, and the user's two requests here were specifically about layout balance and the timeline, not a request to add the rest of the reference's decorative elements.

**Consequences:** The Founder section is now complete and accurate to what the user provided, including an honest gap (no end date shown for one role) rather than a guessed one. This is the second time this exact reference image has been revisited for the same section (ADR-066 for the initial adapted layout, ADR-067 for photo/bio-only, now this ADR for the timeline) — each pass added exactly the piece that had real data behind it at the time, never more. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and full-resolution screenshots confirming the photo/text balance reads correctly and all five timeline entries render with the exact roles, companies, and dates supplied.

---

## ADR-070: Founder timeline decoupled from the photo column; giant gradient-fade "Meet Ademola" headline added

**Date:** 2026-08-12

**Context:** Two more Founder-section follow-ups. First, the user flagged the layout again: pairing a short square photo with a five-row timeline in the same column meant the timeline ran well past the photo's bottom edge, leaving the photo looking "orphaned" once scrolled past — a real layout problem, not a taste disagreement. Second, the user asked directly why the reference's giant, gradient-faded "Meet [Name]" background headline — present in every version of this reference image shown across ADR-066/067/069 — had never actually been built, after two prior passes deliberately left it out on the reasoning that it was decorative and unconfirmed-content-adjacent.

**Decision:** Moved the `EXPERIENCE` list out of the `2fr_3fr` grid entirely into its own full-width block below the photo/bio row (matching `Values`'/`Industries`' established divided-list pattern), so the timeline's length no longer has to visually reconcile with the photo's height — the photo/bio pair now stays compact and centered together regardless of how long the experience list grows. Separately, added the gradient-fade headline this time, correcting the earlier reasoning: the giant-text technique is a pure typographic effect (a `bg-clip-text` gradient fading `from-neutral-300 to-transparent`) — it carries no factual claim the way the reference's badge/timeline/social-icons did, so withholding it was overcautious, not a defensible application of the no-fabrication standard. Implemented as the section's actual `<h2>` (not `aria-hidden` decoration — the heading's real text is "Meet Ademola," semantically sound, just visually faded) at `text-[clamp(3rem,9vw,7rem)]`, with the photo/bio row pulled up underneath it via a negative top margin so the photo overlaps the fade's bottom edge, echoing the reference's overlap without copying its specific colors.

**Consequences:** The Founder section now has the reference's signature visual moment (the faded name-reveal headline) while staying honest — the photo/bio pairing is stable regardless of experience-list length, and the previously-declined visual flourish is now justified by a clearer read of what it actually was (a typography technique, not a claim). Worth remembering for future "why didn't you do X from the reference" questions: the right test is whether X asserts something unconfirmed, not whether X is elaborate or decorative — this session had at times conflated the two. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (clean) and a live screenshot confirming the gradient headline renders and overlaps the photo as intended.

---

## ADR-071: "Meet Ademola" headline stretched to full container width; "Founder, Unda Studio" label made a real subheading

**Date:** 2026-08-12

**Context:** ADR-070's gradient headline was sized with `text-[clamp(3rem,9vw,7rem)]` alone — clamp-based font sizing approximates a width but can't guarantee the text's actual rendered width matches the container edge-to-edge, the way the reference's "Meet Finton" spans the entire frame. The user confirmed this by name: the headline needed to reach "from left to righh of the whole frame," not just sit narrower above the photo. Separately, the "Founder, Unda Studio" label was still styled as a small `text-body-sm text-text-disabled uppercase` caption, and the user asked for it to be "semi bold, big and black" instead.

**Decision:** Reused `components/motion/stretch-text.tsx` — the same `ResizeObserver`-driven `scaleX` component already proven on Hero's "Creative Studio" headline — for "Meet Ademola," wrapping it in `<h2>` (`<h2><StretchText>...</StretchText></h2>`, same nesting pattern as Hero's `<h1>`) so the real heading text stays semantic while the visual span is forced to exactly match the container width regardless of viewport or string length; kept the same `clamp` font-size and gradient/`bg-clip-text` classes as the vertical/color baseline that `StretchText` scales horizontally. Changed the "Founder, Unda Studio" label from `text-body-sm text-text-disabled uppercase tracking-wide` to `font-display font-semibold text-h4 text-text-primary` — large, semibold, near-black, matching the weight the user asked for.

**Consequences:** The headline now touches both edges of the container exactly, matching the reference, and updates correctly on resize since `StretchText` re-measures via `ResizeObserver`. Confirms `StretchText` as the standing house pattern for "headline must span full frame width" requests anywhere else in the site, not a one-off Hero trick. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot at 1440px confirming both the full-width stretch and the bolded label render correctly.

---

## ADR-072: `StretchText` gained a `fit="size"` mode — font-size scaling instead of `scaleX` transform, to stop distorting letterforms

**Date:** 2026-08-12

**Context:** ADR-071 shipped, but the user immediately flagged the actual visual result: "don't Stretch the Text just make sure it's goes from left to right just like the 'Creative Studio' text on the home page." `StretchText`'s only mode applies a horizontal `scaleX` transform to fill the container — for "Creative Studio" (15 characters) the needed ratio stays close to 1, so it reads as normal type; for "Meet Ademola" (12 characters, same container width class), the ratio needed was large enough that the transform visibly fattened every letterform, which is what "stretch" was objecting to, not the full-width span itself.

Building a font-size-based alternative (grow the font-size until natural text width equals the container width, so glyphs keep their true proportions) surfaced a real React/DOM bug during verification, not just a styling miss: the first implementation measured the "natural" font-size by imperatively clearing the *visible* span's own inline `style.fontSize`, reading its computed value, then calling `setFontSize()`. On the second measurement pass (`document.fonts.ready` firing, or `ResizeObserver`'s mandatory initial callback), the same clear-and-recompute produced the identical numeric result as the previous render — and because React bails out of re-rendering when new state equals current state, it never re-applied the inline style to the DOM. The just-cleared style stayed cleared, silently reverting the headline to its unscaled 112px clamp size with no console error. Caught by comparing computed-style values via a Playwright `page.evaluate()` against what the component's own logic should have produced, not by re-reading the source.

**Decision:** Added a `fit` prop to `StretchText` (`"scale"` default, unchanged — still what Hero's "Creative Studio" uses; `"size"`, new). `fit="size"` measures off a separate, permanently-hidden probe `<span>` (same `className`, so it inherits the same `clamp()` font-size, `visibility: hidden`, `position: absolute`, never touched after mount) instead of the visible text node — so measurement never fights with React's own state-diffing, and the visible span's font-size only ever moves forward as a real state update. Founder's "Meet Ademola" now uses `fit="size"`.

**Consequences:** `StretchText` now has two independent, tested-separately code paths — verify both if the component changes again. `fit="size"` is the correct choice specifically for short strings in a wide container, where `scale`'s ratio would be furthest from 1; `scale` remains right for longer strings (Hero) where the ratio stays close to 1 and the distortion is imperceptible. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean), and — critically, since the prior bug produced no build/type error and no visible console warning — a `page.evaluate()` check against live computed styles confirming the visible span's font-size and scroll width actually match the container, not just that the screenshot looked plausible.

---

## ADR-073: Founder section trimmed — "The founder" eyebrow and the Experience timeline removed, "Founder" recolored to brand orange

**Date:** 2026-08-12

**Context:** The user asked to remove two elements from the Founder section outright: the "The founder" eyebrow label above the gradient headline, and the entire Experience timeline block (added in ADR-069, decoupled into its own full-width section in ADR-070) — a scope reduction, not a layout complaint. Separately, in the same message, asked for the word "Founder" specifically (within "Founder, Unda Studio") to change to the brand's orange accent color, leaving ", Unda Studio" as-is.

**Decision:** Removed the `<span>The founder</span>` eyebrow entirely from the `Reveal` wrapping the headline. Removed the whole `mt-800` Experience block — the "Experience" eyebrow, the divided `<ul>`, and the now-unused `EXPERIENCE` data array — rather than leaving orphaned data in the file. Split the "Founder, Unda Studio" label into two spans: `<span className="text-brand-accent-text">Founder</span>` followed by plain `, Unda Studio` text, both still inside the existing `font-display font-semibold text-h4` wrapper from ADR-071 so the size/weight is unchanged, only "Founder"'s color shifts to `text-brand-accent-text` (the same orange token used for eyebrows elsewhere, e.g. Hero's "Now accepting new projects" dot).

**Consequences:** The Founder section is now shorter — photo, bio, and the colored label only, no eyebrow, no timeline. The five real work-history entries the user supplied in ADR-069 are no longer displayed anywhere on the site; they weren't deleted from any other source of truth (they only ever lived in this component), so if the user wants them back, they'd need to be re-supplied or this ADR reverted rather than recovered from data still on disk. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot confirming the eyebrow and timeline are gone and "Founder" renders in the brand orange against the black ", Unda Studio."

---

## ADR-074: FAQ section switched from the dark `neutral-900` treatment to white, matching the page's alternating pattern

**Date:** 2026-08-12

**Context:** `/studio`'s original spec (start of this build) called for 7 sections alternating black/white backgrounds. FAQ had been built dark (`bg-neutral-900`, `neutral-0`/`neutral-400`/`neutral-700`/`neutral-800` text and border tokens), immediately after Daily Stack — which is also dark — leaving two consecutive black sections at the bottom of the page rather than an alternating rhythm. The user asked directly for FAQ to be white.

**Decision:** Swapped every dark-section token in `components/studio/faq.tsx` for the light-section equivalents already used elsewhere on the page (`Philosophy`, `About`, `Values`): `bg-neutral-900` background removed entirely (defaults to the page's light background), border tokens `border-neutral-700` → `border-border-default`, heading/body text `text-neutral-0`/`text-neutral-400` → `text-text-primary`/`text-text-secondary`, the eyebrow `text-brand-accent` → `text-brand-accent-text` (the correct token for text-on-light, matching Philosophy/Values/Founder's eyebrows), and the toggle icon's circle background `bg-neutral-800` → `bg-neutral-100` with `text-neutral-0` → `text-text-primary` so the plus/minus icon stays visible against a light circle.

**Consequences:** FAQ now closes the page on a light section, restoring the black/white alternation (Mission white → About white → Values white → Founder white → Daily Stack black → Philosophy white → FAQ white — Daily Stack remains the page's one dark section by design, not strictly alternating every section, but FAQ no longer doubles up on black immediately after it). Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot confirming legible contrast on the new light background, including the open/closed accordion states.

---

## ADR-075: About restructured into an asymmetric two-column layout to stop duplicating Mission's look

**Date:** 2026-08-12

**Context:** The user flagged that "Our Mission" and "About Us" — the page's first two sections, directly adjacent — "look almost the same." Comparing them confirmed it: both used the identical layout (centered eyebrow, giant centered `text-h1` statement, a muted-color trailing clause inline in the same sentence), differing only by background color (Mission dark, About light) and About's added capability-pill row. Screenshotted together, the two read as one section repeated rather than two distinct ideas.

**Decision:** Restructured `components/studio/about.tsx` into a left-aligned, two-column editorial layout (`grid-cols-[3fr_2fr]`, matching the asymmetric-grid pattern already established by `Founder`) instead of About's own centered pull-quote: the eyebrow and headline sit left-aligned in the wider column, a separate short supporting sentence sits right-aligned in the narrower column (no longer one continuous inline sentence split by color, which was the strongest visual echo of Mission's treatment), and the capability pills below are now left-aligned rather than centered. Mission was left untouched — it now uniquely owns the large centered pull-quote treatment on this page, reading as a singular manifesto statement rather than a template repeated by the section after it.

**Consequences:** Every section on `/studio` now has a visually distinct signature layout: Mission (centered pull-quote), About (asymmetric editorial masthead + pills), Values (numbered divided list), Founder (photo/bio + gradient headline), Daily Stack (icon grid), Philosophy (left-aligned blockquote with giant quotation mark), FAQ (accordion). Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot of both sections together confirming they no longer read as duplicates.

---

## ADR-076: About's supporting line moved from bottom-aligned to top-aligned

**Date:** 2026-08-12

**Context:** ADR-075's two-column `About` grid used `md:items-end`, so the right-column supporting sentence ("With clarity, speed, and no drama, from first sketch to shipped product.") sat at the bottom of the row, level with the headline's last line. The user asked for it to sit at the top instead.

**Decision:** Changed the grid's vertical alignment from `md:items-end` to `md:items-start` in `components/studio/about.tsx`, so the supporting sentence now sits level with the "About us" eyebrow at the top of the row instead of the headline's baseline.

**Consequences:** Purely a vertical-alignment change — column widths, text alignment, and the pill row below are unaffected. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot confirming the supporting line now aligns to the top.

---

## ADR-077: About's supporting line re-aligned to the headline, not the eyebrow above it

**Date:** 2026-08-12

**Context:** ADR-076's `md:items-start` fix moved the supporting sentence to the top of the row, but "top of the row" turned out to mean level with the small "About us" eyebrow (the first element in the left column), not the "We help ambitious startups..." headline beneath it — not what the user meant by "align with the main text." Clarified: align with the headline, not the label.

**Decision:** Added an `invisible` (but layout-occupying, `aria-hidden="true"`) duplicate of the eyebrow — `<span className="invisible block text-caption uppercase">About us</span>` — as the first element in the right column, followed by the supporting paragraph with the same `mt-150` spacing the real eyebrow uses before the headline. This pushes the paragraph down to start exactly where the headline starts, using the same spacing unit rather than a guessed pixel offset, and stays correct if the eyebrow's type size ever changes since both columns reference the same class.

**Consequences:** The right-column paragraph now visually pairs with the headline it's meant to support, not the small label above it. The invisible spacer is `aria-hidden` so screen readers skip it — it exists purely for layout, not content. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot confirming the paragraph now lines up with "We help ambitious startups..." rather than "About us."

---

## ADR-078: Mission gained a three-card highlight row — honest, non-numeric content only, declined to copy the reference's fabricated stats

**Date:** 2026-08-13

**Context:** The user said Mission shouldn't be "just text" and supplied a reference screenshot (a bento-style mission section: pill badge, headline, and a grid of cards — partner avatars + "40+ partners," "90% ROI," "$2.5+ revenue generated," a dark testimonial-style card with "4.8/5" and "trusted by clients worldwide"). Every one of those cards asserts a specific, unconfirmed metric — client count, ROI percentage, revenue figure, star rating — for a studio that, per this site's own Founder section, is currently a single-founder practice with no published track record. Copying those numbers verbatim would be fabrication, the same category of problem this session has declined every other time it's come up (stats in ADR-018/019 rendered as `—` placeholders or flagged explicitly; the "trusted by" real-company-logo row withheld entirely). Asked the user directly how to fill the space rather than guessing; they chose "honest, non-numeric cards" — content drawn only from what's already true and already published on the site, no invented figures.

**Decision:** Kept Mission's centered pull-quote exactly as established in ADR-075 (its one distinguishing layout on this page) and added a three-card row beneath it, inside the same dark section: **Availability** ("Now accepting new projects," reusing the identical dot-indicator pattern already live on Home's Hero — the same real, current signal, not a new claim); **Focus** ("One focused practice: product design, brand identity, and AI-native product work" — a summary of facts already stated elsewhere on the site, in About's capability list and the Founder bio, not new information); **Access** ("Work directly with the founder, from kickoff to delivery" — consistent with the Founder section, which already names Ademola as the sole team member shown, so this asserts nothing beyond what's already public). None of the three cards states a number, percentage, rating, or client claim.

**Consequences:** Mission is no longer text-only, but the added content stays inside the same honesty boundary the rest of the site has held to throughout this build. If Unda Studio later has real, confirmable metrics (project count, client testimonials, a verified rating), those would replace these cards in a future pass — this ADR is not a permanent rejection of a stats-driven Mission section, just a refusal to fabricate one now. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot confirming the three cards render correctly beneath the pull-quote.

---

## ADR-079: Mission's headline left-aligned, dropping the centered pull-quote treatment

**Date:** 2026-08-13

**Context:** Immediately after ADR-078 added the three-card highlight row, the user asked to left-align the text. Mission's headline had been centered since ADR-075, deliberately as the page's one centered "manifesto" moment distinguishing it from every other left-aligned section — but with a left-aligned card row now sitting directly beneath it (ADR-078), the centered headline over a left-aligned grid read as mismatched, and the user's instruction resolved that in favor of left alignment.

**Decision:** Removed `mx-auto` and `text-center` from the headline wrapper in `components/studio/mission.tsx`, leaving `max-w-3xl` so the line-wrap width is unchanged — only the alignment and centering are gone. The highlight cards below were already left-aligned and needed no change.

**Consequences:** Mission's headline now sits flush with the left edge of the container, consistent with the card row beneath it and with every other section on the page. This does supersede ADR-075's specific claim that Mission "uniquely owns the centered pull-quote treatment" — that distinction no longer holds; Mission's signature is now its dark background and highlight-card row instead. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot confirming the headline and cards now share the same left edge.

---

## ADR-080: Mission section polish — wider headline measure, per-card icons, soft ambient glow to fill the dead right-side space

**Date:** 2026-08-13

**Context:** After the left-alignment change (ADR-079), the user shared a screenshot and asked to make the section "visually appealing" — a direct aesthetic critique, not a specific bug. Comparing against the screenshot: the headline and cards occupied only the left ~60% of a 1440px-wide dark section, leaving a large, flat, empty void on the right with nothing to anchor it; the three highlight cards had only a label and one line of body text, reading as thin/underweight for how much bordered box surrounded them.

**Decision:** Three changes to `components/studio/mission.tsx`, all cosmetic — no content changed. (1) Widened the headline wrapper from `max-w-3xl` to `max-w-4xl` so the pull-quote uses more of the section's horizontal space, narrowing the empty gap on the right. (2) Added a lucide icon (`Sparkles`/`Compass`/`UsersRound`) above the label in each highlight card, matching the icon-plus-label pattern already established elsewhere on this same page (About's capability pills, Daily Stack's tool grid) rather than inventing a new visual language; added `hover:border-neutral-600` for a small interactive cue. (3) Added a single large, blurred, low-opacity (`bg-brand-accent/10`, `blur-[120px]`) radial glow positioned top-right of the section (`absolute`, `pointer-events-none`, `aria-hidden`) to give the remaining dark space some atmosphere instead of flat black — the same "soft glow on a dark hero" technique used by Linear and Vercel, both named as direct visual references in this repo's own CLAUDE.md.

**Consequences:** Purely visual — no new claims, data, or interactive behavior. The glow is decorative only (`aria-hidden`, `pointer-events-none`), so it doesn't affect accessibility tree or layout of real content. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot at 1440px confirming the wider headline, the three card icons, and the glow all render correctly together.

---

## ADR-081: Services page hero redesigned — same dead-space fix as Mission, filled with a real derived stat instead of decoration; fixed a duplicate sentence

**Date:** 2026-08-13

**Context:** The user shared a screenshot of `/services`' hero (`app/(frontend)/services/page.tsx`) and asked for a redesign — the same structural problem ADR-080 fixed on Mission: a left-aligned headline and paragraph in a dark section, with roughly 40% of the 1440px width sitting empty on the right with nothing in it. Investigating for a way to fill that space with something real (per this session's standing no-fabrication rule) surfaced two additional facts: `SERVICES` in `lib/services-data.ts` has exactly 8 real, already-published entries, and the hero's supporting sentence ("Engage us for one service or the full arc from strategy to shipped product, the same team, the same standard, throughout.") was being repeated **verbatim** immediately below in `ServicesList`'s own section header — an unintentional content duplication, not a design choice.

**Decision:** Restructured the hero into the same two-column pattern as Mission (`grid-cols-[3fr_2fr]`, `items-end`): headline/paragraph in the wider left column, unchanged; a bordered stat card in the right column showing `08` (computed as `String(SERVICES.length).padStart(2, "0")`, not a hardcoded number, so it can't drift out of sync if a service is added or removed) with the caption "Services under one roof, from first sketch to shipped product." Added the same ambient top-right glow treatment as Mission (`bg-brand-accent/10`, `blur-[120px]`, `aria-hidden`) for visual consistency between the site's two dark hero sections. Separately, changed `ServicesList`'s duplicate sentence to "Each service stands on its own, or combines into a single connected engagement." — distinct wording, same underlying claim, no longer a copy-paste repeat of the hero.

**Consequences:** The Services hero no longer has dead space, and the number shown is real and self-updating rather than a decorative or invented figure. The duplicate-sentence fix is a small content-quality catch that wasn't part of the original ask but was directly adjacent and worth fixing in the same pass rather than leaving a known duplication in place. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots of both the hero and the `ServicesList` header confirming the new layout and the corrected copy.

---

## ADR-082: Services hero rebuilt from scratch — full-width stretched headline, real service index, ambient glow removed for a flat solid black

**Date:** 2026-08-13

**Context:** The user asked for a completely different design for the Services hero — new content and layout, not a tweak of ADR-081's version — and explicitly required the background be solid black, "not grainet" (gradient). ADR-081's `bg-brand-accent/10 blur-[120px]` glow was the likely source of that complaint: a large, heavily-blurred, low-opacity radial fill can render with visible banding/softness rather than reading as flat black, especially at the section's actual size.

**Decision:** Rebuilt `app/(frontend)/services/page.tsx`'s hero entirely. Removed the glow div and the `relative overflow-hidden` wrapper it required — the section is now a plain `bg-neutral-900` with no decorative layer, matching every other flat-black section on the site. Replaced the plain `text-h1` "Services." heading with a full-width `StretchText fit="size"` treatment (the same technique proven on Founder's "Meet Ademola," ADR-072) so "Services" spans edge-to-edge as a bold visual statement — reusing an established, tested pattern rather than a new one. Replaced the stat card with a real, functional index: all 8 actual `SERVICES` entries rendered as a two-column numbered list (`01`–`08`, `font-mono` numerals matching the numbering style already used in Home's Hero services list), sitting beside the supporting paragraph below a divider line. This list is live data, not decoration — every title is pulled directly from `lib/services-data.ts`, so it can't drift from what `ServicesList` actually renders below it.

**Consequences:** The hero now has a genuinely different composition from ADR-081 (giant stretched wordmark + functional index, versus a two-column measure-plus-stat-card layout) while keeping the same underlying real data and honesty standard — nothing here is fabricated or purely decorative. Confirms `StretchText fit="size"` as a repeatable "big headline" pattern beyond Founder. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots at both 1440px and 390px confirming the stretched headline, the flat black background with no gradient, and the service index rendering correctly (single column on mobile, two columns on desktop).

---

## ADR-083: Services hero's stretched headline changed from "Services" to "Sketch to shipped."

**Date:** 2026-08-13

**Context:** The user asked whether the giant stretched headline could say something other than the literal word "Services" — a fair critique, since it just repeated the page's own nav label/title rather than saying anything. Presented three candidate lines pulled from language already used elsewhere on the page (echoing `ServicesList`'s "full-stack design partner" line, the supporting paragraph's own "sketch to shipped product" phrase, or the real one-founder-practice fact from the Founder section); the user picked the one echoing the supporting paragraph.

**Decision:** Changed the `StretchText` content in `app/(frontend)/services/page.tsx` from "Services" to "Sketch to shipped." — no other changes. The longer string needs a smaller scale-up ratio to fill the container than the 8-character "Services" did, so the rendered type is a bit smaller relative to the container, still spanning edge-to-edge via the same `fit="size"` mechanism.

**Consequences:** The hero headline now directly echoes the supporting paragraph immediately below it ("...from strategy to shipped product...") rather than restating the page title, giving the section its own distinct message instead of a redundant label. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot confirming the new phrase renders full-width with correct letter proportions.

---

## ADR-084: Mission redesigned to match the new Services hero pattern — glow removed, stretched headline extracted from the real mission statement

**Date:** 2026-08-13

**Context:** Right after ADR-082/083 rebuilt the Services hero (flat black, no glow, `StretchText` headline, real supporting content), the user shared a screenshot of Mission — still carrying ADR-080's ambient glow treatment — and asked for the same kind of redesign. The glow was the same "not flat black" issue already resolved on Services.

**Decision:** Removed the `relative overflow-hidden` wrapper and the `bg-brand-accent/10 blur-[120px]` glow div entirely — `components/studio/mission.tsx`'s section is now plain `bg-neutral-900`, matching Services. Extracted a short, punchy phrase — "Craft over speed." — directly from the existing, already-approved mission sentence ("...without trading craft for speed, or speed for craft") and set it as a `StretchText fit="size"` headline spanning the full container width, the same technique now used on Founder and Services. The complete original mission sentence stays on the page verbatim, moved below the headline as supporting body text under a divider rule (mirroring Services' divider-then-content structure) rather than being cut. The three highlight cards from ADR-078/080 (Availability/Focus/Access) are unchanged in content, kept as a three-card row below.

**Consequences:** Mission and Services heroes now share one consistent visual language: eyebrow → stretched full-width headline → divider → supporting content, both flat black with no decorative glow. The short headline is a genuine extraction from copy the user already approved, not a new claim. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot confirming the flat black background, the stretched headline, and the full mission sentence and cards rendering correctly below it.

---

## ADR-085: Mission's supporting sentence forced to one line — but only above 1280px, to avoid an off-screen overflow on mobile

**Date:** 2026-08-13

**Context:** The user asked for the mission sentence ("To help ambitious teams build products and brands worth remembering, without trading craft for speed, or speed for craft.") to render on one line instead of wrapping. The first attempt applied `whitespace-nowrap` unconditionally. Checking it across viewports (via `page.evaluate` measuring `scrollWidth`) before calling it done caught a real problem the screenshot at one width wouldn't have shown: the sentence needs roughly 967px of unwrapped width, so at the mobile viewport (390px) it overflowed straight off the right edge of the screen, clipped mid-word with no wrap and no visible way to read the rest — a real regression, not just a stylistic quirk.

**Decision:** Changed the class to `whitespace-normal xl:whitespace-nowrap` — wraps normally by default (safe on mobile/tablet/small-laptop widths), and only forces a single line at Tailwind's `xl` breakpoint (≥1280px) and up, where the container's available width (1136px inside the 1200px-capped `Container`) comfortably exceeds the sentence's ~967px natural width with margin to spare. The narrower `lg` (1024px) breakpoint was checked and rejected — at exactly 1024px viewport the container's available width (960px) is a few pixels short of what the sentence needs, which would have reintroduced the same overflow bug at a common laptop/split-screen width.

**Consequences:** The sentence renders on one line on typical desktop viewports (the common case, and what the user asked for) while remaining fully readable and non-overflowing on mobile and mid-size viewports. This is the kind of thing that looks correct in a single screenshot and breaks silently elsewhere — verified by measuring actual computed text/container widths across seven viewport sizes (768, 900, 1024, 1100, 1150, 1200, 1280) rather than trusting one screenshot. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots at both 390px and 1440px confirming correct wrapping behavior at each end.

---

## ADR-086: Divider rule removed from both the Mission and Services heroes

**Date:** 2026-08-13

**Context:** The user shared a cropped screenshot of the thin `border-neutral-700 border-t` divider line that ADR-084/082 had added between each hero's stretched headline and its supporting content, and asked to remove it from both `/studio` (Mission) and `/services`.

**Decision:** Removed `border-neutral-700 border-t` (and the `pt-400`/`pt-600` padding that existed specifically to space content off that border) from both `components/studio/mission.tsx`'s supporting paragraph and `app/(frontend)/services/page.tsx`'s content row. Services' row gained a small `pt-100` in its place to keep a touch of breathing room now that the visual separator is gone, rather than leaving the supporting content flush against the `mt-600` spacing alone. Mission needed no equivalent addition — its `mt-400` alone still reads correctly without the rule.

**Consequences:** Both hero sections now flow directly from the giant stretched headline into their supporting content with no rule between them — a cleaner, quieter transition. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots of both pages confirming the line is gone and spacing still reads correctly.

---

## ADR-087: Work page split into a black hero and a white content section, matching Mission and Services

**Date:** 2026-08-13

**Context:** The user shared a screenshot of `/work`'s current single white section (heading, description, filter tabs, and project grid all together) and asked for it to be redesigned to match the pattern now established on `/studio` and `/services` — a black hero followed by a white content section.

**Decision:** Split `app/(frontend)/work/page.tsx` into two sections. The new hero is `bg-neutral-900` with the same structure as Mission/Services: an "Our work" eyebrow, the heading as a `StretchText fit="size"` full-width treatment (text unchanged — still "Selected Work.", just restyled, since this request was about layout/background, not copy), and the existing supporting paragraph unchanged in wording but recolored for the dark background (`text-neutral-400`). A second, separate `py-1200` section (default light background) now holds the filter tabs and `WorkGrid` — previously bundled into the same section as the hero. Removed `WorkGrid`'s internal `mt-600` top margin on the filter-tabs row, since that spacing assumed it would always follow a heading block in the same section; now that the tabs are the first element in their own section, the extra margin was redundant on top of the section's own `py-1200` padding.

**Consequences:** `/work` now visually matches the black-hero/white-content pattern shared by Mission and Services. The case-study grid, filtering behavior, and empty state are all unchanged — this was purely a section/background restructuring. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot confirming the black hero, the white content section, and the filter tabs/grid all render correctly.

---

## ADR-088: Services hero's numbered service index removed; supporting paragraph expanded to fill the space

**Date:** 2026-08-13

**Context:** The user asked to remove the `01`–`08` numbered service list from the Services hero (added in ADR-082) and keep only the supporting paragraph, asking for that paragraph to be made longer.

**Decision:** Removed the two-column grid, the numbered `<ol>`, and the now-unused `SERVICES` import from `app/(frontend)/services/page.tsx`. The paragraph is now the hero's only supporting element, widened to `max-w-2xl` (previously `max-w-sm`, sized for a narrow column next to the list) and expanded with a second sentence: "Whether it's product design, brand identity, or AI-native product work, every engagement gets the same attention to detail, from the first sketch to the last pixel shipped." This isn't a new claim — "product design, brand identity, and AI-native product work" is the exact same three-service summary already used on Mission's "Focus" card (ADR-078), just restated here rather than introducing anything unconfirmed.

**Consequences:** The Services hero is now simpler — headline and a single, longer paragraph, no list. The real 8-service index that was removed is still fully present just below, in `ServicesList`'s own numbered/full-detail listing, so no information was lost, only de-duplicated from the hero. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live screenshot confirming the list is gone and the expanded paragraph reads correctly.

---

## ADR-089: Services paragraph reverted to its original wording with a larger font size instead; Mission's three highlight cards removed

**Date:** 2026-08-13

**Context:** Two corrections from the user in one message. First, ADR-088's "make it longer" was a misread — the user meant increase the paragraph's font *size*, not add more sentences; they asked to revert the added content. Second, they asked to remove the three highlight cards (Availability/Focus/Access, added in ADR-078/080/084) from Mission on `/studio`.

**Decision:** In `app/(frontend)/services/page.tsx`, reverted the paragraph to its original single-sentence wording ("Engage us for one service or the full arc from strategy to shipped product, the same team, the same standard, throughout.") and changed its size from `text-body-lg` to `text-h4` — a real size increase, not a word-count increase. In `components/studio/mission.tsx`, removed the `HIGHLIGHTS` array, its imports (`Compass`, `Sparkles`, `UsersRound` from `lucide-react`), and the three-card grid entirely; the section now ends right after the supporting sentence beneath the "Craft over speed." headline.

**Consequences:** Services hero: headline, then one short, larger-type sentence — no added claims, matching what ADR-088 should have done the first time. Mission: shorter section, ending with headline + one sentence, no cards — the honest Availability/Focus/Access content isn't reused elsewhere, so if any of it is wanted again later it would need to be re-added, not recovered from another source of truth. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots of both sections confirming the revert and the removal.

---

## ADR-090: All three hero subtexts (Work, Services, Studio) enlarged to `text-h4` and widened to span the full container, without stretching the letterforms

**Date:** 2026-08-13

**Context:** The user asked for a consistency pass across all three of the site's black hero sections (`/work`, `/services`, `/studio`): increase the supporting paragraph's size, make it span edge-to-edge like the giant headline above it, and explicitly *not* apply the `StretchText` letterform-stretching technique to that paragraph — the headline is the only place that treatment belongs.

**Decision:** In all three files (`app/(frontend)/work/page.tsx`, `app/(frontend)/services/page.tsx`, `components/studio/mission.tsx`), changed the supporting paragraph from `text-body-lg` (17px) to `text-h4` (20px) and removed the width constraints that were capping it short of the container (`max-w-[65ch]` on Work, `max-w-2xl` on Services, nothing on Mission but it still needed a size bump). None of the three paragraphs use `StretchText` or any `scaleX`/`whitespace-nowrap` trick — they're plain block-level `<p>` elements that now fill 100% of the container width and wrap naturally, so "spans edge to edge" comes from the block's own width, not from distorting the text the way the headline is. Also removed Mission's `xl:whitespace-nowrap` single-line rule from ADR-085/086 — forcing one line at a now-larger font size would have reintroduced the same overflow risk that fix was built to avoid, and the new instruction (full-width, natural wrapping) supersedes the earlier one-line request.

**Consequences:** All three hero sections now have a consistent, larger, full-width supporting paragraph beneath their `StretchText` headline, with a clear rule going forward: `StretchText` is for headlines only, body text just gets `w-full`/no-max-width and wraps normally. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots of all three pages at both 1440px and 390px, confirming larger, full-width, non-distorted text with no overflow on mobile.

---

## ADR-091: Journal given its own black-hero page matching Work/Services/Studio, without touching the shared `ComingSoon` component

**Date:** 2026-08-13

**Context:** The user asked for Journal to match the black-hero pattern now shared by Work, Services, and Studio (ADR-082/084/087/090). Journal currently renders via a generic `components/marketing/coming-soon.tsx` component — light background, plain `text-h1`, a "Back to home" button — that is also reused, unchanged, by `/privacy` and `/terms`. Restyling `ComingSoon` itself would have pulled the legal pages into a giant-headline dark-hero treatment they never asked for and that doesn't suit legal copy.

**Decision:** Gave `app/(frontend)/journal/page.tsx` its own dedicated markup instead of calling `ComingSoon`, matching the exact structure used on Work/Services/Studio: a `bg-neutral-900` hero with a "Journal" eyebrow, a `StretchText fit="size"` headline, and a `text-h4` full-width supporting paragraph (the original description text, unchanged in wording), followed by a separate white section holding the "Back to home" button so the page still ends on the same black-then-white rhythm as the other three. The headline text is "Coming soon." — a short phrase reflecting Journal's real, current status — rather than repeating "Journal" (already stated in the eyebrow) or forcing the full original title sentence into the giant-text treatment it wasn't suited for. `Privacy` and `Terms` continue to use `ComingSoon` completely unchanged.

**Consequences:** All four main content pages (`/work`, `/services`, `/studio`, `/journal`) now share one consistent visual identity. `ComingSoon` remains available and untouched for legal/utility pages that shouldn't carry the marketing-hero treatment. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots confirming Journal's new hero renders correctly and Privacy is unaffected.

---

## ADR-092: Home page's text and images made editable from `/admin` via a new Payload Global — the first non-collection CMS content in the project

**Date:** 2026-08-14

**Context:** Extended discussion (not a quick ask) about whether Payload should grow beyond case studies/testimonials into a general site editor. Landed on a deliberately bounded scope after several rounds of clarification: Home page only for now (not a sitewide page-builder), text and images only (no new sections/pages from the dashboard — those stay code-only), and specifically **not** a Blocks-based page builder — the user confirmed new sections/pages should keep being built in code "for now." The motivating example was concrete: wanting to add real photos to Home's scrolling gallery strip (currently placeholder boxes) and edit copy, both without needing an engineering pass, with the explicit expectation that edits could come from either the dashboard or a future code/script-driven update, with no divergence between the two since both write to the same underlying record.

**Decision:** Added `src/globals/home.ts`, Payload's first `Global` (a singleton document, distinct from the existing `collections` which are repeatable) registered in `payload.config.ts`. Scoped to the four genuinely Home-exclusive sections — Hero, Who We Are, Services Summary, Selected Work — each as a field group with `defaultValue` set to the exact current hardcoded copy, so the admin form shows real content on first load, not blank fields. Two sections were deliberately **excluded**: `Process` and `FinalCta` are also rendered on `/services`, and editing their text from a "Home" global would silently change that other page too — those stay hardcoded until they get their own, separately-scoped decision. The underlying `SERVICES`/`HOME_SERVICE_TITLES` data (tags, descriptions) also stays in `lib/services-data.ts`, shared with `/services` — only the page-specific heading/intro text around it is editable per page.

Two array fields were added specifically for images: `hero.reviewerAvatars` (replacing the generated placeholder avatars once real client photos exist — this is the exact TODO flagged in the code since ADR-033) and `whoWeAre.gallery` (the scrolling marquee strip the user named as the motivating example). Both start empty and fall back to their existing placeholder rendering (generated avatars, icon boxes) exactly as before — no fabricated seed images, consistent with this project's standing no-fabrication rule.

`Home` (`app/(frontend)/page.tsx`) became an async Server Component that fetches the global once via a new `getHomeGlobal()` helper in `lib/payload.ts` and passes the relevant slice to each section as a `data` prop — including to `ServicesSummary`, a client component (`"use client"`, owns accordion state), which can't fetch server data itself but can receive it as a prop. Every text usage in `Hero`, `WhoWeAre`, `ServicesSummary`, and `SelectedWork` follows the pattern `data?.field || "<original hardcoded string>"` — a JS-level fallback in addition to Payload's own field-level `defaultValue`, so the page cannot render blank even in an edge case where the global document doesn't resolve as expected. `HomeGlobalData` was hand-written in `lib/payload.ts` rather than generated, matching the project's existing convention (ADR-047: the `payload generate:types` CLI crashes on this machine's Node version).

**Consequences:** This is the first Payload content in the project that isn't a repeatable collection — establishes the `Global` pattern for any future single-page editable content (Studio, Services, Work hero text, if ever extended the same way). Verified end-to-end: `pnpm format`/`lint`/`check-types`/`build` all clean; live screenshots of Home (Hero and Who We Are sections, both before-fold and after scrolling into view) confirmed pixel-identical rendering to the pre-conversion hardcoded version, proving the fallback path works; `pnpm build` shows `/` still prerenders statically with the existing `revalidate = 60`, so admin edits surface within a minute, same tradeoff already accepted for Testimonials/Selected Work. Could not verify the `/admin/globals/home` screen directly — it requires the user's own login, which wasn't attempted — so the user should confirm the dashboard form itself looks and behaves as expected before relying on it. `payload`'s own CLI is still broken on this machine (same root cause as ADR-047, an ESM/CJS interop crash unrelated to this change), so type generation and any future CLI-based scripting around this global will need the same workaround already in use.

---

## ADR-093: The Global pattern extended to Studio, Services, Work, and Contact — every remaining page's text and images are now admin-editable

**Date:** 2026-08-14

**Context:** Immediately after ADR-092 shipped and the user confirmed the `/admin/globals/home` screen worked as expected, they asked to extend the same treatment to "the other pages" — clarified as: yes, the same Payload Global pattern, not a visual redesign. Followed the exact scope rule already established: text and images editable, layout/structure/icon-coupled data stays in code, shared components (rendered on more than one page) stay out of any single page's global.

**Decision:** Added four more Globals, one per remaining real page, following ADR-092's exact structure (field groups per section, `defaultValue` matching current copy, JS-level `|| "fallback"` in every component on top of that):

- **`src/globals/studio.ts`** (`studio-page`) — the largest: Mission, About, Values (as a real editable array — title+description per value, not just the intro text), Founder (headline, the two-tone "Founder"/", Unda Studio" label kept as two separate fields so the color split survives editing, bio, and a photo upload that falls back to the existing `/founder.png`), Daily Stack intro, Philosophy, and FAQ (also a real editable array — this directly answers the standing code comment on `FAQS` that called it "drafted... meant to be edited once the user confirms real answers"). Icon-coupled lists (Daily Stack's tool logos, About's capability pills) stay in code, same reasoning as Home's service tags.
- **`src/globals/services.ts`** (`services-page`) — hero, the `ServicesList` intro, and the `Industries` intro. `Process` stays code-only (shared with Home).
- **`src/globals/work.ts`** (`work-page`) — hero text only; the case studies themselves are already editable via the separate Case Studies collection.
- **`src/globals/contact.ts`** (`contact-page`) — hero text, the contact email (now driving both the display text and the `mailto:` link from one field), and the three-item "What to expect" list as an editable array.

All four pages (`app/(frontend)/studio/page.tsx`, `services/page.tsx`, `work/page.tsx`, `contact/page.tsx`) became (or already were) async Server Components fetching their global and passing slices down as `data` props, exactly matching Home's wiring. `Work` and `Contact` gained `export const revalidate = 60` (Studio and Services already had it from earlier work).

**Consequences:** Every real content page in the app (Home, Studio, Services, Work, Contact) is now on the same editable-content pattern; Journal, Privacy, and Terms remain plain `ComingSoon`/static pages with no CMS content yet since they don't have real copy worth making editable at this stage. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean, dev server schema-synced without errors), and live screenshots of all four pages plus a close-up of Founder's photo/bio and the FAQ accordion, confirming pixel-identical rendering and correct fallback behavior with no console errors.

---

## ADR-094: The shared `SERVICES` list converted from static code to a real `services` collection — the one piece ADR-093 missed

**Date:** 2026-08-14

**Context:** The user caught a real gap in ADR-093: the actual per-service list on `/services` (`ServicesList` — title, description, tags, placeholder graphic, pricing/timeline row) was still reading from the static `SERVICES` array in `lib/services-data.ts`, not from Payload. This had been a deliberate exclusion in both ADR-092 and ADR-093, reasoned as "shared data used by two pages, so keep it in code" — but that reasoning was wrong for this specific case. `Process` and `FinalCta` were correctly excluded because they're the *same component* rendered on two pages with *page-specific* text that shouldn't cross-edit. `SERVICES` is different: it's the *same content* (the same 8 services, same descriptions) intentionally shown on both Home and `/services` — exactly the case for one shared, editable source of truth, not per-page copy.

**Decision:** Added `src/collections/services.ts`, a real repeatable Collection (not a Global — this is 8 distinct records, not singleton page content) with `title`, `description`, a `tags` array, an `order` number for display sequencing, and a `featuredOnHome` checkbox replacing the old fixed `HOME_SERVICE_TITLES` list — an admin can now choose which services appear in Home's 4-item summary instead of that being hardcoded by title-matching. Registered it in `payload.config.ts` and added `Service` type + `getServices()` to `lib/payload.ts`, following the same hand-written-type convention as everything else (ADR-047).

Since collections don't auto-populate the way a Global's `defaultValue` does, migrating existing content required an actual data migration, not just a schema change. Wrote a temporary `POST /api/seed-services` route handler that read the 8 real entries from `lib/services-data.ts` and created them as real documents (skipping if any already existed, so it's safe to have accidentally hit twice) — hit it once via `curl`, confirmed all 8 titles came back in the response, then **deleted the route file immediately after**, since an unauthenticated data-writing endpoint has no business staying live in the codebase past its one-time use.

`ServicesList` (`/services`) and `ServicesSummary` (Home, via a new `services` prop threaded through `app/(frontend)/page.tsx` since `ServicesSummary` is a client component and can't fetch its own data) both now call `getServices()` first and fall back to the original static `SERVICES`/`HOME_SERVICE_TITLES` data only if the collection is empty — same fallback discipline as every other converted field this session.

**Consequences:** The 8 services are now single-sourced from one collection instead of a static file duplicated in spirit across two components — editing "Product Design"'s description in `/admin` now correctly updates both Home and `/services` consistently, which is the actually-correct behavior for genuinely shared content (unlike Process/FinalCta, which stay separate on purpose). `lib/services-data.ts` remains in the codebase as the fallback/seed source, not deleted. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots of both `/services`' full list and Home's featured-4 summary (including the working accordion), confirming pixel-identical rendering to the pre-migration static version and zero console errors, now reading from the real database.

---

## ADR-095: Confirmed — Home's compact service accordion and Services' full detail cards are meant to look different

**Date:** 2026-08-14

**Context:** Right after ADR-094 unified the underlying service *data*, the user flagged that the two pages still *look* different — Home renders each service as a compact numbered accordion row (title, tags, one-line description, no image), while `/services` renders a fuller card (title, a placeholder graphic, tags, description, plus a "Starts at / Timeline" row). Asked whether this was a leftover bug from the CMS migration.

**Decision:** Confirmed it's not a bug — `ServicesSummary` (Home) and `ServicesList` (Services) have always been visually distinct components built for different roles (Home as a compact teaser, `/services` as the full detail page), independent of today's data-layer change. Asked the user directly whether they should be made visually consistent or kept as intentionally different; they confirmed **Home-as-compact-teaser vs. Services-as-full-detail is the right call** — no code change.

**Consequences:** No file changes. Recorded here specifically so a future pass doesn't mistake this confirmed, deliberate visual difference for an inconsistency left over from ADR-094 and "fix" it unprompted.

---

## ADR-096: ADR-095's confirmation surfaced directly in the Payload admin, not just in DECISIONS.md

**Date:** 2026-08-14

**Context:** The user asked to reflect ADR-095's confirmation "on the payload dashboard" — the decision log entry alone isn't visible to someone editing content in `/admin`, and a future editor changing a service's copy there has no way to know it feeds two differently-styled sections without reading this repo's ADR history.

**Decision:** Added a collection-level `admin.description` to `src/collections/services.ts`: "Shown in two places with different layouts, by design: the full list on /services (image, tags, pricing/timeline), and a compact accordion teaser for featured items on Home. Same content, same edit — the two pages are just meant to look different." Payload renders a collection's `admin.description` directly in its list view in the dashboard, so this is now visible to anyone editing Services from `/admin`, not just anyone reading `DECISIONS.md`.

**Consequences:** No behavior change — purely an admin-UI documentation addition. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean). Could not screenshot the rendered admin list view directly (requires the user's own login, same limitation noted in ADR-092/093) — worth a quick check at `/admin/collections/services` to confirm the description text appears where expected.

---

## ADR-097: Services' image, "Starts at", and "Timeline" turned into real editable fields — they weren't actually in the collection

**Date:** 2026-08-14

**Context:** The user pushed back on ADR-096's fix as too shallow — a description string doesn't let anyone actually edit anything. They clarified what they meant by "the element on the dashboard": they want to edit *both* the Home teaser and the full `/services` detail card, including its image and its "Starts at" / "Timeline" row. Checking `ServicesList` against the ADR-094 migration found the real gap: the per-service placeholder graphic was (and still is, when empty) a decorative generated pattern, `<PlaceholderGraphic variant={i}>` — never a real image. And "Starts at: Custom pricing" / "Timeline: Custom timeline" were literal hardcoded JSX strings, identical for every service, not per-service data at all. ADR-094's migration had only carried over `title`/`description`/`tags` — it missed that the detail card has more editable surface than that.

**Decision:** Added three fields to `src/collections/services.ts`: `image` (upload, relation to Media, optional — falls back to the existing generated placeholder graphic when empty, so no fabricated stand-in image is required), `startsAt` (text, default `"Custom pricing"`), and `timeline` (text, default `"Custom timeline"`) — both editable per service now instead of one hardcoded value shared by all eight. Updated the `Service` type and `getServices()` (now fetching at `depth: 1` so the image relation resolves to a full Media object) in `lib/payload.ts`. Rewired `ServicesList` to render the real image when present (`next/image`, falling back to `PlaceholderGraphic` otherwise) and the real `startsAt`/`timeline` values (falling back to the same "Custom pricing"/"Custom timeline" defaults as before when a service predates these fields or leaves them blank).

**Consequences:** Every visible element of the `/services` detail card — title, description, tags, image, pricing, timeline — is now a real field in one place, editable from `/admin`, rather than three of six being invisible hardcoded literals. The 8 services created in ADR-094's migration predate these three new fields, so they currently read as empty/default (placeholder graphic, "Custom pricing", "Custom timeline") until edited — same visual result as before, nothing regressed. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean, schema synced without errors) and a live screenshot confirming the detail card and pricing/timeline row still render correctly with the new fallback-driven fields.

---

## ADR-098: Vercel deployment and CI both fixed — `PAYLOAD_SECRET` was never set, and Turborepo was stripping the env vars that were

**Date:** 2026-08-14

**Context:** The user reported a failed Vercel deployment (`dpl_5LbNJeJbNS4mmjBGECTSoK4e61o4`) and a failing GitHub Actions CI run on the same push. `vercel inspect --logs` showed the actual failure: `Error: missing secret key. A secret key is needed to secure Payload.`, thrown while statically prerendering `/contact` — Payload's `buildConfig({ secret: process.env.PAYLOAD_SECRET })` had nothing to read. The build log's own warning block was the second clue: it listed `DATABASE_URL`, `BLOB_READ_WRITE_TOKEN`, and every Neon-provisioned Postgres variable as "set on your Vercel project, but missing from turbo.json" — Turborepo strips any environment variable not explicitly declared on a task before running it, and `turbo.json`'s `build` task had no `env` field at all. So even the variables that *were* configured on Vercel never reached `next build`. Checked `vercel env ls` directly and confirmed `PAYLOAD_SECRET` specifically had never been added to the Vercel project at all — the other three (`DATABASE_URL`, `BLOB_READ_WRITE_TOKEN`, and the auto-provisioned Neon vars) existed but were being filtered out by the missing `turbo.json` declaration. GitHub Actions' CI runs the same `pnpm build` → `turbo run build` path with zero secrets configured at all — its last green run (commit `1f9275e`) predates Payload being added to the project entirely, so it's been broken the same way since.

**Decision:** Grepped the actual source for every `process.env.` read in `payload.config.ts` (the only place that matters) to get the precise, minimal list rather than guessing — exactly three: `PAYLOAD_SECRET`, `DATABASE_URL`, `BLOB_READ_WRITE_TOKEN`. Added `"env": ["PAYLOAD_SECRET", "DATABASE_URL", "BLOB_READ_WRITE_TOKEN"]` to `turbo.json`'s `build` task. Generated a fresh, cryptographically random secret (`openssl rand -hex 32`, not a reuse of the local dev value) and added it as `PAYLOAD_SECRET` to the Vercel project for Production, Preview, and Development — the CLI's `vercel env add ... preview` (all-branches form) hung in a non-interactive loop for this CLI version, so fell back to the Vercel REST API directly (`POST /v10/projects/{id}/env`) using the locally-stored CLI auth token, which succeeded where the CLI didn't. Updated `.github/workflows/ci.yml`'s build step to pass `PAYLOAD_SECRET`/`DATABASE_URL`/`BLOB_READ_WRITE_TOKEN` from `${{ secrets.* }}` — the workflow file change is safe to make directly, but the actual secret *values* still need to be added in GitHub's own Settings → Secrets and variables → Actions by someone with repo admin access, which this session doesn't have.

**Consequences:** The Vercel deployment issue is fully resolved and verified live: pushed both fixes, watched the new deployment reach `Ready`, and confirmed `/`, `/services`, `/studio`, `/work`, `/contact` all return 200 on the actual production domain (`studio.undasolutions.com` — the deployment-specific preview URL 302s on its own, unrelated Vercel SSO deployment-protection redirect, not a bug). **CI remains red** until the three secrets are added on GitHub's side — the workflow is now correctly wired to use them the moment they exist, but no code change can supply secret values that live outside the repo. This is an explicit follow-up item, not silently left broken.

---

## ADR-099: Home hero's mobile hierarchy fixed — top row was wrapping cramped, and the tagline/rating order was inverted from a reference the user liked

**Date:** 2026-08-14

**Context:** The user shared a reference screenshot (a "Sylven" creative-studio mobile hero) and asked to match its visual hierarchy on our Home hero's mobile layout — explicitly not changing any content, only structure/spacing. Comparing our actual mobile render (390px) against the reference surfaced two concrete problems, not just a vague "doesn't feel right": (1) the top meta row (`text-caption` eyebrow left, "now accepting projects" status right) used `flex items-center justify-between` with no wrap allowance, forcing "A division of Unda Solutions" and "Now accepting new projects" to each fight for half the row width and wrap into cramped two-line columns; (2) the reference's order is headline → bold tagline (full width) → rating/avatar row (full width) → paragraph, but ours was headline → rating row → tagline, in a single `flex-wrap` container — reversed, and with no clear separation between the two.

**Decision:** Two changes to `components/home/hero.tsx`, both purely structural (no copy changed). The top row became `flex flex-col gap-100 sm:flex-row sm:items-center sm:justify-between` — stacks cleanly on mobile (each string gets the full row width, no wrap), unchanged side-by-side layout from `sm:` up. The tagline/rating block had its JSX order swapped (tagline now first in source, the avatar+rating block second) and the container changed to `flex flex-col gap-300 md:flex-row-reverse md:items-center md:justify-between md:gap-400` — on mobile (`flex-col`, no reverse) this renders in DOM order: tagline first, rating second, matching the reference; at `md:` and up, `flex-row-reverse` flips the *visual* order back to rating-left/tagline-right, reproducing the exact desktop layout that existed before this change, unchanged.

**Consequences:** Mobile now matches the reference's hierarchy — clean single-line meta row, headline, prominent full-width tagline, then the rating row as supporting proof beneath it, then the paragraph — while desktop is confirmed pixel-identical to before (verified via side-by-side screenshot comparison, not just code review). Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots at both 390px and 1440px.

---

## ADR-100: Three more mobile-only Hero refinements — two-line headline, smaller one-line tagline, tighter spacing before the paragraph

**Date:** 2026-08-14

**Context:** Following ADR-099, the user shared a real-device screenshot of the mobile hero and asked for three specific fixes, explicitly scoped to mobile only: let "Creative Studio" break onto two lines, shrink the tagline so "For small & medium scale businesses." fits on one line instead of wrapping to two, and remove the large gap between the avatar/rating row and the "We partner with..." paragraph.

**Decision:** All three changes in `components/home/hero.tsx`, gated behind `sm:`/`md:` breakpoints so desktop stays untouched. (1) The headline now renders two different ways depending on viewport: on mobile (`block sm:hidden`), the headline string is split on its first space into `headlineFirstWord` and `headlineRest`, each wrapped in its own `StretchText` so both stack as separate full-width-stretched lines; at `sm:` and up (`hidden sm:block`), the original single-line `StretchText` with the full headline renders unchanged. Splits generically on whitespace rather than hardcoding "Creative"/"Studio", so this still behaves reasonably if the headline is ever edited from the CMS to something other than two words. (2) The tagline's size changed from a flat `text-h4` to `text-body sm:text-h4` — smaller on mobile only, restoring the original size from `sm:` up. (3) The `mt-800` spacing above the paragraph/button/services-list block became `mt-200 md:mt-800` — tight on mobile, unchanged on desktop.

**Consequences:** All three fixes are mobile-only by construction (verified live, not assumed) — a side-by-side screenshot comparison at 1440px confirmed desktop renders pixel-identical to before. `StretchText`'s `children: string` prop meant the headline split had to produce plain strings, not JSX — resolved by deriving `headlineFirstWord`/`headlineRest` from `headline.split(" ")` with an explicit fallback rather than relying on possibly-`undefined` array indexing. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots at 390px (all three fixes visible) and 1440px (unchanged).

---

## ADR-101: Mobile's two-line headline switched to `fit="size"` — splitting "Creative Studio" into two words reintroduced the letterform-distortion bug from ADR-072

**Date:** 2026-08-14

**Context:** The user flagged the two-line mobile headline from ADR-100 as visibly stretched. Correct catch: both `StretchText` instances rendering `headlineFirstWord`/`headlineRest` were left on the default `fit="scale"` mode, which stretches text to fill its container via a `scaleX` transform. The original single-line "Creative Studio" never showed this problem because the full phrase is long enough that the scale ratio needed to fill the container stays close to 1 — but splitting it into two standalone words ("Creative", "Studio") for the mobile two-line treatment meant each one individually needed a much larger scale ratio to fill the same full mobile width, visibly fattening the letterforms. This is the exact same bug already diagnosed and fixed once before, for Founder's "Meet Ademola" headline (ADR-072).

**Decision:** Added `fit="size"` to both mobile `StretchText` instances in `components/home/hero.tsx` (the `headlineFirstWord` and conditional `headlineRest` lines) — switches them from transform-based stretching to font-size-based fitting, which fills the container width by growing the font size rather than distorting glyph proportions. Left the desktop single-line `StretchText` (`hidden sm:block`, full `headline` string) on the default `fit="scale"` — unchanged, since it was never distorted and this bug is specific to short strings.

**Consequences:** "CREATIVE" and "STUDIO" now render with normal, proportional letterforms at full mobile width, and are visibly larger than before since font-size growth (not horizontal stretching) is what fills the line now. Reinforces the standing rule from ADR-072: `fit="size"` for short strings/words that need to span a wide container, `fit="scale"` (default) only safe for strings already long enough that the required ratio stays near 1. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and a live 390px screenshot confirming undistorted letterforms.

---

## ADR-102: Mobile headline stopped forcing full-width fill entirely — even proportional, it still read as "stretched"

**Date:** 2026-08-14

**Context:** After ADR-101's `fit="size"` fix, the user shared a real-device screenshot and said it was still stretched "in a funny way." Looking closely, the individual letterforms were in fact proportional this time — `fit="size"` had done what it was supposed to. The actual problem was the underlying approach, not the fitting mode: forcing a short word like "STUDIO" to span 100% of a narrow mobile container, edge to edge with almost no margin and minimal space between characters, reads as "stretched" to the eye regardless of whether the mechanism is a letterform-distorting `scaleX` or a proportional font-size blow-up. A short word forced to fill a full line always looks unnaturally huge and cramped against the edges — that's inherent to the technique, not a bug in a particular `fit` mode.

**Decision:** Dropped `StretchText` (and the `headlineFirstWord`/`headlineRest` word-splitting logic) from the mobile headline entirely. It's now a plain `<span>` at the same `text-[clamp(2.5rem,9vw,7rem)]` size already used elsewhere, with `whitespace-normal` so the browser wraps "Creative Studio" naturally at its intrinsic size — no JS measurement, no forced width-fill, no per-word stretching. At mobile's clamp-minimum size (2.5rem), the full headline doesn't fit on one line, so it still wraps to two lines on its own, just at a natural, comfortably-sized weight instead of an artificially maximized one. Desktop (`hidden sm:block`) is untouched, still the original single-line `StretchText`.

**Consequences:** This removes `StretchText`/`fit="size"` from the mobile headline path entirely — the "two lines, each stretched to fill" idea from ADR-100/101 is fully superseded by "wrap naturally, don't force-fill." Worth remembering for any future full-width-headline request: `StretchText` is the right tool when the *goal itself* is an edge-to-edge look (Founder, Services/Work/Studio hero headlines, which are long enough or explicitly designed for that treatment) — it's the wrong tool when the real goal is just "let this break onto two lines," where natural wrapping at a fixed size is simpler and doesn't risk the cramped, forced look this ADR undoes. Verified via `pnpm format`, `pnpm lint`, `pnpm check-types`, `pnpm build` (all clean) and live screenshots at 390px (natural two-line wrap, no forced fill) and 1440px (unchanged).
